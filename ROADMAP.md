
# ROADMAP

## Editing

Deferred items are for items that need significant systems we cannot do now.
If you can do the item now, do NOT add to the deferred list.

Do NOT add new deferred work as a roadmap item. Place it in
`docs/DEFERRED.md`. If a TODO item is not FULLY completed, split the
TODO item, keeping the concise short dash point list with block infront and
split it as another item under the same version.

For example, if we complete A and B but not C and D:
```
### v0.5
- [x] do: A1, A3, A5
- [_] do: A, B, C, D
- [x] do: G, H, I
- [x] do: K, K2, K3
```

becomes:
```
### v0.5
- [x] do: A1, A3, A5
- [x] do: A, B
- [x] do: G, H, I
- [x] do: K, K2, K3
- [_] do: C, D
- [_] deferred `D0001` for later.
```

or defer to another version.

## VERSIONS

The milestone headings below are development labels. Not in line with project versioning.

### v0.17

- [x] pbr rendering support
- [x] ~/Documents/GitHub/node-graph-template
- [x] batch multiple worlds and cameras into one GPU command buffer, with world-scoped graph work submitted once per world and pipeline
- [x] submit instance, particle, ribbon and overlay uploads through dedicated SDL copy command buffers without a CPU fence; cycle destinations per view and expose actual upload bytes and submissions
- [x] submit async-eligible compute prefixes on dedicated command buffers when no dependency-bound GPU work precedes them; expose actual dispatch and async submission counts
- [x] replace the old fixed renderer with the render graph; remove the fixed pass API and flat pipeline, ship a default graph, and make the graph-owned `View` batch the only public rendering entry point
- [x] Studio render graph editor with Blender-style typed sockets and wires for images, buffers, entity lists, cameras and material channels, plus controls for culling, queue choice, async eligibility, GPU compute dispatch, transfers and resource lifetime
- [x] execute authored frustum, distance and tag culling as composable entity-flow nodes; Studio exposes only the controls those nodes consume and the backend refuses misplaced hints
- [x] integrate `SurfaceAppearance` colour, normal, roughness, ambient-occlusion and emissive maps with the default graph
- [x] sample `SurfaceAppearance.HeightMap` with parallax UVs in the opaque and G-buffer material paths
- [x] render pipeline is per world, not per process
- [x] render pipelines are saved in the world's export data
- [x] rendering pipeline debugger and profiler with per-node operations, CPU wall time, GPU timestamps, expandable stage images and histograms; the default graph ends at one `Output Image` after scene, debug and interface image composition
- [x] preserve valid per-node Vulkan GPU timestamps when an async-eligible compute prefix uses a dedicated command buffer submitted before the main graph command buffer
- [x] rendering pipeline additions: ambient occlusion, emissive, PBR and a default node setup containing all of them
- [x] apply the useful traffic, clear and CPU-to-GPU lessons from https://www.youtube.com/watch?v=SnNm7rSSvlg (Threat Interactive Tutorial: How To Optimize Almost Every Step In Modern Game Rendering)
- [x] review https://github.com/fini03/vkDuck for queue, resource and command-recording architecture
- [x] redo generic authored `raster`, `dispatch`, `viewer`, `capture` and pipeline/scope-owned graph-target allocation from `renderer-before-revert` against the current renderer instead of restoring the stale branch
- [x] connect Lighting service properties to both client and Studio rendering
- [x] concise `RUNNING.md` tutorial for publishing a CDN store and using it as a folder or HTTP server
- [x] concise `RUNNING.md` tutorial for pointing a game server at a custom CDN
- [x] concise `RUNNING.md` tutorial for adding a local store or remote origin to Studio
- [x] concise `RUNNING.md` tutorial for launching a client that accepts the server-announced CDN, grant and publisher key, with an explicit CDN override mode
- [x] ensure we can attach shaders to (visual) assets as well to change how they render: mesh, texture, gifs, images and videos
- [x] localtransparency property for clients (only visible in local side, server cannot see), take priority over standard transparency if not set to 0
- [x] character auto-rotation
- [x] set character with LocalTransparency=1 when you zoom in (make a camera controller system with shift lock, scroll zoom, I/O zoom, poppercam system - add items to ignore in zoom and make partly transparent using LocalTransparency). Would it be worth changing LocalTransparency to a component and we can add/remove as needed per-client?
- [x] add compute and postprocessing shaders to meshes/textures/pbr
- [x] wireframe view mode
- [x] add emissive textures / pbr
- [x] real mesh creation in real-time (EditableMesh) + ensure it renders properly
- [x] ensure EditableImage works and renders
- [x] add conservative occlusion culling after the renderer has a depth hierarchy and indirect draw path; explicit occlusion documents are refused until then
- [x] create portal test scenes for lighting through portals and out of portals (one light in portal, check if emits out of portal, two colored lights on either side of portal, check emits inside and mixes)
- [x] audit every remaining service, object and instance property so edits mutate authoritative state and reach their consumers
- [x] prototype portal seam light-field capture: render each room against a lit void, capture both directions, and project the matching result into the portal entrance; expose an `Enabled` property on Portal components to skip this capture path - prototype-scoped: the `portal-capture` node renders each enabled mouth's far room into a 128x128 probe cleared to the world ambient (fog disabled), and `deferred-lighting.frag` projects the nearest two probes out of their entrances as a windowed rectangle light; `Portal.Enabled = false` withdraws the seam and with it the probe, the spill and the light copies. Two projectors per view, opaque-list content, no interreflection - the full seam-lighting item below is where that grows up
- [x] check raytracing with portal test scene as well - there is no executable raytraced path yet: the catalogue's `raytrace` kind has no backend node, the renderer refuses such a graph whole and the profile falls back to Default PBR, pinned in `client.scene.worldpipelines`; portal scenes therefore light through the raster seam-copy path only until the raytrace port lands
- [x] split dependency-bound compute and later transfer work into traffic-plan command buffers; physical overlap remains unavailable because SDL exposes one unified queue rather than independent graphics, compute and transfer queues - `graph::PlanCommandBuffers` is the traffic plan, downloads ride a later-transfer buffer submitted after the main one, the async compute prefix is plan-gated, and dependency-bound compute stays in the main stream because the present is bound to the buffer that acquired the swapchain
- [x] `ShapeCast` needs a swept-volume walk and its own de-duplication rule; the current union of the start and end box bounds is loose over long sweeps, and the ray walk's run rule only applies to the centre line
- [x] show Universe in Explorer with mutable execution mode, maximum catch-up ticks, bus budget, channel queue limit and channels-per-world; show federated mode, world counts, fault counts, tick cost and bus traffic read-only
- [x] build ui features out (more roblox): flex list layouts - `UIListLayout` `Wraps`/`HorizontalFlex`/`VerticalFlex`/`ItemLineAlignment` and `UIFlexItem` with `FlexMode`/`GrowRatio`/`ShrinkRatio`, laid out, saved, bound and in the Properties panel
- [x] build out code editor with auto complete (more vs-code like, hover on keywords, right-side minimap) - hover tooltips from the same language-aware surface the completion uses, a run-stripe minimap with click/drag scroll, Tab accepts, matched prefix highlighted, kind/doc footer on the list
- [x] add per-world physics tick-rate and replication tick-rate - `WorldSettings` gains `PhysicsTickRate` and `ReplicationTickRate`, zero on either meaning "follow the tick rate"; `physics::PhysicsClock` is a per-world store resource that turns simulated seconds into fixed steps, so a world may solve slower or faster than it ticks and a fast world finishes its extra steps inside `physics.contacts`; the replication clock lives on `World` and holds the change bits across the ticks it does not publish, so a property written on a skipped tick still reaches the wire; both survive the `.agame` file and the universe snapshot, both reach a host process through `--physics-tick-rate`/`--replication-tick-rate`, and the control surface reports them. The studio gap this left is closed: clicking a world row in the Explorer selects it, and Properties edits `TickRate`, `IdleTickRate`, `PhysicsTickRate`, `ReplicationTickRate`, the simulated latency and the fault limit against a new `Universe::Reconfigure` - which ignores the name, because the registry is keyed on it, and which the editor follows with the `physics::SetPhysicsTickRate` push that `world` at L4 cannot make itself. The same pass fixed the universe row, which had never been selectable at all: its tree node was missing `ImGuiTreeNodeFlags_OpenOnArrow`, so every click toggled it open and the `IsItemToggledOpen` guard threw the selection away, leaving already-editable universe settings unreachable
- [x] full particle demo (spam a ton of emitters in a baseplate scene) - `examples/StressParticles.luau`: ten labelled bays carrying every authored `ParticleEmitter` property `Particles.luau` leaves untouched (`Brightness`, `LightEmission` against `LightInfluence`, `EmissionDirection`, `ShapePartial`, `InAndOut`, `TimeScale`, `ZOffset`, all four `Orientation`s, `Enabled` pulsed from the tick, and `LockedToPart` against `VelocityInheritance` on a swinging arm), and then 102,400 emitters on the baseplate at five particles each with `Additive` alternating so the draw loop's state grouping has two runs rather than one. 320 squared is not arbitrary: it is the largest grid the 524,288-slot default pool holds without refusing emitters, and a refused emitter is silent. All three of these scenes, and every other example shipped in `mono.engine/examples`, are reachable in the studio as `New Scene from Example` - in the `World` menu, in the universe's context menu in the explorer, and behind the Worlds panel's `Example...` button, which are the three places that already offer New World. One function submits the rows and it walks the staged directory through `examples::ExampleScenes` rather than a hand-kept list, so a scene added to the repository is in all three without a second edit
- [x] full physics demo (100k parts moving around) - `examples/StressPhysics.luau`: 100,000 unanchored blocks in a 240 metre tray whose floor and four walls are one rigid body re-placed every tick, so the low corner walks round the tray and nothing ever settles. `Cube.luau` is the pile at rest at ten thousand, which is the solver's worst case and a scene that ends; this is the case where every body has a velocity every tick, so nothing sleeps and the dynamic index is rebuilt from nothing each time. It costs five property writes a tick and no per-block work at all. **`client --script` will not simulate it** - a client installs `RegisterCharacterSystems` and not the pipeline, so the blocks hang in the air - and the two hosts that do are the studio's Play and `server --game`. `--physics-tick-rate` is the v0.17 knob the scene exists to be pointed at, and it is measured in the file's header: `release`, 24 threads, headless at 30 Hz, 600 ticks, the tick rate costs a 361 ms mean tick and a rate of 10 costs 125 ms - two ticks in three at 22 ms and the third carrying the whole solve. A hundred thousand contacting bodies is past interactive on this machine either way, which is the honest reading of a stress scene rather than a caveat on one
- [x] make a ico-sphere mirror ball scene (subdivisions, radius) - basically to stress test mirrors - `examples/StressMirrors.luau`: an ico-sphere subdivided in Luau (0 is 20 facets, 1 is 80, 2 is 320, 3 is 1,280) with a flat mirror tile on every face, sized from its own edge so `RADIUS` and `SUBDIVISIONS` both scale it, and an `EditableMesh` core behind them so a reflection has something to be on. **The finding is `scene::MAX_SURFACES`, which is sixteen** - a slot owns a ping-ponged texture pair, `AimSurfaceCameras` hands them out in walk order, and a camera that gets none is aimed every frame and reaches no screen - so the scene authors sixteen cameras spread over the ball and leaves the other facets plain, with `MIRRORS` as the deliberate lever for asking for more. `workspace.SurfaceBounces = 1` is a guard rather than a taste: a ball is the worst shape the automatic depth rule has, every pane can see most of the others so it says "deeper" at every level, and the passes go as `panes x (panes - 1) ^ (levels - 1)` - 16 at one level, 240 at two, 3,600 at three
- [x] tunnels demo portal void has parts in the center of the walk path, take screenshots in a test to ensure it works - the two drifting blocks travelled each tunnel's centre line at eye height, so one filled the mouth on the approach and was the first thing in the picture through every pane, and the isolated corridor - the one space in the scene that exists only on the far side of a hole - had one running its whole length. `DRIFT_SIDE` puts them two studs to the east, which leaves each block wholly inside its tunnel and wholly out of the walked channel while still crossing every pane, so the timing the blocks exist to show is unchanged; the offset survives every hop because each pair here is one pane facing +Z and its partner facing -Z, which makes a crossing a pure translation. The claim is asserted as a *volume* in `examples/tests/Scene.cpp` - nothing stands in the middle of any walked space, from above the floor markings to over a character's head, and the panes are excluded by carrying a `Portal` rather than by name - and photographed by `scripts/demos/capture-tunnels.sh`, which reports the block's share of the box a walker looks through and the mean brightness behind it, matched against the *rendered* colour rather than the authored one
- [x] fix cross-world portal rendering (cannot see character on other side) - ensure test - **the pane named a world and a replica is not registered under that name.** A host that mirrors a universe for a viewer names each copy apart - the editor's is `"<world> (client 1)"`, because rule 4 makes a name the identity and two worlds may not share one - while everything a scene *authored* still says `"<world>"`: `Portal.DestinationWorld` is a string somebody typed and replication carries it verbatim. `AttachForeignSurfaces` compared the two directly, so from inside a client's view the destination fell back to the authority (a room no client's character is in), this world's own name never matched what the far pane named, and the two rules that turn on that match both went quiet - the far pane being left out of this pane's picture, and the far world's straddlers coming back into this room. The visible half is the first: the far world's slab stands exactly where the camera is aimed and is the same rectangle the frustum covers, so it fills the hole edge to edge in one flat colour and the room behind it - character included - is drawn and hidden. `world::Replica` gains `Of` and `View`, `client::SurveyWorlds` reads them once per frame and `client::ResolveDestinationWorld` prefers the same viewer's copy over the authority, which is also what keeps two clients' rooms from being joined to each other. `Editor::PresentPortalDestinations` resolves through the same pair, or it presented nothing and the attach read a draw list nobody had built this frame. Pinned in `client/tests/Presentation.cpp` against the editor's own naming: without the fix the picture holds the authority's occupant instead of the client's and a row in it still samples a surface
- [x] editablemesh has no transform component (does not render) - **not a missing `Transform`, and it should not gain one**: an `EditableMesh` is an `Instance` rather than a `PVInstance` because it has no place of its own in the world - what draws it is a `MeshPart` naming its content id, which is Roblox's arrangement and `scene/EditableMesh.cpp` argues it. What was missing is the upload. `client::EditableMeshUploader` turns those arrays into geometry a device can draw and is called once a frame by whichever program is drawing; `client::Client` called it and `studio::Editor` did not, so a `MeshPart` whose `MeshId` is `editable-mesh://N` resolved to a name nothing had ever registered and drew no triangles at all - in the viewport, in a Play run and in the studio's own capture, while the identical scene under `client --script` was correct. Every scene built out of geometry a script makes was invisible in the one program it is authored in, and `StressMirrors.luau` was a shell of floating tiles there for the same reason. Both uploaders now run beside the shader refresh the editor already had. The check is `scripts/demos/capture-editable-mesh.sh`, which photographs one scene through both programs and compares: a plain `Part` beside the built mesh separates "the upload is missing" from "the camera is pointed elsewhere", which is the one thing a single still cannot say
- [x] mirror stress test needs ALL faces to have mirrors, also should be a filled icosphere instead of a sphere with holes - the scene authored sixteen cameras over eighty facets because sixteen is what `scene::MAX_SURFACES` resolves, so five sixths of a *mirror ball* were not mirrors; every facet asks now and the ceiling does the refusing, which is the finding a stress scene exists to produce rather than a cost to hide. The holes were the tiles: each was a square 0.62 of an edge across with whatever roll `CFrame.lookAt` chose, and a square smaller than its triangle cannot meet its neighbours however it is turned - the ball was loose plates over a dark core with a gap at every seam, and a reflection has to be read *across* a surface. A tile is now the triangle's own bounding box in the triangle's own plane, turned to the triangle's own axes, so every point of every face is under mirror and neighbours overlap across the edge they share; the core is derived from the closest face plane rather than from a constant, because a chord is inside its own arc by an amount that moves with the subdivision. `SUBDIVISIONS` defaults to 2 for the same reason the tiles changed: a box circumscribing a triangle is twice its area at every level, and at level 0 that is a plate as wide as the ball is round. 320 facets, 320 cameras, no core visible anywhere in the frame. `examples/tests/Scene.cpp` pins the camera count against the facet count and the tiled area against the ball's own, which separates a covering arrangement from a gapped one without re-deriving the ico-sphere
- [x] terrain demo, color them, also only do a 512x512 area with perlin noise 1 unit vertex interval. - and the colour half is not a scene change, it is `client::BuildMeshData`. `assets::MeshVertex` has no colour field, so every `EditableMesh` was averaged down to the *one* flat colour an `assets::Submesh` can hold: a heightfield came out as a single mud brown and a painted ball as a uniform grey, both of which read as a badly authored scene rather than as a conversion dropping the data. It does not follow that a per-vertex paint job has to collapse - a mesh may carry as many submeshes as it likes and `MeshTable::Add` already turns each into its own draw run with its own `BaseColour` - so a triangle's colour is now the average of its three corners and every distinct result gets a run, quantised to eight bits a channel before grouping and emitted in first-appearance order so two conversions of one mesh are byte-identical. `Terrain.luau` is then the consumer: 512 studs a side sampled once a stud, cut into 64 chunks of 65 by 65 that share their edge columns, normals from the field's own central differences and bands - water, shore, grass, scrub, rock, snow - cut as fractions of the field that was actually generated rather than as absolute heights, because a threshold written beside the relief constants turns the whole map one colour the moment either is retuned
- [x] mirrors-4-worlds demo, the reflection is super dark. this is for ALL mirrors. - **the mirrors were innocent and the scene was unlit.** Measured against a probe with a plain pane, a reflected floor reads 235 where the same floor seen directly reads 228, so the surface path loses nothing; measured in this demo, the pane's floor reads (90, 82, 71) and the *directly drawn* floor reads (87, 72, 70) - the same. `Mirrors-4-worlds.luau` touched `Lighting` nowhere, so it ran on the service default of `Brightness = 0.44` under a 14:00 sun at 41.7 degrees, which lands on a wall almost edge-on: every surface came out at about a quarter of its authored colour, an `Ember` wall of (196, 84, 72) read as (51, 37, 17), and the warm-to-cool palette this demo exists to make legible was four indistinguishable browns. A noon sun at 2.2 fixes both halves at once, because there was only ever one
- [x] add a right-click zoom-to in studio for objects with transforms - `Zoom To` in the explorer's context menu, greyed rather than hidden for anything without a `scene::Transform` because a menu whose length changes per row is one nobody learns the shape of. **The orientation is kept and only the position moves**, which is what every editor's frame-selection does and the behaviour that does not surprise: an author who has lined a shot up wants the part centred from where they are standing rather than a new angle chosen for them. The distance is the same arithmetic `MeshPreview` already uses - the bounding *sphere* of the part's box over `tan(fov / 2)`, sphere rather than box because a camera pulled back for the half-extent alone leaves the corners outside the frame the moment the part is turned. It moves one panel: the focused viewport when that is showing the world, and the main one otherwise - two viewports both jumping because a row was right-clicked once is the surprise, and the second panel is usually the one being held still on purpose
- [x] magic demo is broken (script error) - ```script error: examples/Magic.luau:44: ReplicatedStorage.MagicCore was not mounted - is assets/lib staged?
[C] function assert
examples/Magic.luau:44```. Same with TerrainCore. - **the libraries were staged and the world had never been given them**, which is the one thing the message named that was not wrong. `examples::LoadScene` mirrors every directory under the staged `assets/lib` into `ReplicatedStorage` before a scene's first `require`, so the client, the server and the harness all get the tree - and `studio::Editor` does not go through `LoadScene` at all. It installs an example as a `Script` instance in a world it built itself out of `InstallServices` and `InstallGuiServices`, so a scene whose first line is `require(ReplicatedStorage.MagicCore)` ran correctly under `client --script` and failed in the one program it is authored in. The fix landed twice: first as a `MountSharedLibraries` door the editor called on every world, and then - when the line below moved the modules out of `ReplicatedStorage` entirely - as `MountSceneLibraries`, which the editor calls on the script it has just parented. Pinned by a test that mounts under a script no scene was loaded into, because every existing test went through `LoadScene` and that is exactly why the gap survived
- [x] the custom terrain system should be: 1. a underlying EditableMesh 2. pure luau code - `Terrain.luau` was a voxel streamer: a 16384-block map turned into merged boxes over a `Part` pool with an eviction ring round the camera. It proved streaming and it proved nothing about the class this engine gives a script for building geometry - every surface on screen was an axis-aligned box, so a slope was a staircase and a colour was a part property rather than anything the mesh carried. What is there now is the shape a heightfield genuinely is, built by `Instance.new("EditableMesh")` and `AddVertex`/`AddTriangle`/`SetVertexNormal`/`SetVertexColor` and nothing else - no C++ helper, no staged library. The noise library is the one part kept verbatim, because it was always the good half. A chunk a frame rather than all sixty-four before the first present, so the window is not held blank for the twenty thousand mesh calls each one costs
- [x] move magic core system INTO the scene demo, it should NOT be a studio library, move to pure script - **they were a property of the program and are now a property of the scene.** The modules were staged into one `assets/lib` tree and mirrored into every world's `ReplicatedStorage` before anything ran, so `MagicCore` and `TerrainCore` appeared under every world of every game - a brand-new empty one included - and a scene that wanted them had no way to say so: it asserted that somebody else had already put them there and failed with "is assets/lib staged?" whenever nobody had. That is the shape a shipped engine library has and these are a demo's modules. They stage per scene now (`assets/examples/Magic/MagicCore/...`) and mount as children of the `Script` that requires them, which is Rojo's own arrangement and the one every `require(script.Parent.X)` inside the modules was already written against - not a line of `MagicCore` or `TerrainCore` changed, and `TerrainRuntime` lost the one line that named a service instead of a sibling. **One copy in the repository and a copy per scene in the stage**, because three directories under `lib/` would be three things to keep in step with a port that is meant to stay byte-identical, while what the runtime wants is the opposite. `examples::MountSharedLibraries` is gone and `MountSceneLibraries` replaces it, called by `LoadScene` after it mints the script and by `Editor::InstallExampleScript` after it parents one. Pinned by a test that mounts under a bare script and then checks `ReplicatedStorage` is *empty*, which is the half that would otherwise regress silently
- [x] when you enter a portal, the (replication) interpolation makes it jump through space visibly. should we have a "portal move" packet or a "instant move" or a "full component sync" for it? - **none of the three, because the packet already exists.** `CrossPortals` maps a crossing body's `PreviousTransform` through the seam so the frames between one tick and the next blend inside the destination room rather than across the hundred units between the panes - that fix is correct and it is local, because `PreviousTransform` is render-side history with no wire form. A client receives a `Transform` that has jumped while holding a previous frame from the room the body left, and blends straight through the gap: at three frames to a tick the character is drawn once or twice somewhere in between, which is the streak. What crosses the wire already is `scene::PortalTransit`, a serial and a yaw that `FollowPortalTransit` uses to turn the camera - so the client end needed a reader, not a message. `scene::SnapPortalTransit` runs in `PreRender` on every presenting host and collapses `PreviousTransform` onto `Transform` for anything whose serial has moved since it last drew. **A serial rather than a flag**: nobody has to clear it, a dropped delta leaves the counters further apart rather than losing the crossing, and it answers "how many crossings have I not drawn" instead of "was there one". **A snap rather than a mapped blend, because a replica cannot map** - the seam is the authority's knowledge and what arrives here is a position; one tick of lost smoothing at the moment of a crossing is invisible next to the streak it replaces. The authority takes its own counter inside `CrossPortals`, so the pass finds nothing there and the mapped previous frame it computed survives - which is the case `engine.scene.interpolation` pins, along with the idempotence and the second crossing. **And the serial now moves on every crossing rather than only on turning ones**: it used to be written inside the guard that measures the yaw, so a pair of panes facing each other head-on wrote nothing at all - and a straight-through portal is both the commonest kind and the one where a replica had no way to know
- [x] PortalLightMix Lighting service brightness is set to 0 and no lights emit when brightness is 0. - **they emit; there was nowhere to stand.** `Lighting.Brightness` folds only into `WorldLighting::Direct` - the sun - and `client::CollectLights` never reads it, so a `PointLight` is untouched by it. What the scene did was leave `workspace.CurrentCamera` alone unless its `VIEW` line was rewritten, and `capture-portal-lighting.sh` is the only thing that ever rewrote it: run by hand, the scene opened on the client's own starting eye with both twenty-stud pools outside the frame, and a nearly black picture in a scene whose first line is `Lighting.Brightness = 0` reads as the sun having put every lamp out. Measured from the near viewpoint at `Brightness = 0`: 153,662 red pixels and 26,598 green, which is both pools and both channels. At `Brightness = 1` the same frame holds 20,030 red and 529 green, because the sun washes them out - the scene is *more* legible with no sun, which is why it has none. The default is now `near` and the two capture scripts match whatever the scene's own default is rather than only an empty one, with their existing guard turning a moved line into a message
- [x] EditableImage demo does nothing its just purple-black checker? was it meant to make a editableimage then set the meshpart's texture to it? if so, we'll probably need to setup something proper for the texture component where we have a underlying asset delivery for pulling cdn items or in-memory items from luau / ecs engine like EditableImage objects and their buffered data. - **that is what it does, and the delivery layer it needs already exists.** `EditableImageContentName` mints `editable-image://N`, `client::EditableImageUploader` turns the pixels into an `assets::TextureData` and registers it under that name, and `render::TextureTable` neither knows nor cares that nothing published it - a `MeshPart.TextureID` resolves it exactly as it resolves a CDN asset. The purple-black checker is `TextureTable::Missing()`, which is what that name resolves to in a program that never ran the uploader: `client::Client` ran it and `studio::Editor` did not, which is the same gap the `editablemesh` line above closed. In the client it worked and looked broken for a different reason - the scene set no camera, so a four-by-four panel two tenths of a stud thick was seen edge-on from the default eye as a red sliver a few pixels wide, and a picture of a red sliver reads as a texture that did not apply. It has a camera square to the front face now, and the rings and the sweeping crosshair are both visible
- [x] particles are SUPER slow and they don't render visually either (refreshEmitters, stepParticles) - **they render, and the statistics line was what said otherwise.** `DrawParticles` and `DrawRibbons` added to `FrameResult::DrawCalls` and never to `FrameResult::Triangles`, so a frame drawing three hundred and ninety thousand particle quads reported "108 triangle(s)" - a number small enough to read as nothing having been submitted at all. A four-vertex strip is two triangles and a ribbon strip of `n` is `n - 2`; both are counted now, and the same scene reports 787,674. **The speed half is the `dev` preset**, which `AGENTS.md` already says is `-O0` for first-party code: `Particles.luau` - 102,400 emitters at five particles each - runs at **4 FPS in `dev` and 48.5 FPS in `release`**, with no dropped ticks in the latter. Going past that is the v0.18 line that already exists for it rather than a bug in this one
- [x] add a PAUSE button to the flamegraph so i can pause it - and the panel now draws a snapshot it publishes rather than the live span buffer, which is what makes pausing mean anything: nothing is sampled while paused, so the numbers on screen are exactly the ones that were there when it was pressed. The snapshot copies the names, because `core::FrameSpan::Name` is a `std::string_view` that may point at a buffer the frame owns - safe to read during that frame and a dangling read afterwards, which every feature that holds spans across frames has to answer
- [x] add a dropdown to add a update interval option to the flamegraph AND add a "average" checkbox that will average the results across the interval OR only shows on the update outputs - six intervals spanning two decades (every frame, 250 ms, 500 ms, 1 s, 2 s, 5 s) and the checkbox picks which of the two answers an interval gives. Off, it decides how often the panel takes a fresh sample and what is shown is one real frame, unlucky ones included; on, every frame in the interval is summed and the mean is drawn, which is what a span *typically* costs. The sum is matched by name and depth rather than by position - a frame that opens one fewer span shifts every later index, so summing positionally would add a renderer's time to a script's for the rest of the interval - and the checkbox is greyed rather than hidden at "every frame", because a control that vanishes when its neighbour changes is one nobody finds again
- [x] add a preferences tab called "Default Worlds" that list all available default world items and add checkboxes to select them. Set "Rings", "MeshGrid" and "Hallway" worlds as the defaults and disable the rest. persists as well. - `Editor::NewGame` was twenty `AddWorld`/`Enter` pairs written out in the source, each with a paragraph saying why that world earned its place, which made the set a property of the function - nobody could open with a different one without editing it. `DefaultWorldCatalogue` is the same list as a table of thirteen rows, the paragraphs kept as each row's tooltip, and the page is checkboxes over it with select-all, select-none and reset. The keys are what land in `preferences.json` and are fixed once shipped; a key that no longer exists is skipped on load and left in the file, for `panelColours`' reason. **Absent and empty are different answers**: a config written before this existed has no array and must not read as "every world off", so `DefaultWorldsChosen` tells the two apart and the first tick writes the whole current set rather than one row - otherwise unticking `Rings` would silently untick `MeshGrid` and `Hallway` too. Unticking everything is allowed and gets one empty world called `Start`, because an editor with nowhere to put a part is not what anybody meant by it
- [x] separate interface update Hz (idle, active) with renderer Hz in studio (focused, unfocused) under preferences - four rates under the existing ceiling, persisted, with idle meaning three seconds since any SDL key or mouse event. Idle is decided from the raw events rather than from `ImGuiIO::WantCaptureMouse`, which is false for every frame somebody spends flying the viewport - and a camera being flown is the last moment to drop the frame rate. Focus is the window's own `SDL_WINDOW_INPUT_FOCUS`. **They combine as a minimum rather than running independently, and the page says so.** `render::Renderer::Render` owns the swapchain, the interface and the present in one call, so a frame that rebuilds the panels redraws the world too; there is no arrangement today where the two run at different rates. Four independent-looking numbers that are not independent would be worse than four ceilings and a line admitting it - splitting them for real means `Render` taking the world and the chrome separately, which is a change to the shared renderer
- [x] content is 0.1ms in idle studio as well for some reason with only flamegraph open (content). flamegraph is not granular enough and misses a bunch of stuff (only pump is visible). - **one bar labelled `content` could only say the total was small and non-zero**, which is exactly the reading that sends somebody looking and finding nothing. Three things sit under it and they cost different amounts for different reasons: a delivery client polling its socket, a walk over parts waiting for a mesh to size them against, and an upload queue. Split as `content.deliver`, `content.fit` and `content.upload`, with `thumbnails` and `node previews` lifted out beside them - and the answer is the first: measured over 119 idle frames, `content` is 0.024 ms mean of which `content.deliver` is 0.020, so it is the socket poll and nothing else
- [x] there are also gaps between "build interface" and "ecs.systems", "ecs.systems" and "Renderer::render", etc. - every gap was a real piece of work with no span on it, which is the one thing a flame graph cannot distinguish from a stall. `PresentWorld` is the whole second half of an editor's frame - pick a panel, gather its portals and surfaces, upload whatever a script changed, present the world, then call `Renderer::Render` - and only the last of those was named, so the rest read as a hole between `build interface` and the renderer. It gains `present world` with `world present`, `collect surfaces`, `editable upload` and `shaders` inside it; `sample frame`, `output`, `team create` and `thumbnails` cover the smaller ones. **And the display wait is now inside the frame**: `FrameGraph::BeginFrame` moved above `Renderer::WaitForFrame` and the wait is an `Idle` span, so the largest single thing an editor does with vertical sync on stopped being invisible - the panel used to report a two-millisecond frame while the wall clock said sixteen, with the difference nowhere. Measured after: a typical idle frame is 0.68 ms of which `present world` is 0.32 and `build interface` 0.28, and what is left over is the named small spans rather than a blank
- [x] use pet dance gif to test particle flipbook - it drew an untextured square, and the reason is a pipeline seam rather than the flipbook. `Particles.luau` names `effects/fox_dance.atex`; the GIF is in the local store and `contentimport` - which is how that store is filled - renames every file it takes to its own content hash, so what the store actually holds is `67e91cae....atex` and no name in any scene can reach it. That is right for a bulk import and wrong for an asset a demo refers to by name. `assetc` is the other half of the pipeline and it keeps the tree, so `scripts/demos/bake-fox-flipbook.sh` stages the four pet GIFs under the names the scenes use - recovering the original filenames from the store's own import log, which is what makes it work without anybody keeping a second copy - bakes, and publishes a store of its own **beside** the library rather than over it, because re-publishing across somebody's hand-filled store is a step a demo script has no business taking. Verified by capture at three points in the loop: the pet is drawn from the baked sheet and the pose changes, so the cell selection is advancing
- [x] find which tests take forever and optimise them - measured per binary and then per suite: the whole run is about 168 s of suite time, of which `server.replication` is 35 s and `server.hostmode` 12 s. Both spend nearly all of it in `Settle` and `Wait`, which tick a client and sleep in *real* time while a spawned server process gets somewhere. **That wait cannot be shortened by asking a better question, and it was worth finding out why.** Rewriting `Settle` to stop once the replica went quiet broke the case that walks a character - a body that has already arrived is a body whose entity count never changes again, so the wait ended before the movement did; rewriting it to stop on a named condition broke a different case, because "more entities than before" is not "finished arriving", and a third, because a client that stops polling stops acknowledging and an authority with nothing acknowledged has no baseline to build the next delta against. The fixed wait is load-bearing in three different ways and all three are invisible. What a sleeping suite does not need is a core, so `testrunner` runs suites concurrently instead: a scratch file per worker (the one piece of shared state the loop had), results reported in list order rather than completion order so a run can still be diffed, and `--jobs` to set the width. Measured on 24 threads: serial 2 m 54 s, `--jobs 2` 1 m 36 s over three clean runs, `--jobs 4` 1 m 11 s with `server.replication` red once in three. The default is 2 - the fastest setting not observed to flake - because a runner that goes red under its own concurrency is worse than a slow one
- [~] a few of the portal scenes (in all the various worlds) have z-index fighting issues - **not reproduced, and the measurement is worth more than another guess.** `scripts/demos/zfight-scan.sh` photographs a scene twice from cameras a thousandth of a stud apart and reports how much of the frame changed *through* rather than at an edge - the erosion matters, because a sub-pixel shift outlines every silhouette in a room made of tiled walls and `Portals-1-world` reads 1.26% that way with not one pixel of it a fight. Eroded, all fourteen portal and mirror scenes read 0.00%. **And the control says that is a real zero rather than a blind one**: two slabs with their top faces at exactly y = 0 come out a uniform blue in this engine, one winning every pixel, with no stripe at any distance - the depth tie resolves the same way every frame and the draw order does not depend on the projection, so coplanar geometry here is *stably* wrong rather than noisily wrong. That rules out the classic reading of the report and leaves the ones a still cannot show: a sorted transparent run changing order, or 2D `ZIndex` between overlapping `ScreenGui` elements. Needs a scene name and roughly where the eye was
- [~] PortalLightMix, when you're on the side view of the mirror, you move left the light shows up only on left side, you move right left side hides and right sides hows up, lighting culling with portals - **not reproduced in the client, and the two mechanisms that could cause it are both ruled out by arithmetic.** `client::CollectLights` cuts to `render::MAX_SCENE_LIGHTS` only when there are more than sixteen, and this scene has four - two lamps and the two copies the seam makes; and the seam copy is gated on `SeamDistance(seam, lamp) < lamp.Range`, which is a fact about the lamp and the pane and mentions the viewer nowhere. Measured two sweeps past the near pane, counting red and green pixels per frame. Perpendicular at x = -18, -9, 0, 9, 18: red 80,556 / 162,163 / 188,301 / 162,007 / 79,212 and green 15,172 / 16,699 / 17,377 / 16,712 / 14,302. Edge-on at x = -14 through 14: symmetric about zero to within a fifth of a percent, with the transported green falling smoothly as the pane turns away because the pane subtends less of the frame. No popping and no left-right asymmetry anywhere. Needs the program it was seen in - the studio presents through `Editor::PresentWorld` rather than `client::Client`, and that is the path this has not been able to sweep
- [_] discord rich presence (https://docs.discord.com/developers/discord-social-sdk/getting-started/using-c++)

### v0.18

- [_] build out proper code architecture documents (AGENTS.md, docs/CODE_FORMAT.md, docs/CODE_QUALITY.md) => CODE_ARCH.md
- [_] DOMAIN DRIVEN DESIGN & HEXAGONAL ARCHITECTURE
- [_] check if we need to move files / classes / structures around in the codebase to properly fit (mainly focus on engine)
- [_] properly make a ECS component document list so i can see all components and what they're for
- [_] clean up all ECS components that exist and find better ways to represent stuff (e.g. merge, split, rename).
- [_] in the engine, rename Anchored to Static, keep roblox shim as Anchored but refer to static when changing property
- [_] think plan for future features as well listed in roadmap and plan for them now
- [_] rename mono.unified_server_client to mono.unified_tests which imports all the mono reports into the code so it can fully test all features with all variations between clients, servers, cdn, networking, engine, etc. This one focused more on CROSS communication and management, NOT per-mono specific systems and such.
- [_] deferred catchup
- [_] improve build times (flamegraph => optimise)
- [_] mono.launcher (run singleplayer game, host game on server, join server, run studio, run cdn, etc)

- [_] optimise full particles (try reach 5 million rendering particles)
- [_] optimise physics (physics demo, -O0 and in studio)
- [_] optimise mirrors (try varying bounce levels to see bottlenecks in demo scene)

### v0.19

- [x] thoroughly implement every user-interface element, including `SurfaceGui` and `BillboardGui` - `SurfaceGui` gains `ZOffset`, `MaxDistance`, `ClipsDescendants` and `Active`, and `BillboardGui` gains `Active`, `Brightness`, `ClipsDescendants`, `CurrentDistance`, `DistanceStep`, `ExtentsOffsetWorldSpace`, `SizeOffset` and `PlayerToHideFrom`; new classes `UIGradient`, `UITableLayout`, `UIPageLayout` and `UIDragDetector`; `ScrollingFrame` completed with `ScrollingEnabled`, `AutomaticCanvasSize`, the two `ScrollBarInset`s, `VerticalScrollBarPosition`, `ElasticBehavior`, the three bar images and `AbsoluteCanvasSize`/`AbsoluteWindowSize`, plus wheel and thumb-drag input; `RichText`, `MaxVisibleGraphemes`, `ContentText`, `TextBounds` and `TextFits` on every text class; `Interactable`, the four `NextSelection*`, `SelectionOrder` and `SelectionImageObject` on `GuiObject`; `HoverImage`, `PressedImage` and `ResampleMode` on the image classes; `Enabled` and `ApplyStrokeMode` on `UIStroke`. Laid out, drawn by both backends, saved, replicated, bound and in the Properties panel. `D00120` carries the members that need a subsystem this engine has not got
- [_] build out all remaining roblox surfaces with available underlying surface
- [_] port many particle features from unity to here (https://docs.unity3d.com/6000.5/Documentation/ScriptReference/ParticleSystem.html)

### v0.20

- [_] finish portals so lighting, physics, projection, clipping and geometry crossing the seam are seamless
- [_] ensure per-mesh render capabilities, global lighting render capabilities, camera lighting render capabilities, etc. compute shaders, post-processing, etc.
- [_] simplify and strip old rendering code that is not part of the node system. Everything should be in the node system.
- [_] port semi-real raytrace and path-trace as part of nodes
- [_] make demo render pipelines with semi-real raytrace and path-trace
- [_] (dynamic) ambient occulusion, screen-space, fog, atmosphere, clouds, global illumination, displacement maps (make it rendering only but not physical)
- [_] render pipeline nodes for above
- [_] plan the entire rendering system to a visual compositor system like Unity. https://docs.unity3d.com/Manual/scriptable-render-pipeline-introduction.html https://docs.unity3d.com/Packages/com.unity.visual-compositor@0.27/manual/nodes.html

### v0.21

- [_] build out default plugins (move all topbar tools and stuff to plugins as a "Default Studio" plugin)
- [_] build out plugin function suite (create dropdown, edit toolbar, edit viewport, edit script editors, etc)
- [_] universe shared assets folder and setup easy cdn with it (when you load the universe file, it sets up a cdn with it).
- [_] add a universe loading widget - shows cdns the universe has and asks to allow permission, also http enabled property if changed
- [_] add tabs to the universe importer: general, assets, permissions, cdn, misc with all or per-world breakdown

### v0.22

- [_] default R6 base character (capsule collider)
- [_] gtlf default character (unreal)
- [_] make humanoid a shim for character controller (so not a black box), loads a default one in
- [_] character controller + humanoid + character states + state controller + bone controller, etc. More modular than roblox standard humanoid. state machine? node graphs? etc.
- [_] animation handler
- [_] skinning and animation - `bake` skips joints and weights and keeps the rest pose, because there are no skeletons in the engine yet
- [_] add accessories support

### FUTURE

- [_] unity porting tools / unity shop
- [_] roblox porting tools (rbxl) - in the widget that pops up, show all asset ids and make a assets selector so you can click which asset id points to which file asset (same for animations and whatnot where possible).
- [_] (procedural, node-based) terrain generator (refer to discord references)
- [_] porting roblox games (DEFER THIS UNTIL LATER ONCE TYPES ARE BUILT UP) - untouched, and the trigger is unchanged: there are four instance classes in this engine and a Roblox place names hundreds. Will show a widget that tells you conflicts and missing classes.
- [_] add modulescript boundaries between luau and javascript VMs. moving values between vms.
- [_] consider adding C# as another scripting langauge?
- [_] constraints system
- [_] deferred `D00106` - JavaScript and TypeScript breakpoints. The vendored QuickJS exposes no line hook and no debugger API at all, so this is a submodule decision rather than a feature. Asking for one on a .js/.ts chunk is refused with the reason, at the service, the gutter and the panel alike. **The TypeScript half of the entry shipped at v0.15 and is not part of this** - source maps are emitted and read, so the lines a debugger would land on are already the right ones.
- [_] full audio DAW (digital audio workbench) system
- [_] embedded whiteboxing tools (planning)
- [_] full procedural terrain studio tools
- [_] full ui features
- [_] level-of-details (4 different meshes version, auto-decimate version, smart-triangle-reduction-version thinking of nanite triangle surface area)
