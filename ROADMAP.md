
# ROADMAP

## Editing

Do NOT add new deferred work as a roadmap item. Place it in
`docs/DEFERRED.md`. The existing `Deferred:` blocks below are version history
and pointers to that register. If a TODO item is not FULLY completed, split the
TODO item, keeping the concise short dash point list with block infront and
split it as another item under the same version.

For example, if we complete A and B but not C and D:
```
### v0.5
- [x] do: A1, A3, A5
- [_] do: A, B, C, D
- [x] do: G, H, I
- [x] do: K, K2, K3
```

becomes:
```
### v0.5
- [x] do: A1, A3, A5
- [x] do: A, B
- [x] do: G, H, I
- [x] do: K, K2, K3
- [_] do: C, D
- [_] deferred `D0001` for later.
```

or defer to another version.

## Documents referenced from here that are not in this repository

`CDN.md`, `RENDER_PIPELINE.md`, `repo_layout.md`, `GARG_ECS_Layout.md` and
`DATATYPES_LIBRARIES.md` are cited by section number throughout this file and by
several module `AGENTS.md` files. None of those design notes is committed —
`git rev-list --all --objects` finds no such path. Said once, at the top, rather
than qualified at every citation. The former `v02v03v04.md` plan is different:
its committed retirement copy is
[`docs/retired/v02v03v04.md`](docs/retired/v02v03v04.md).

**Four of the five are in a sibling repository, `../atomic-game-engine-hidden-docs`**,
which is where a section reference can actually be followed — along with
`core-features.md`, `MCP.md`, `SHADERC_VENDORING.md` and the Roblox, Unity and
Unreal notes. Recorded because "held outside the tree" left every citation a
pointer somebody had to be handed, and the path is not a secret even where the
contents are.

**`GARG_ECS_Layout.md` is not there either**, so that one citation is still
unfollowable and is the only one of the five that is. Worth knowing before
somebody goes looking for it twice.

**A document outside the tree can describe another engine, and one of them
does.** `RENDER_PIPELINE.md` is a design and staging plan for a graph renderer
built beside an existing one — `--renderer=v1`, box3d, `Camera.Shaders`,
`ChainCache` — and none of that machinery is here. Its stage numbers therefore
do not index work in this repository, which v0.11's own line assumed they did.
See `v11.md` §1, at the repository root.

## VERSIONS

The milestone headings below are development labels. They are not synchronized
with CMake package metadata, which currently reports project version `0.4.0`.

### v0.0

- [x] repository setup; readme, license (mlp2.0), subfolders, build process, basic C++ main.cpp files for each subfolder.
- [x] repository setup: mono.server, mono.tools, per-folder tests/, tier checking at configure time, four presets (dev, release, server, ci)
- [x] vendors setup and build auto-downloads vendor folder (recursive clone submodules).
- [x] vendors setup: shaderc builds glslc from source, so no system shader compiler is a prerequisite and the SPIR-V is reproducible
- [x] concise documentation
- [x] concise documentation: RUNNING.md, SECURITY.md, THIRD_PARTY_NOTICES.md, docs/DEFERRED.md, and an AGENTS.md in every mono.X folder carrying that folder's invariants
- [x] essential libraries (spdlog, catch2, asio, tracy, sdl, imgui, flecs)
- [x] smart tests (cascading signature hash, smart-tests.txt)
- [x] code format documents, code quality checklist, CONTRIBUTING.md, ai guides (AGENTS.md, CLAUDE.md points to AGENTS.md), ai guidelines (always read the ai guide, code quality and code format documents)
- [x] ai skills and workflows — `.claude/skills/` (grug, ste-writing) and `.claude/commands/` (`/run-checklist`, `/new-module`, `/pr-analysis`), checked in so every contributor gets the same ones
- [x] essential libraries 2 (asio, imgui)

Deferred:
- D00001

### v0.1

- [x] cli argument parsing — `core::Arguments`, declared rather than scanned for, so `--help` is generated and an unknown flag is an error
- [x] entity-component system — `ecs::Store` and `ecs::Scheduler` over flecs, with a thread-affinity check that aborts
- [x] basic graphic rendering demo — SDL3 GPU, instanced, one draw call for the whole scene
- [x] tracy flamegraph (F5 keybind with pixel art visualiser) — four views: flamegraph, categories, systems, counters
- [x] fps counter (F3 with pixel art text) — 3x5 bitmap font, min/avg/max over a 20 s window, plus jitter
- [x] ecs test — plus unit suites for every other module, an integration suite and an architecture test
- [x] mono.server — `server`-tier library and thin main, fixed tick, headless. The binary contains no renderer and `just check-server-is-headless` proves it
- [x] tests live in each mono.X folder; tooling is C++ and CMake, so no scripting runtime is a prerequisite
- [x] `Store::EachParallel` — parallel within a tick. 3.5x at 500k entities, and slower than serial below ~60k, which is why the default grain is 4096. **Both halves of that were re-measured at v0.4 and neither survived: the crossover is ~262,144 rows and the ceiling is 1.3x, not 3.5x. The grain is still 4096, for a reason that is now written at the constant rather than inferred from this line**
- [x] `core::Name` — a stable string and an interned dense id. The mechanism behind every registry that has to survive a rename
- [x] runtime shader compilation — `render::ShaderCompiler` over vendored libshaderc, the compiler `RENDER_PIPELINE.md` §11.9.1 calls the one honest blocker on a live shader swap
- [x] simulation and rendering tick separately — `core::FixedTimestep`, `Scheduler::RunPhases`, interpolated rendering and both rates on F3. `RENDER_PIPELINE.md` §14's measurement holds: 60.0 ticks/s while rendering at 2400 fps

Deferred:
- D00002

### v0.2

Planned in [v02v03v04.md](docs/retired/v02v03v04.md). Userland gets Roblox-style instancing, tweaked; the engine underneath is full ECS. The order below is the order the steps land in, and each one leaves the tree building and passing.

Standing discipline for v0.2 and v0.4, in that document's own section: preallocate and reuse by default, take the better data structure without ceremony but attach a number to an algorithm swap, and allow async only for work the tick cannot observe finishing.

- [x] jobs: an atomic pool claim so a nested or concurrent `For` runs inline instead of deadlocking; `Store::Owner` atomic, because a world is picked up by a different worker every tick
- [x] `core::ByteWriter` / `ByteReader` — explicit little-endian framing, used by messages now and by saves and the wire later
- [x] engine-owned storage: `TypeDescriptor`, `Column`, `ComponentSet`, `SparseSet`, the archetype graph and cached queries. flecs removed, `Store::Native()` deleted, and the public `Store` API unchanged so the existing suites are the acceptance criterion
- [x] `ecs::ChangeChannel` — per-row dirty bits held as a lazily-added `DirtyBits` column (never a tag, which would move the row on every write), plus a coarse counter a batch write still moves. `Observe<T>` opts a type in; `EachChanged<T>` walks only what moved
- [x] instance model: the class table with per-class prototype rows (so `Instance.new` is a column copy and `:Clone()` falls out of it), `:IsA`, property descriptors, and the `Parent`/`FirstChild`/`NextSibling` hierarchy. Organisational only — not a transform hierarchy, because `Transform` is world-space and nothing propagates
- [x] change signals fired at a phase boundary rather than on assignment — `Store::OnChanged<T>` and `FlushSignals`, called by `World::Tick` after the simulation phases. The dirty bits *are* the queue, so there is no second record of what moved; three writes in one tick signal once with the value it ended at, and the changed set is collected before anything fires so a listener may add, remove or destroy without walking a table that has moved underneath it. A flush refuses to re-enter, so a listener writing what it listens to is one signal at the next boundary rather than a loop. Changes clear at the *start* of a tick, not the end, so `PreRender` still sees what the tick did
- [x] world snapshot — `Store::Save`/`Load` through each `TypeDescriptor`. Components are recorded by **name**, so a snapshot crosses processes and builds; the entity directory is reproduced exactly, index and generation alike, so an `Entity` stored inside a component is still the same entity after a restore. Refuses rather than half-writes when a component has no serialisation, and leaves the store empty rather than half-restored on any failure
- [x] `mono.engine/world` at L4: `Universe`, `World`, `WorldId`, the directory and the control queue. Universe = overarching simulation, worlds = each subarea to simulate. Per-world tick rates, Active/Idle/Suspended/Faulted, soft-fault quarantine with a crash-loop cap, and `Enter` as the only — scoped — way to reach a store
- [x] World Entities — each world's root instance (`workspace`), created with the world so no system has to check for one; structural changes to the world list queue to the barrier rather than mutating it underneath a running batch
- [x] communication buses — worlds never address each other: MessagingService (pub/sub topics), MemoryStore (ephemeral shared map, sorted map, queue), DataStore (durable key/value, versioned, read-modify-write) and Teleport. Routing is hub-and-spoke, so there are N channels rather than N², and every ordering decision is made in one place. There is no cross-world entity reference at all, which is the Roblox model and deletes the type that would have broken rule 3
- [x] outbox and inbox — resources on the world's store (so a snapshot carries pending traffic), exactly one tick of latency, applied at the barrier in `(sender, sequence)` order. `From` is stamped by the driver, not the sender: a world does not get to say who it is, and every ordering decision depends on that field
- [x] multi-world parallel processing — worlds are the batch, dispatched longest-first so the tail stays short; `WorldParallel` and `WorldSerial` proven by test to produce identical results. Host pool sizing lands with `parallel/process`
- [x] Universe Data (shared data in Universe) — the bus backends and nothing else. Player data is a DataStore key, not a row in a world nobody owns. `Universe::Peek` is the documented escape hatch for consumers outside the simulation, and a system may never call it. DataStore's durable backing is in-memory for now and says so
- [x] asynchronous and synchronous methods to not block scripts — every bus call returns a `Ticket` and replies land next tick, which is the contract `:GetAsync()` already teaches. Per-world request budgets, reset at the barrier, so one world cannot starve a neighbour
- [x] recording and replay — `Recorder`/`Replayer` over a versioned format that is one snapshot plus retained input, plus `Universe::Save`/`Load`. Snapshots are byte-stable (every map is written in name order), so two of them can be diffed. Determinism is same-binary and cross-machine is documented as not promised
- [x] `mono.server --record` / `--replay` flags, proven end to end by a test that records a run, replays it and compares every entity
- [x] a scene run twice and diffed — `just determinism` records the same scene twice and `cmp`s the two files, so a wall clock, a pointer address or an unordered container that reached the simulation fails a recipe rather than becoming an unreproducible bug a week later. `just replay-check` is the stronger one and now diffs as well as runs: recording a replay has to give back the recording it replayed, which needed `--replay` and `--record` together to stop being silently ignored. `just check` chains format, build, every suite, the architecture check and both of those, cheapest first
- [x] `parallel/ipc` — `Channel`, framed bytes, bounded and non-blocking (a send that waited would stall a job worker and with it every world in the host). Local implementation; the caller cannot tell which transport it holds, which is the whole point
- [x] `parallel/process` — `Process` (spawn, poll-and-reap, request-stop, kill) over `posix_spawn`, plus `WorkersPerHost` so eight hosts on a 24-core machine do not each start 23 workers. A destroyed handle never leaves an orphan
- [x] soft-fault quarantine — a throwing system faults one world and its neighbours never notice; a hard fault takes the host and is documented as un-isolatable, because catching `SIGSEGV` and carrying on means carrying on with a suspect heap
- [x] supervisor — `PlanHosts` groups worlds into hosts (dedicated ones alone, shared ones packed, deterministic so a restart rebuilds the same grouping), plus heartbeat deadlines, restart, and a crash-loop cap. Time is passed in rather than read, so a five-second deadline is testable in a microsecond and no wall clock reaches anything reproducible
- [x] `mono.server --host` mode and a `Channel` over a socket, so a supervised host is a real process rather than an in-process one. `MakeProcessChannel` is the same `Channel` interface over a `socketpair` — framed, bounded, non-blocking, and close-on-exec on both ends so each side actually notices the other going away. `Process::Start` places one end at a fixed inherited slot, so a host needs no address, no retry and no timeout to find its driver. `HostLink` carries five signals and stamps the sender rather than trusting it. A host's universe is `Federated`: it holds worlds and no bus backend, collects and orders what they posted, and hands it up — two processes each answering the same DataStore key is the thing that cannot be allowed. Proved by four cases that spawn the real binary
- [x] multi-world rendering — a `ViewChannel` per world view: three slots and an atomic publish index rather than a lock, so a slow compositor drops frames instead of throttling a simulation. Carries an opaque payload at L4 — a draw list rather than pixels, because a host that publishes pixels needs a GPU and stops being `server` tier, and SDL3's GPU API exposes no shared texture handles. Drops are counted rather than hidden
- [x] `mono.server` moves onto `Universe` — one named world in a universe rather than a bare store, `Enter` as the only (scoped) way in, presentation run explicitly because `PreRender` on a headless server is where replication extraction lives
- [x] `mono.client` moves onto `Universe` — one named world, the universe owning the accumulator so the client no longer keeps a second copy of its own tick rate, and the renderer called from *inside* `Enter` so the camera and draw list stay where they were produced
- [x] the client composites N views through `ViewChannel` — `client::Compositor`, one channel per world. The renderer no longer reaches into a store at render time: it draws what the compositor took off the channels, so a frame at the display's rate and a world at its own no longer touch the same memory. `--worlds N` gives it something to composite; each view after the first is *placed* rather than overlaid, because two worlds' coordinates do not mean the same thing. A producer that stalled keeps its last frame and is counted stale — a world flickering out of existence for one frame is worse than a world one frame behind
- [x] the driver's side of the host link — `world::Driver`, one barrier over worlds here and worlds elsewhere: pump the links, ingest their traffic, tick, dispatch outward, poll. A world a host holds is registered in the driver's directory as `WorldState::Remote` from the moment it is planned, so a subscription or a teleport addressed to it has somewhere to go before its host has answered anything. **There is one router**, so a world's bus behaviour does not change by being moved out of the driver — proved by a differential test that runs the same script of publishes with every world local and then with half of them in a host, and requires the delivered set to match. `Envelope::From` stays stamped rather than trusted across the boundary: traffic is tagged with the host that handed it over, and a host claiming a world it does not hold is refused and counted. `mono.server --remote-world` makes it reachable end to end, with a real driver spawning real hosts, and the worker budget worked out once by the only process that knows how many there will be
- [x] reserve the replication seam — the server has authority and a client simulates its own replica, so v0.2 makes snapshot restore work into a *running* store rather than only an empty one, and a replica's bus handle refuses writes. `Store::Apply` merges into a live world under an `ApplyMode` — `Authoritative` destroys what the sender did not mention, `Overlay` leaves it — matching entities by index *and* generation so an entity the sender destroyed and recreated is a different entity here too, and reading a corrupt stream into a scratch store first so a bad packet cannot half-merge. The `Replica` resource refuses every bus write at the call rather than at review time, while the inbox still delivers. Building it surfaced one thing the wire protocol has to solve: two independently built stores allocate the same entity indices, so a replica cannot yet own a locally predicted entity — pinned by a test and carried in `ecs/docs/TODO.md` under v0.3. The wire protocol, interest management and lag compensation are a later version with their own plan, and are not started here
- [x] `mono.cdn` — the content origin scaffolded at `shared` tier, which is the strict choice: `MONO_TIER_ALLOWS_shared` is `shared` alone, so a presentation or hosting module on its link line fails the configure with the edge named rather than needing a build-option guard the way `mono.server` does. Library plus thin main, the `cdn` preset, `just cdn` / `just serve` / `just check-cdn-is-bare`, and the `expected_graph.json` row. Landed at v0.2 rather than v0.8 so the tier and the link row are settled before there is anything to move
- [x] `cdn::ContentRoot` — the boundary between a content name and the filesystem, with two checks that are not redundant: components before the disk is touched, which catches `..` and absolute names and cannot see a symlink, then containment of the resolved path against the canonical root, which catches the symlink. A symlink staying inside is served, because an atomically swapped `current` is the deployment pattern this is for. Profiling spans and `cdn.resolve.served` / `.refused`, so a refusal rate — somebody walking the origin — reads apart from content going missing
- [x] the CDN's own work was pulled forward from v0.8 rather than left for it — `Engine::assets` at L8 and the rest of `mono.cdn` are built and listed under that version. What is still open there is the wire hop and the client's barrier-applied completions, and both wait on something outside the CDN: `Engine::net`'s transports, and the world driver
- [x] `mono.engine/assets` at L8 `shared` — the second new engine module this version, after `world`. Content addressing, chunking, the hash tree, the manifest and the one signature over it; the format both halves of content delivery share, so the running game and the origin cannot acquire a dialect. Deliberately depends on no simulation module, which is what lets the origin link it alone. Full detail under v0.8
- [x] two vendors added — **BLAKE3** for content addressing and **Zstd** for delivery-group compression, both in `mono.build/MonoVendor.cmake` with an entry in each `THIRD_PARTY_NOTICES.md`. Neither is gated on a tier: portable code with no platform dependency, like asio and Crypto++. Zstd is dual-licensed and **we take BSD-3-Clause, not GPLv2** — the one choice in that file where reading the wrong text leads to a materially wrong conclusion about shipping the engine. Legacy 0.x frame support is off, as decoder surface parsing origin-supplied bytes that nothing here could have written
- [x] `just docs-check` restored to green — four dangling `[v02v03.md]` links in this file were failing the site pass, and because that pass runs first, the coverage pass behind it had never run at all. Exactly the cascade `docs/CODE_DOCUMENTING.md` describes happening once before: a check that has been failing for a while stops being read, and takes the real failures down with it. Fixing the links surfaced two genuine documentation gaps that had been invisible
- [x] the recipes a pipeline would call all exist and pass locally — `just preset=cdn test-all` runs the origin's suites rather than merely compiling them, `just test-architecture` takes a `preset=`, and `check-server-is-headless` / `check-cdn-is-bare` / `docs-check` each configure a preset from scratch. **Running `test-architecture` on the `server` and `cdn` presets is the only thing that exercises `requires` in `expected_graph.json` at all**: against a preset with every option on, an entry whose `requires` is wrong looks exactly like one that is right
- [x] `mono.cdn` wired into the documentation build — it was absent from all three of docgen's hand-maintained input lists, so its pages were never generated. The include-roots list is globbed precisely so a new module cannot be missed; the two above it are literal, so it was
- [x] smart benchmarking — `benchrunner`, a thin main over `Tool::testrunner`'s own library, so discovery, the cascading signature and the skip cache are the *same code* rather than a second copy that drifts. A benchmark declares `TEST_SUITE_ID` like a test and answers `--mono-suites` like one; `mono_suites` was split out of `testmain` so a benchmark binary gets that without linking a test framework whose per-case overhead would land inside the numbers. Reports the **minimum** sample per iteration, not the mean — a benchmark is bounded below by the work and unbounded above by the machine's mood — with the spread beside it. `just bench` / `bench-all` / `bench-accept` against a `bench` preset that optimises, and its skip cache lives in the build directory because a signature covers sources and not compiler flags. A regression is reported and never enforced: a laptop on battery swings further than most real ones
- [x] benchmark the ECS, world and renderer areas — 24 measurements across `engine.ecs.bench.iteration`, `engine.world.bench.barrier` and `engine.render.bench.overlay`. They confirm two documented claims and produce one number nothing had: `EachParallel` is **5.5x slower than `Each` at 10k entities** (21.9 us against 4.0) and **3.0x faster at 500k** (67.7 us against 202), so the ~60k crossover the default grain was chosen for still holds after the storage rewrite — **it did not survive v0.4's move to `-O3`; see there**; `EachBatchParallel` at 500k is 2.5x `Each`; a *quiet, empty* world costs about 0.4 us of barrier per tick measured serially, so 200 of them are 81 us before any simulates anything; and the debug panels cost 997 us at 4K against 60 us at 1080p — 16.7x the time for 4x the pixels, which is superlinear and the one figure here nobody had guessed
- [x] the harness earned its keep twice more: it caught a query-plan allocation showing as a 65% regression, and it caught a *fix of mine* that would have cost 1.9x. `MINIMUM_GRAINS` infers the cost of one index from the grain, which is right for rows and wrong for a world tick — four worlds dispatched are 45.7 us against 78.1 inline, and the blanket rule refused to dispatch below eight. `Jobs::For` now takes an explicit `minimum` and `Universe` passes two
- [x] the harness reports its own noise — a run that reported `+52.1% slower` for code that had just been made faster is what forced this. Each figure now carries the spread of its own samples, and a delta smaller than that spread is labelled `(noise)` and left out of the faster/slower counts. The multi-world benchmarks were running at +-25% to +-128% because thread wake-up dominates fifty empty worlds, so serial variants were added beside them: those measure the barrier rather than the scheduler and hold +-5%. A tool that cannot say how much it does not know generates confident noise
- [x] act on those numbers, first pass — the per-world barrier cost. Three avoidable allocations per world per tick: `Fanout.assign(N, {})` destroyed and rebuilt every world's delivery vector, throwing away the capacity it had just grown; handing an inbox over with `SetResource` **copies**, and a resource column holding a non-trivial type destroys and re-copies on assignment, so every world's delivery buffer was freed and reallocated each barrier; and the per-tick budget reset was a hash lookup and a copy to move eight bytes. Now a resize-and-clear, a vector swap, and an in-place write. **59.9 us to 40.2 us, a third off**, on 50 worlds fanning 2450 deliveries a tick, at +-5-9% — measured against the committed version by reverting to it and back
- [x] act on those numbers, second pass — four wins, each measured against the baseline before and after. **The overlay was clearing its whole buffer every frame**: thirty-three megabytes at 4K to erase two panels covering a fraction of it, which is why it measured fifteen times slower at 4K than at 1080p for four times the pixels. It clears the dirty rectangle instead — an invariant `Resize` and `MarkRegion` already maintained — and went **997 us to 10.2 us, and flat across resolutions**. **`VisitTables` allocated three times per query call** — a vector and a string for the plan key, plus the columns vector — on the hottest non-row path in the engine; a stack buffer and a transparent hash made the hit path allocate nothing, **25% off `CountMatching`**. **`Jobs::For` woke the whole pool for two chunks**: a ten-thousand-row parallel iteration cost 21.7 us against 4.0 serial, so a span must now hold `MINIMUM_GRAINS` of work to be dispatched — **91% off `EachParallel` at 10k**. Resources moved from a hash map to a sorted vector, which is right on its merits but measured inside the noise and is not claimed as a win
- [x] established what `Each` at 500k actually costs, rather than asserting it. The claim that it was at the memory wall was a conclusion with no control behind it, and one figure contradicted it: `EachBatchParallel` gets 2.7x from threads, which a bandwidth-bound loop should not. So the floor was measured — two flat arrays, the same three adds, no query, no table walk, no per-row dispatch. **Raw arrays are 211.7 us at 500k and 39.2 at 100k; `Each` is 203.3 and 40.1.** The ECS costs nothing measurable over hand-written array code, so there is no software overhead here to remove and the serial loop is at the single core's streaming limit. That the pool still buys 2.7x is consistent rather than contradictory: one core cannot saturate memory on its own, several can — which `EachParallel` already does. `benchmarks/Iteration.cpp` keeps the control beside the measurement, because "500k takes 200 microseconds" is unreadable without it: it could be a hardware limit or ten times more overhead than work, and those want opposite responses

- [x] measured what a thousand small worlds actually hold, which is what "chunked column storage" was filed against — and **the measurement moved the item.** A thousand worlds of a hundred entities held **72.7 MB resident against 2.7 MB of live rows**, and the culprit was not the column high-water mark this list blamed: **64 MB of it was entity-directory pages.** `SparseSet` allocates a page whole on a world's first entity, and a page was 4096 slots — of *sixteen* bytes, not the twelve the comment defending the number did its arithmetic with — so every world paid a **64 KB entry fee for existing**, which is invisible with one world and is the whole footprint with a thousand. The first page is now 512 slots and every page after it is still 4096: shrinking them all was tried and rejected, because it gives a large world eight times the allocations, they interleave with the columns and scatter them, and a multi-world tick over 100k entities each came out 8–21% slower across repeated runs. Two sizes cost one branch in the index arithmetic and nothing measurable. **72.7 MB to 16.7 MB, 77% off, with every barrier and iteration benchmark inside its own noise.** Chunked storage itself moves to v0.4 with the number that justifies it and a trigger that is now the right one — see there
- [x] archetype edge cache — the trigger was a measurement, so the measurement came first. `benchmarks/Structure.cpp` is built as a subtraction: overwriting a component already present is the floor, toggling one on and off is the same work plus two transitions, and interning alone is the part a cache could delete. **A transition cost 50.8 ns against 9.2 for an overwrite** — changing an entity's shape was five times writing to it — and **the interning was 28 ns of that**, over half, spent allocating a vector, sorting it, hashing it and taking a process-wide mutex to arrive at the answer it had arrived at the last thousand times. `ArchetypeEdges` caches add-one and remove-one per table as a short scanned list rather than a hash of a composite key, because a lookup that cost what the interning cost would be a cache that bought nothing. **11.99 ms to 6.25 ms on 100k toggles, 48% off, at ±2%**, with the overwrite path unmoved as the control. The one thing that can invalidate an edge is `Observe`, which gives a table a `DirtyBits` column and so changes where a transition lands — every lookup carries a watch epoch and a stale one wipes the cache, because that edge is not a slow answer but a wrong one: the row lands in a table with no bits and the write goes unreported. Proved by reverting the invalidation and watching exactly that test fail. **A removal edge cannot go stale**, for a reason the suite states rather than assumes — the destination is a subset of the source, so anything observed in it is observed in the source too and the source's own table index moves — and the epoch wipes both directions anyway, because a cache that invalidated only the half somebody had the argument for is one refactor from being wrong

Deferred:
- D00005

### v0.3

Replication. The server has authority and a client simulates its own replica and syncs against it — decided while planning v0.2, which reserves the seam for it (see [v02v03v04.md](docs/retired/v02v03v04.md) §2.12) and builds nothing else. It sits here rather than later so that physics, scripting and rendering are all built against a replicated world instead of being retrofitted into one.

**Where this version actually stands:** the two modules are built, covered and passing — `net` at L11 and `replication` at L12 — and **both programs now link them**: `mono.server --listen PORT` serves its world and `mono.client --connect HOST:PORT` replicates one beside its demo, over a real UDP socket between two real processes.

Worth keeping, because it is the lesson of the version rather than a detail of it: **wiring it up found four bugs that a passing in-process suite could not see.** `EndToEnd.cpp` stands both halves inside one process, where they share a component table, an allocator and a clock — so a snapshot naming a component the other build has not registered, a delta too large for a datagram, a write path that marks nothing dirty, and a client that never speaks first were all invisible. Each is written up in the item below. The remaining open lines are refinements with stated triggers; the mechanism ships.

- [x] `mono.engine/net` at L11 `shared` — the connection lifecycle, framing and channels every one of `upstream/`, `downstream/`, `predict/` and `http/` sits on. `Link` is the state machine: Connecting → Connected → Disconnecting → Disconnected, handshake and idle timeouts, keep-alive, per-tick byte *and* packet budgets with the overflow visible in `ConnectionStats` rather than as a mystery stall. **Time is passed in, never read** — no wall clock in the subsystem whose failures are hardest to reproduce, and a timeout the suite states rather than waits for. `Packet` is the wire format, and it refuses a wrong magic, an unknown version, a channel byte outside the enum, and a length that runs past the buffer; a version mismatch is refused rather than negotiated downward. Sequence comparison is wrap-aware, because a 16-bit counter wraps every eighteen minutes at sixty packets a second and a plain `>` breaks every long match. Unreliable is the default and the stale rule applies to it alone — a late reliable packet is a resend that still has to be delivered
- [x] the transports — `net::Transport` over a loopback hub and an asio UDP socket, both behind one interface a caller cannot tell apart. `Endpoint` is this engine's own value type, so no public header names a socket or an `error_code` and the transport stays swappable. A hub rather than a connected pair, because the two cases only a routed network produces are the two this layer must get right: a datagram from a sender nobody has heard of, and one addressed where nobody listens. UDP is polled from the tick and never calls `run()` — async delivery on somebody else's thread is exactly what a reproducible tick cannot have
- [x] reliability — `ReliableSender`/`ReliableReceiver`: unacknowledged payloads held and resent on their original sequence, `Acknowledge` plus `AcknowledgeBits` retiring up to 33 at a time, and out-of-order arrivals held until the gap ahead of them is filled. Both queues are bounded, because an unbounded one is a memory-exhaustion attack from a peer that simply never acknowledges. Two findings worth keeping: `Link`'s single ack window cannot retire reliable traffic at all — the unreliable counter outruns it within seconds — so the reliable receiver writes its own; and the send window has to bound the *sequence span* rather than the entry count, because payloads retire out of order and one straggler would otherwise become unacknowledgeable forever. The fuzz caught the second
- [x] transport encryption — X25519 agreement into HKDF-SHA256 into ChaCha20-Poly1305, with a `Handshake` state machine that yields two directional keys and a `Sealer`/`Opener` pair that hold no vendor type in a public header. **Nonce uniqueness is structural, not documented**: the counter is private and monotone, a `Sealer` is move-only and a moved-from one is poisoned, and there is no constructor from raw key material at all — the only source is a handshake, which runs once per fresh ephemeral pair. Verified against RFC 7748 §6.1 and RFC 8439 §2.8.2 rather than only against itself, which is how a wrong crypto implementation passes its own tests
- [x] join by full snapshot, then per-tick deltas — `mono.engine/replication` at L12, a new module: `net` carries bytes and must not know what a component is, so the knowledge of entities and tables lives one layer up and the transport stays swappable. `Authority` chunks a snapshot under the datagram limit and spreads it across ticks, then streams deltas; `Replica` reassembles, applies through `Store::Apply` and acknowledges. A client too far behind is re-snapshotted rather than repaired — a repair path is a second way to reach a correct world and only one of the two gets tested
- [x] deltas from `ChangeChannel`'s per-row dirty bits — the third reader of the bits, as planned, with no second record of what changed and no snapshot diffing. `EachChangedBatch` yields *runs* of adjacent changed rows rather than one row at a time, so a delta is a memcpy per run; `EachChangedRuns` is the runtime-keyed form replication needs, because it resolves component names off a wire and cannot name a type at compile time
- [x] client-side prediction of the local player only — `Prediction` keeps the unacknowledged inputs and `Reconcile` retires exactly what the server consumed, leaving precisely what has to be replayed. The buffer is bounded and says when it overflowed: the server may simply stop acknowledging, and the symptom on screen has a dozen other possible causes. The oldest is dropped rather than the newest, because the newest is the input the player just made and can see not happening
- [x] reconciliation — the v0.2 seam used for what it was reserved for. `Store::Apply` merges into a running store, and `Store::CreateAt` is the one primitive a delta needed that a snapshot already had internally: an entity created after the join has to arrive at the sender's exact index *and* generation, or a handle stored inside a component stops meaning the same thing on both machines
- [x] client input submission, and the replica bus refusal — inputs travel on the reliable channel, because a lost input is a jump that never happened and nothing later covers it. Which channel a message takes is decided by its kind inside `Session` and is deliberately not the caller's to choose: that is how somebody eventually makes everything reliable and turns one lost packet into a visible stall. The bus refusal is v0.2's `world::Replica`, and replication routes nothing around it — an operation a client wants performed goes up as an input and comes back as state
- [x] interest management — a predicate per client per entity, with a per-client known set that `Created`, `Destroyed` and `Forget` are all differences against. **Losing sight of an entity is a forget, never a destroy**: a client that conflated the two would delete something still there and then be wrong about it the moment it came back into view. Not deferred

- [x] the joint between the two layers — `replication::Session` binds a `Transport`, a `Link` and the reliable pair, so a message goes out framed by `net::Packet` with a real budget, a real acknowledgement window and a real resend. Proved end to end by six cases running a join and a stream over a loopback transport with real encoding, which is what makes `repo_layout.md` §16.6's claim checkable rather than asserted — there is no configuration in which this path is skipped
- [x] `core::SecureWipe` — one zeroing primitive the optimiser may not delete, at L1. It had grown three private copies: `assets` wiped a signing seed and a grant key, `net` wiped a cipher key and a handshake secret, and each carried its own spelling of the same reasoning. A module may not include another module's private header, so the only place one copy serves all of them is the module every module already links. **Three copies of a security primitive is three chances for one of them to be quietly weakened** — and a key erased with `memset` in a destructor is frequently not erased at all, because the write is dead by every rule the optimiser has. Two overloads, `uint8_t` and `std::byte`, rather than one and a `reinterpret_cast` at every call site: a cast in front of a security primitive is a place for a wrong length to hide

- [x] **`net` and `replication` wired into the two programs** — `mono.server --listen PORT` and `mono.client --connect HOST:PORT`, both link rows added, and one process is now an authority for another over a real UDP socket. `replication::Listener` and `Connector` hold the pump so that the loop is written once rather than once per program; the replicated world is a *second* world in the client's universe marked `world::Replica`, so the demo keeps running beside somebody else's authority and the v0.2 bus refusal is used for what it was reserved for. **Four bugs came out of it, and every one of them was invisible to `EndToEnd.cpp`** — because two halves in one process share a component table, an allocator and a clock. (1) The join snapshot was the *whole* store: every component whether replicated or not, every resource including the bus outbox and the DataStore records, and `DirtyBits` — which a receiving process has never registered, so the join failed outright. It is built from a scratch world holding only the replicated components of the visible entities, which fixes the crash and the contradiction of the opt-in rule in one move. (2) **A delta larger than a datagram was refused by `Link::Reserve` every tick, silently**, because a refusal is ordinary backpressure and a message that can never fit looks exactly like one. Thirty-two entities was already 1365 bytes against a 1200-byte limit: the snapshot had been chunked from the start and the delta never was. A tick's delta now goes out as however many independently applicable messages it takes, all stamped with the same tick — so `Replica` treats an equal tick as another part rather than as stale, which is the second half of the same fix. (3) **A system that writes through `Each`'s mutable reference marks nothing dirty**, so the delta path carried nothing at all: `Set` and `GetMutable` mark, an iteration cannot, and that is the engine's fastest write path. `Store::MarkAllChanged<T>` is the primitive for saying so afterwards — it over-reports by design, because over-reporting costs bandwidth and under-reporting costs correctness. (4) The connector only ever *replied*, and a server on a datagram socket cannot send to an address nobody has written from — so the real client sat silent forever while the suite passed, because the suite's client submitted an input first and the real one does not. Proved by `mono.server/tests/Replication.cpp`, which spawns the actual binary: five cases, including a 2000-entity world whose delta is many datagrams and a case that calls nothing but `Poll`
- [x] the entity-collision hole closed as far as it can be closed without the index range — **`Store::SetAdoptOnly`, set automatically on every store a `replication::Connector` writes into.** A replica now refuses to mint an entity and says so once, while `CreateAt` is untouched, because this refuses *minting* and not receiving. That is the same shape `world::Postbox` uses for a replica's bus writes and it turns the failure from "two different things quietly become one, discovered later and somewhere else" into a refusal at the call. `ecs/tests/Replication.cpp` still reaches the collision by deliberately not setting it, so what it pins is the behaviour of the storage rather than of a replica
- [x] delta compression against an acknowledged baseline — **and this line undersold itself: it read as a bandwidth item and the thing underneath it was a correctness hole.** A delta built from the dirty bits describes exactly one tick, so an entity that moved on a tick whose datagram was lost and then went still was wrong on that client *permanently* — no later delta mentions it, and nothing re-snapshots the client because it is acknowledging happily and is not behind. `Authority` now keeps, per client and per replicated component, the entities whose values have gone out and not been confirmed, and resends them until the client acknowledges a tick at or after the one they left on. The dirty-bit run walk is still the fast path and covers what moved this tick; the carried-over entries are read one at a time, which costs nothing on a link that is not dropping anything. Bounded by the visible world rather than by time — an entry is replaced rather than appended when the same entity moves again, and a re-snapshot drops the lot. Three cases, two of which fail with the resend disabled: one loss of a value that never changes again, a confirmed value falling silent, and ten consecutive lost ticks converging

Deferred:
- D00008 — the single-player `ALLOW_TIER_ESCAPE`, still not declared. The wiring is the reason rather than an excuse: `--connect` talks to a server in another process over a socket, which is exactly the arrangement where the client links no server code, so declaring it now would add an escape with no user. Trigger is a client hosting a server *in its own process*, which wants a game file first
- D00007 — priority under a bandwidth cap, and lag compensation. Both wanted their own plan rather than being squeezed in here, and both now have a written reopen trigger. The first is no longer merely absent: splitting a delta to fit a datagram gave the server a drop policy by accident — whatever is last in the component list — which is worth knowing about before it is mistaken for a broken component. **The bandwidth half was closed at v0.4, and its reopen trigger fired exactly as written — `SendsOverBudget` off zero in a real run. Lag compensation is unchanged. See v0.4**
- D00006 — `replication::Listener` admits a client on its first datagram. `net::Handshake` is built and not wired in; what bounds it meanwhile is a client limit, so the worst case is a full server rather than an exhausted one. **Closed at v0.4: the handshake is wired in behind a stateless challenge, so an unadmitted peer costs nothing. What is left is narrower and is named there — encrypting the stream, and binding the exchange to a server identity**

### v0.4

Planned in [v02v03v04.md](docs/retired/v02v03v04.md) — written when this was v0.3, before replication took that slot.

**Where this version stands:** all eleven items below are resolved. `scene` at L7, `spatial` at L6 and `physics` at L8 are built, covered and passing; `core/types` has its value types; both programs are off their duplicated component definitions and onto `scene`; the entity directory has a reserved range; and the storage layout has been reopened and closed. The layer heights the plan inferred were confirmed rather than assumed — `physics` sharing L8 with `assets` is recorded as a decision in its `AGENTS.md`, with the reason it is safe (the two modules are disjoint, so there is no edge for the layer rule to adjudicate) and the condition that would break it.

**Two of the eleven were closed by a measurement rather than by code, and both measurements contradicted the item that asked for them.** That is the second time this has happened — v0.2's chunked-storage item was moved by the same kind of finding — and it is worth stating as a pattern rather than as two coincidences: an item written from a plausible mechanism, sitting unmeasured for a version, is an item whose premise nobody has checked.

**One thing outside this list is worth more than most of what is in it.** Wiring both programs onto one component set closed v0.3's stated gap: the client now *draws* the world it replicates, rather than replicating it into a shadow component set nothing rendered. `mono.client/include/client/Replicated.hpp` used to be that translation layer and is not any more.

- [x] chunked column storage with a shared span pool — **and the 703 MB figure that justified it finally has a benchmark behind it, because it never did.** Measured on the shape the item names: a thousand worlds that each grew to ten thousand entities and settled back to a hundred held **820.3 MB of accounted storage against 3.8 MB of live rows; they now hold 13.7 MB** (process RSS 620.7 MB to 85.2 MB — the residual is glibc holding freed blocks in its arenas, not the storage, which `malloc_trim` moves by 0.1 MB). A world that was *always* small is unchanged in kind, as expected: that case was v0.2's directory pages and was already fixed. **`Each` at 500k came out 4.6% faster and `EachBatchParallel` 10.2%**, which is not what a layout change was supposed to buy and is the chunks being warm rather than one 6 MB allocation being cold. What it cost is on the record too: structural change is **6-9% slower** — `At` is a directory lookup now, and a removal has to notice an emptied chunk — and one control regressed 13.5% because chunks come back from the pool in release order rather than address order, which is a fixable thing nobody has measured yet
- [x] chunked column storage: **a fixed chunk size was the plan and it is wrong, found by crash rather than by reasoning.** A *resource* is a single-row `Column`, and some are 720 bytes wide, so a fixed 1024-row chunk charges every world **1.4 MB to hold one resource** — a regression bigger than the leak, and one that hits every world rather than only the ones that shrank. Fixed 1024 also cost **8.0% on `Each · 100k`**, which is the `SparseSet` 8-21% scatter precedent repeating exactly as v0.2 warned it would. Shipped instead: **chunk 0 holds 8 rows — the deleted `FIRST_CAPACITY`, so a world that never grows is not one byte worse — and chunk *k* holds `8 << (k-1)`.** Capacity is never more than twice the rows, `ChunkOf` is one `lzcnt` rather than the divide-and-modulo this list predicted, and boundaries are a function of the row index alone so every column in an archetype still divides identically, which is what `EachBatch<A, B>` needs. Cost against the old layout: **+0.4% at 100k**, against +8.0% for the size the plan named
- [x] chunked column storage: **the item's load-bearing claim was false, and no test in the tree could tell.** "Legal against the iteration paths as they stand" is true of `EachBatch`'s *contract* and false of `EachBatchParallel`'s *implementation*: the dispatch decision is made per slice against a 32,768-index floor, so one slice per chunk puts every slice under it and the pool is **never woken** — a 3.5x loss with nothing failing. The chunk split therefore lives inside `VisitBatch` and inside the `Jobs::For` worker body, and `VisitTables` still yields one slice per *table*. Three more sites read a column as one buffer and were not iteration at all — `ClearChanges` and `MarkAllChanged` both `memset` from chunk 0 over the whole row count, which is a heap overrun rather than a compile error. **The clip in `EachChangedRuns` was the one mutation of five that no test caught**: 320 ECS, 69 replication and 27 server tests all passed with runs straddling a chunk boundary, and an unclipped run is a delta that sends one entity's id with another's value — a corruption that shows on a client as objects teleporting. Two cases now cover it
- [x] the entity directory's own leftover, taken in the same pass — and **the plan over-specified it in one direction and under-specified it in the other**. `SparseSet::Free` *does* release pages, contra the fear that it would need an O(FreeList) purge: a page that empties releases every trailing dead page, the high-water mark comes down with it, and a free-list entry naming a released index is discarded once when it surfaces, which is amortised O(1). This was not optional — without it a settled world still held 239 KB, **204 KB of it directory**. What the plan missed is that `Clear` keeps pages *precisely so generations survive*, so releasing one loses the generation and a handle issued at generation 1 comes back **alive**; a retained per-page epoch, kept even when the page's memory is not, makes that impossible by construction rather than by care. Four bytes per page, and it spans both index regions
- [x] the pool, and a trim policy — because **a span pool that never trims relocates the leak and reports success.** Process-wide rather than per-store, since per-store keeps the spares a thousand times over; one mutex, taken about seventeen times per column *lifetime* rather than per row. A hard 8 MiB retention cap, chosen against the item's own measurement — a thousand settled worlds hold 3.8 MB of live rows, so a host at that shape retains less in spares than in data — and pinned by a test rather than described in a comment. Building it surfaced **two pre-existing use-after-frees at exit**, both static-destruction-order: a `Column` destroyed during teardown reaches the chunk pool and the component registry, and both are function-local statics constructed *later* than whatever static owned the store. It showed up as `bench_physics` printing its whole report and then segfaulting, which then made every suite measured after it look slow — so it had been quietly contaminating benchmark runs
- [x] vectorisable component layout — **rejected, and the measurement did not merely fail to support the item, it refuted the reasoning.** The premise was that a compiler cannot vectorise a twelve-byte stride cleanly, so padding to sixteen is the way past the streaming limit. Measured on the standalone control at `-O3`: **twelve bytes is 16.61 us at 100k and 85.63 at 500k; sixteen bytes is 41.93 and 201.54.** The packed layout is **2.4x faster**, and the reason is the exact opposite of the one assumed — a twelve-byte AoS of floats has no gaps at all, so the vectoriser treats the column as one flat float stream and fills every lane, while padding puts a dead lane in every vector. Through the ECS the change was inside run-to-run spread everywhere except `Each over two archetypes · 100k`, which got **14-19% slower**. The acceptance criterion set before the work — *10k and 100k improve by more than 500k regresses* — was not met, because nothing improved. Reverted; `scene`'s components are untouched; the number is written into `ecs/docs/TODO.md` under "Rejected" beside the occupancy flag, so the idea is not re-proposed without new information
- [x] and the finding that came out of rejecting it, which is worth more than the item was: **the same loop is 2.4x faster at `-O3` than at the `-O2` the `bench` and `release` presets actually used.** A compiler flag, not a data-layout rewrite, and invisible for as long as every measurement was taken at the same optimisation level as every other
- [x] **`release` and `bench` now compile first-party code at `-O3`, and the objection was answered rather than waved past.** The fear worth taking seriously here was floating point — this repository diffs two runs byte for byte — so the test was stated before the flag was changed: `just determinism` and `just replay-check` against the `release` preset. **Both byte-identical.** GCC enables neither `-ffast-math` nor `-funsafe-math-optimizations` at any `-O` level, so IEEE semantics never moved. Two places had to change, and the second is the one that would have rotted quietly: first-party targets now *state* `-O3` instead of inheriting `RelWithDebInfo`'s `-O2`, and `mono_add_benchmarks` had pinned `-O2` of its own — the two agreed by coincidence rather than by construction, so the benchmark binaries would have gone on measuring a build nobody ships
- [x] **the suite has now been run at the level that ships, which it never had been.** `release` has `MONO_BUILD_TESTS` off and `bench` inherits that, so *every* test in this repository's history has run at `-O0`. Raising optimisation is precisely what surfaces latent undefined behaviour, so a one-off optimised-with-tests configure was built for the change: **104 suites and 18 `ctest` targets, all pass.** Worth saying plainly because it is a standing gap rather than a fact about this change — nothing in the presets runs a test against optimised code, and nothing noticed
- [x] the measured outcome, on a quiet machine against the accepted `-O2` baseline, **and it is not uniform — which is why it is a table rather than a headline.** Serial row iteration roughly halves: `Each` **-57.1% / -50.7% / -51.4%** at 10k / 100k / 500k, `EachBatch` **-57.8% / -52.4% / -51.0%**, the raw-array control **-59.2% / -52.9%**, and `Tick · 1 world of 100k` **-71.7%**. The solver is **7.6-11.0%** faster and the spatial rebuild **4-9%**. Against that, a handful of paths are genuinely worse at tight spreads: `Each with a narrowing term · 100k` **+11.1%**, the broadphase pair walk **+7.8%**, `intern only · 10k` **+5.3%**, `toggle · add and remove, 10k` **+4.7%**, `CountMatching` **+4.3%**. The pattern is that straight-line work over contiguous rows vectorises and the pointer-chasing around query planning and structural change does not, and slightly resents the inlining. Net 24 faster, 11 slower, and the ones that got faster are the ones the engine spends its time in
- [x] one thing this immediately invalidated, opened as `D00012` and then closed by measuring it: **`Jobs`' dispatch threshold is a ratio between two costs and only one of them just changed.** `DEFAULT_GRAIN` 4096 times `MINIMUM_GRAINS` 8 means nothing below 32,768 rows reaches the pool — a threshold argued from a crossover measured when the serial loop was twice as slow
- [x] and measuring it **disproved the reason it was opened for**, which is the outcome worth recording. `EachParallel · 10k` reading 17.6% slower is not the job system: 10k is *below* the dispatch floor, so that benchmark times the **inline** path and the floor was doing exactly its job. What moved is the optimiser — that body writes one float of a twelve-byte row, and `-O3` turns a stride-12 read-modify-write into shuffles slower than the scalar loop `-O2` emitted. Proved by rebuilding the same translation unit at `-O2` and running both: the parallel row goes 2.12 to 2.44 us and **a new serial control with the same one-field body goes 2.13 to 2.40** — they move together, and neither moves with `Each`'s three-add body, which halves. The two rows were never a fair pair in the first place. A test now requires `Participants == 1` at 10k, twenty-five times running, so nobody re-derives the wrong conclusion
- [x] the crossover itself, re-measured: **~262,144 rows for the cheapest body, not 32,768 and not v0.1's 60-80k** — and the ceiling past it is **1.3x rather than 3.5x**, because at 500k both paths stream twelve megabytes and the limit is bandwidth. The other half of the ratio had never been measured at all and now is: an empty `For` costs **48 ns** to decline, **31 us** dispatched to 23 workers and **2.3 us** dispatched to one. **The handover is linear in the pool, not in the work** — every worker decrements `Batch::Outstanding` under one mutex whether it took a range or not, so a batch waits for 23 threads to queue through it. That serialised join is the thing to attack if the crossover ever has to come down, and it is a rewrite rather than a constant
- [x] **and the decision was to change neither `Jobs` constant, because neither can move.** The two measured crossovers are 262,144 rows and 8,000 rows — 32x apart as rows, and 49 us against 29 us as *serial work*, which is one handover either way. A grain constant is a row count trying to express a duration, so it can only be right for one row cost. `MINIMUM_GRAINS` multiplies every caller's floor including the one that measured; `DEFAULT_GRAIN` would need to be 32,768 to suit the cheap body and would then refuse a handover that pays for `mono.client/src/Replicated.cpp`, which writes a `CFrame`, two vectors and two ids per row. One constant, two jobs, opposite directions — so the values stay and the justification at both is replaced with the numbers, since a stale reason at a constant reads as evidence and is worse than none. That is the *second* stale crossover this version found and nothing in the build notices when such a number goes stale
- [x] the two items above were filed as **"one decision wearing two names"** and that turned out to be wrong in a way worth recording. They were sequenced together to avoid rewriting `Column`'s internals twice — but the internals changed once, in the chunking pass, and the layout change touched only the *struct definitions* in another module. They were separable all along, and had they been landed as one commit the 33% extra memory traffic from padding and the chunk-boundary cost would have been unattributable to each other. Landing them as one pass and several commits is what made it possible to reject half of it on evidence
- [x] `mono.engine/scene` at L7 — Basic Components: Transform, PreviousTransform, Bounds, Visual, Collider, RigidBody, Surface, Motion, Camera, QuickHash. Deletes the duplicated component definitions, which are all in headers rather than sources: `Transform`, `PreviousTransform`, `Visual`, `SceneBounds` and `ActiveCamera` in `mono.client/include/client/Demo.hpp`, and `Position`, `Velocity` and `WorldBounds` in `mono.server/include/server/Simulation.hpp`. `Spin`, `Orbit`, `DrawList`, `Chatter` and `Heard` are demo and placeholder types rather than duplicates and stay where they are. The C++ test scene itself stays until v0.5/v0.6 give it a game file to load
- [x] `scene::DrawInstance` — the draw-list payload v0.2's `ViewChannel` carries, at `shared` tier because a `server`-tier host writes it and a `client`-tier consumer reads it. Replaces `render::Instance` as the thing a world publishes. `mono.server` gains **`scene`** in its link row — it already links `world`, which it did not when this was written
- [x] an index range for locally predicted entities — **moved here from v0.3, with the hole guarded in the meantime.** Entity identity is an index plus a generation and two independently built stores both start at index 0 generation 1, so an entity a replica mints collides exactly with one the authority minted and `Store::Apply` is right to merge them. v0.3 made that unreachable rather than fixed: `Store::SetAdoptOnly` refuses to mint in a replica, so the collision cannot be walked into. The actual fix is a high index range the authority never allocates from, plus promoting a predicted entity to a server one as an explicit step. **It lands here because it changes how the entity directory is laid out and therefore how a snapshot writes it** — `SparseSet` needs a second page region, since a reserved range at 2³¹ under one linear page list would try to allocate half a million pages to reach it, and the snapshot writes the directory as a run of `Capacity()` entries and so needs the same split and a format bump. v0.4 is already reopening the storage layout for chunked columns and vectorisable components; one format bump beats two. **Trigger is unchanged and still unmet: the client predicting anything that spawns**, which is the first projectile — and a projectile needs the physics and the `Part` this version brings, which is the other reason the design should not be guessed at before its consumer exists
- [x] `core/types`: AABB, Ray, RayHit — the value types v0.4 gives a consumer
- [x] `mono.engine/spatial` at L6 — uniform hash grid, and the optimised spatial query: raycast, overlap and shapecast with layer masks
- [x] basic physics collider — box, sphere and cylinder shapes, `Collider`/`RigidBody`/`Surface`, and the `SurfaceTable` resource the narrow phase reads once
- [x] basic physics pipeline — integrate, broadphase with sorted pairs, six exact analytic narrow-phase pairs, a serial sequential-impulse solver, contact events
- [x] Part — a class rather than a component: `{Transform, Bounds, Visual, Collider, Surface}`, with `PartDesc` and `MakePart` as the one place that decides what a part is
- [x] Camera — the `Camera` component, plus the `ActiveCamera` resource holding the live one and its resolved matrices

- [x] **both programs off their duplicated components, which is what item 3 was actually for** — `Transform`, `PreviousTransform`, `Visual`, `SceneBounds` and `ActiveCamera` gone from `mono.client/include/client/Demo.hpp`, `Position`, `Velocity` and `WorldBounds` gone from `mono.server/include/server/Simulation.hpp`. `Spin`, `Orbit`, `DrawList`, `Chatter` and `Heard` stay, because they are demo and placeholder types rather than duplicates. `render::Instance` and `render::Camera` are **deleted**: a world publishes `scene::DrawInstance` and `render` converts it at the point of upload, which also removed the second view-projection `Renderer.cpp` was building beside `scene::ResolveCamera`. Measured by running the client rather than by a benchmark, 2048 entities uncapped: **1717 fps before, 1822 after** — `CollectInstances` stopped building a matrix and writes 64 bytes where it wrote 80, and the matrix build moved into memory the upload was going to touch anyway
- [x] **the client draws the world it replicates** — v0.3 left this open and said why: the two programs had no shared component set, so the client replicated into `ReplicatedPosition`/`ReplicatedVelocity` under the wire names `server.Position`/`server.Velocity` and nothing rendered them. Both sides now register the same `scene` names, so a snapshot and a delta cross with no translation layer at all. Verified with `--connect` against a real `--listen` server: 512 demo entities plus 64 replicated reads `ENTITIES 576`, and the stronger check — the same run at `--entities 0` — reads `ENTITIES 64  TRIS 768`, so every triangle submitted came from the replica. **Two honest gaps left in the code rather than papered over**: the replica is not interpolated, because the two states worth interpolating between are two *received* ticks and that is snapshot buffering, which belongs in `replication`; and it has no camera of its own, which needs the predicted-entity range's promotion step to have a policy
- [x] `just preset=ci check` **builds and passes, which it had never done** — the `ci` preset makes every warning fatal, and it was failing on `-Wmissing-field-initializers` in `core::Arguments` and `-Wdangling-reference` at five sites in `world`'s suites. So the recipe whose entire purpose is "what the pipeline actually enforces" was the one configuration nobody could run. `D00005` says the checks a workflow would run "are written and pass"; that was true of `just check` on the `dev` preset and false of the preset the same file names as the enforcing one
- [x] **a real replication bug that four separate runs had mis-diagnosed as flakiness** — `mono.server`'s cross-process case *"a world too big for one datagram still streams"* failed roughly one run in three under load, and was twice written off as a wall-clock deadline miss. It is not: raising the budget to twenty-four seconds does not help. `Authority::Publish` advanced the snapshot cursor when a chunk was **built**, and `Listener::Publish` discarded `Session::Send`'s return value — so when the reliable send window filled, the refused chunk was never tracked, never resent, and the authority had already moved past those bytes and freed the buffer. 192 chunks, 8 refused, **184 applied** — byte-exact with the reported figure. A permanent hole, a join that never completes, and every later delta refused as stale: **17,865 refusals against 184 applications**. `Authority::Unsent` now takes back what the transport refused and rewinds the cursor and the known-set edits. This is v0.3's delta bug in a path the `EmitDelta` fix did not cover, and the module's own `AGENTS.md` had already written down the shape of it. **40/40 on both presets afterwards, against 9-in-10 failing before**
- [x] two more of the same family, found while fixing it: a `Forget` was never split, so a three-hundred-entity forget is one oversized message the link refuses outright; and `ResnapshotAfterTicks` was measured against the tick number rather than against how far behind the client was, so a client in perfect agreement with a **quiet** world was re-snapshotted every 121 ticks forever
- [x] **`D00007`'s bandwidth half — priority under a cap.** The item's own reopen trigger is `SendsOverBudget` moving off zero in a real run, and the bug above is exactly that: a tick's delta was ~137 messages against a 64-packet budget, so 73 were dropped every tick with the tail chosen by position in a vector. Now a per-client score with a rotation that outranks it, so **nothing starves** — a value that has waited `StarvationTicks` jumps every score, and the bound is `StarvationTicks + ceil(n/k)`, asserted rather than argued. Per client rather than per server, and the header says why: the budget is a link's and there is one link per connection, so a server-wide cap would have to be divided before it could be enforced and that division *is* a per-client cap. Costs nothing when there is no pressure — rows are packed in dirty-bit order first and only re-packed by score if that did not fit. `Statistics::Deferred`/`Stalest` mean "the budget was exceeded" and `ConnectionStats::SendsOverBudget` still means "the link refused", which is the distinction D00007 says people will otherwise get wrong
- [x] **`D00006` — a stranger's first datagram no longer takes a slot.** `net::Handshake` was finished and unused for a version; it is wired in behind a **stateless** challenge, which is the part that matters: the cookie is an HMAC over the peer's address and its key-exchange message, keyed by a rotating secret, so an unanswered challenge costs **zero bytes and stays zero however many are outstanding**. Proved rather than asserted — two hundred distinct endpoints say hello, nothing is allocated, and the *first* of them still answers successfully, which no bounded table would manage. Ephemeral keys come from the OS CSPRNG and **not** from `core::Random`, which is deterministic by design and whose determinism is the whole reason it must never produce a key. Admission is an injectable policy the game supplies, defaulting to one whose behaviour the header states in as many words: it admits anybody who completes the handshake, which proves the peer can receive where it says it can, and nothing about whether it is welcome. `MaximumClients` stays in front of the handshake, because the bound is what makes this a gap rather than a hole. **The stream itself is still in the clear** and both `AGENTS.md` files say so, so that "handshake" is never read as "encrypted"
- [x] the smaller half of `D00006`: an entity with **no** replicated components no longer appears in the join snapshot as a bare row. It leaked a count rather than data, and it contradicted the opt-in rule in the same way v0.3's whole-store snapshot did
- [x] **a registration bug that only one test order in twenty could see.** Reading `PhysicsWorld` off an unprepared store went through `Store::Resource<T>()`, which resolves by `ComponentId` and therefore *mints* one under the compiler's spelling for anything unregistered — so asking a world whether it had a physics resource is what registered it wrongly, and the later explicit `physics.PhysicsWorld` was correctly refused as a second name for one type. The process aborted, in a different case each run, depending on Catch2's shuffle. It is the cross-process hazard the explicit-naming rule exists to prevent, reached from inside a single process: `Components::Find` takes a name and registers nothing, which is what the snapshot loader already uses and what this should have used
- [x] **measured, and it moves a number this file has carried since v0.1**: the serial/parallel crossover for `IntegrateMotion` is **~4096 rows, not the 60-80k** the `EachParallel` default grain was chosen against. The old figure was taken on a three-multiply-add body over twelve-byte structs; a row carrying a `CFrame` and integrating a quaternion is expensive enough to repay the handover an order of magnitude sooner. 20k entities: 80.2 us serial against 28.5 parallel. The grain and the crossover are now deliberately the same number. **Re-measured after the `-O3` change and it moved again — the crossover is ~8000 rows, so a grain of 512 was dispatching a six-thousand-row world into a measured 1.27x loss. `INTEGRATE_GRAIN` is 1024, which is also 9-18% better at every count above the floor because a range costs about 95 ns to hand out whatever is in it.** Recorded because of what it says about the class of number rather than this one: the constant that was *supposed* to be the carefully-measured one went stale within a version, from the same cause as the default it was written to correct

Deferred:
- D00007 — **half closed.** Priority under a bandwidth cap is built, measured and bounded; **lag compensation is not started and is unchanged**, still wanting a server-side history buffer this module deliberately does not keep and a fairness policy that is a game-design decision rather than an engine one. Trigger is still the first hitscan weapon
- D00006 — **the hole is closed and what is left is narrower and now named**: transport encryption of the stream, and binding the exchange to a server identity. The keys are derived and used to confirm the exchange, then destroyed. Until the second one lands the agreement is unauthenticated against a relay, which is why the default policy's honest description is load-bearing
- D00002 — the graph renderer. Untouched; still wants `mono.engine/graph` at L9, which does not exist
- D00004 — Crypto++ in `core` for `core::Random`. Untouched, and its trigger is unchanged: the demo dies at v0.6. Worth noting that `net` now has a *second* reason to link crypto, so the question when the demo dies is narrower than "does anything still need `Random`"
- D00008 — the single-player `ALLOW_TIER_ESCAPE`, still not declared and still with no user. Unchanged: `--connect` talks to another process over a socket, which is exactly the arrangement where the client links no server code

### v0.5

The engine has been full ECS since v0.2 and userland instancing is a façade over it — see [docs/retired/v02v03v04.md](docs/retired/v02v03v04.md) and [docs/retired/v05.md](docs/retired/v05.md). This version makes the façade reachable from script, and **the scene moved a version early**: `Rings.luau` and `Rings.js` build and animate it themselves. What v0.6 keeps is what a scene file cannot yet say — a camera, its own scripts, and the rendering they describe.

- [x] **a property stopped being a component and an offset and became a conversion** — the item below assumed a manifest could be generated from the property lists, and **the property lists were empty and the model could not have described them.** `Classes::Property` was called zero times outside `ecs`'s own suites, and `PropertyDescriptor` was `{component, offset, type, size}`: enough for `Visible`, and incapable of `Size` (a doubled half-extent), `Position` (a sub-range of a `CFrame` whose rotation must survive) or `Anchored` (not stored anywhere — it is whether `RigidBody` and `Motion` exist). Roblox's `Size` is a full extent and `Bounds::HalfExtent` is half of one, so **a member pointer was the wrong primitive**, which is what `scene/AGENTS.md` had been saying under "do not add half of one". A descriptor is now a getter, a setter, the component sets it reads and writes, and a kind — `Field`, `Computed` or `Structural`. One mechanism: a plain field's conversion is generated from a member pointer taken as a *template* argument, which is what makes it captureless
- [x] `PropertyType` gains `Vector3`, `CFrame` and `Color3` — and stays small permanently, because under conversions it describes the **userland value** rather than the storage. `spatial::LayerMask` will never need a case: no property is one, `CollisionGroup` is a `Name`
- [x] `core::CFrame::ToAngles` — the inverse of `CFrame::Angles`, missing since v0.1, and needed the moment a script can read an `Orientation` it will assign back. Intrinsic Y-X-Z, matching `Angles` exactly, because any other Euler order round-trips to a *different rotation*. **The gimbal threshold was wrong first and the reason is worth keeping:** it tested `cos(pitch) < 1e-6`, and at a true pole the quaternion round-trip leaves `sin(pitch)` at 0.99999994, so `cos` reads 3.5e-4 — a thousand times the threshold — while the two matrix elements feeding `atan2` have already lost every significant bit. **A test on the derived angle cannot see that; a test on the operands can.** Now measured from the operands at 1e-3, with a 512-triple sweep and a pole case behind it
- [x] `scene` declares the property surface: `CFrame`, `Position`, `Orientation`, `Size`, `Color`, `Visible`, `Mesh`, `Material`, `CanCollide`, `Anchored`. Roblox's names, for the reason `Part.cpp` already gives about the class tree. Four are plain fields; six are conversions. **`Size` writes two components** — `Bounds::HalfExtent` and `Collider::Extent`, which `MakePart` sets from one number, so a setter moving only the first leaves a part drawn at one size and collided at another with nothing reporting it. **`CanCollide` is the inverse of `Collider::Trigger`, not the layer mask**: clearing a mask to say "no" and restoring `All()` to say "yes" destroys whatever a game configured, and `Trigger` is what Roblox's `CanCollide = false` actually means. Two are named as gaps rather than declared — `Transparency` has no field (it becomes a renderer feature at v0.6) and `CollisionGroup` needs a name-to-layer registry that does not exist
- [x] `Store::GetProperty` / `SetProperty` by name — bytes and a size, size-checked so a `Vector3` handed to a `CFrame` property fails instead of writing twelve bytes into twenty-eight. **The dirty bit came for free and that was a finding rather than a plan:** a conversion reaches its component through `GetMutable`, and `StoreState` already counts handing out a mutable pointer as a write. A replica refuses loudly, because a script author cannot tell "rejected" from "applied and overwritten by the next delta"
- [x] **`mono.engine/script` at L9 — Luau runs.** `Instance.new`, property get and set, `Vector3`/`Color3`/`CFrame`, `print` into the engine log. **The marshalling switches on `PropertyType` and never on a name**, so a property `scene` declares tomorrow is reachable today. Sandboxed as a design rather than a hardening pass: no `os` (wall clock — a script branching on one produces a run that does not replay) and no `debug` (stack frames and upvalues, the library for climbing out of a sandbox), frozen globals, hidden metatables, a memory ceiling through the allocator, and a **step budget counted rather than timed** — a wall-clock deadline would make whether a script finished depend on how busy the machine was. A yield is refused rather than resumed, because v0.6 has to take that decision deliberately and a convenient default here is how it gets taken by accident. The VM is `VENDOR` and no `lua_State` reaches a public header, which is what makes the second VM a source file rather than a second module
- [x] **the test scene is a script, a version early** — `mono.engine/examples/Rings.luau` builds the 512-cube ring scene and `--script` loads it. 1172 frames at 59.9 avg fps, byte-identical determinism and replay still passing. **`DemoPart` is registered by `mono.client`, not by the engine**, which is the part worth keeping: `Instance.new("DemoPart")` goes through exactly the path `Instance.new("Part")` does and neither `ecs` nor `script` knows the class exists. What is scripted is the *scene*; `orbit` and `spin` are still C++ systems over columns, which is the split v0.6 keeps
- [x] **`mono.engine/examples` at L10 — the scene is the engine's, and all three programs load it.** The ring scene lived in `mono.client/src/Demo.cpp`, so only a client could ever build one — and **a scene is not a client-tier idea**: a server authors the same world and replicates it, and the unified harness runs both halves against one. `Orbit`, `Spin`, the `ExamplePart` class and its properties, and the three systems that move it are all here now; `mono.engine/examples/Rings.luau` is staged into the assets tree so any program finds it from any working directory. `--script` on the client, `--game` on the server (a flag that had been accepted and ignored since v0.3), `--scene` on the unified harness. **Proved end to end rather than asserted: the harness reads `512 entities on the server, 512 on the client`, all 512 drawn.** What stays per-program is exactly the part that is: a client adds a camera and a draw list, a server adds neither
- [x] the world extent is measured from what a script built rather than declared by it — **and finding out what "built" means cost a run.** The first version measured `Transform` alone and reported an extent of 1.0 for a scene twelve units across, because *nothing has moved yet*: a script sets `OrbitRadius` and leaves the transform at the origin, and the position only becomes true after the first tick — long after a camera has been framed from the number. Where a thing will be is part of how far a scene reaches

- [x] **the bindings manifest** — `mono.tools/bindings` emits `mono.engine/script/bindings/manifest.json` from the class table: five classes, their trees, their component sets, and every property's type, kind, byte width, writability and the components each side reads and writes. **No offsets anywhere in it**, which is not restraint — a property is a conversion, so there is no byte offset to leak, and rule 4 is satisfied by construction rather than by a disclaimer about which fields survive a recompile. Versioned from the first commit. Component names are sorted rather than left in registration order, because registration order depends on which translation unit ran its static initialiser first and a manifest that changed when a link line moved would fail its own check for a reason nobody could act on
- [x] **`just bindings-check`, inside `just check`** — regenerate, diff, fail on drift, and **proved to fail rather than assumed to**: renaming one property in the checked-in file makes it exit non-zero with the name and the recipe to fix it. Rule 6 is what makes it mandatory, and this repository has twice watched a check rot into a false claim
- [x] **the `.d.ts` and Luau declarations**, generated from the same manifest so the two surfaces cannot drift into two APIs. The TypeScript one is a type root, which is the shape a toolchain consumes in place of `@rbxts/types`; the derived interfaces carry only what each class declares itself, because repeating an inherited property would be a second declaration of one fact that TypeScript would silently accept a narrowing of
- [x] **the concurrency document** — [docs/retired/SCRIPT_CONCURRENCY.md](docs/retired/SCRIPT_CONCURRENCY.md), and it builds nothing: `Universe`, `ExecutionMode`, `Postbox` over four buses, `Ticket` and the barrier all exist. It settles the rule everything follows — **a script may only resume from something the barrier delivers in a deterministic order**, which is narrower than "no yielding" and stricter than "yield freely" — and then what falls out of it: `wait` in ticks with a recommendation to rename rather than redefine, a value-to-bytes codec that must sort keys and produce **identical bytes from both VMs** (one shared test, not two), a cross-world lock that is `MemoryStore::Update`'s versioned compare-and-swap because rule 3 leaves no shared memory to guard, and budgets and replica refusals as part of the contract rather than as implementation detail
- [x] **QuickJS is linked, and JavaScript runs beside Luau.** `Runtime` is an interface; `LuauRuntime` and `JavaScriptRuntime` are two files in one module and adding the second changed no caller — which is what the module's `VENDOR`-not-`VENDOR_PUBLIC` row was for. The extension picks the VM, so `Rings.luau` and `Rings.js` build **the same world** through the same bindings and every program loads either. **Three things the tests caught rather than the design:** `JS_NewContext` adds `Date`, so the context is built from `JS_NewContextRaw` and an explicit intrinsic list; `JS_AddIntrinsicEval` turned out to be required by `JS_Eval` itself, so excluding it produced "eval is not supported" for every script rather than a sandbox, and the global `eval` is deleted afterwards instead; and `JS_AddIntrinsicBigInt` over a raw context leaves an object alive so `JS_FreeRuntime` asserts, reproduced against upstream in isolation. Instances are sealed and scripts run in strict mode, because in sloppy mode assigning to a non-extensible object **silently does nothing** — `part.Transparency = 0.5` would have looked like it worked
- [x] **`Instance.new`, properties and the hierarchy bind to the class table — the same one C++ calls.** A calling convention rather than a second mechanism: `Instance.new` goes through `Store::CreateInstance`, `part.Size = v` through `Store::SetProperty`, and `part.Parent = workspace` through `Store::SetParent`. The marshalling switches on `PropertyType` and **never on a property's name**, so a property `scene` declares tomorrow is reachable from both languages today. **What is not bound yet is named in v0.6 rather than implied**: `.Changed`, `:IsA`, `:Destroy`, `:Clone` and `:GetChildren` all exist in C++ and none of them has a script spelling
- [x] **`--script PATH` stops warning and starts loading**, which closes the oldest bullet in `D00001` — it had been accepted and ignored since v0.1. `--script` on the client, `--game` on the server (ignored since v0.3), `--scene` on the unified harness, and the extension picks the VM
- [x] **the C++ test scene is a script, a version early** — `Rings.luau` and `Rings.js` build the 512-cube ring scene and **animate it themselves** off `RunService.Heartbeat`, composing `CFrame`s the way a Roblox script does. There is no `Orbit` component on that path and no engine system reading one: the engine does not know what an orbit is. `Demo.hpp`/`Demo.cpp` do **not** go away yet, because the C++ path is still what `--entities` builds and is the honest comparison — a scene format and a scripting layer, side by side, with the cost of each on the record
- [x] **`game` is the universe and `workspace` is the world a script runs on**, which is the foundation the rest sits on. `game:GetService` reaches services; `workspace` is the world itself and **not an instance in it**, because a world is what entities live in and making it an entity would put a phantom row in every scene. A script holds one `Store` and no binding hands it another — asserted, not assumed — so **the only way out of a world is a service in the universe**: `MessagingService:PublishAsync` / `SubscribeAsync` over `world::Postbox`, proved across two worlds in one universe. Rule 3 as an API rather than a convention
- [x] the datatypes and globals a scene actually needs: `Instance`, `Vector3`, `CFrame` (with `*` composition and `Angles` in **radians**, matching Roblox, while `Orientation` stays degrees — Roblox's own split, reproduced deliberately), `Color3` with `new` and `fromRGB`, `game`, `workspace`, `RunService.Heartbeat:Connect`, `MessagingService`, `print`, and `nil` as a JavaScript global so one idiom reads in both. The standard libraries a deterministic script may have — `bit32`, `buffer`, `coroutine`, `math`, `string`, `table`, `utf8`, `vector` — and **deliberately not `os` or `debug` in Luau, nor `Date`, `eval` or `Proxy` in JavaScript**, because each is either a wall clock or a way out of the sandbox


### v0.6

Where a world *contains* its scripts rather than being handed one, and where the renderer catches up with what a script can already say. v0.5 made the façade real; what is left is the half a Roblox author reaches for next — signals, the instance methods, the datatype vocabulary — and the rendering those scripts describe.

**The rule everything script-side lands under is already written**: [docs/retired/SCRIPT_CONCURRENCY.md](docs/retired/SCRIPT_CONCURRENCY.md) §1 — a script may only resume from something the barrier delivers in a deterministic order. Every item below that yields answers to it.

**Where this version stands.** The script half is built and measured; the render half is not started. That split is not arbitrary — everything below the line needs `mono.engine/graph/` at L9, and everything above it needed only the class table `scene` already declared. The items are marked individually rather than the version being called done.

- [x] `.Changed` per instance, and the fan-out the projection model creates. `ecs/docs/TODO.md` had the engine half — `OnChanged<T>` is per *type* and Roblox's is per instance, so filtering a whole-world signal per script would walk every change for every connection. The half nothing had written down: **one component write maps to N property names**, so writing `Transform` fires `CFrame`, `Position` *and* `Orientation`. `PropertyDescriptor::Reads` is exactly the map that needs, which is why it was declared rather than left to a conversion to know privately. `script/src/Changes.cpp` is the fan-out; it subscribes **once per component for the whole world** and filters to watched instances with a hash lookup, so the cost scales with what somebody watched and not with what moved — `benchmarks/Surface.cpp` measures 97 ns per watched instance and 1.9 us for a quiet tick. Fires at the **next** beat rather than at the write: `FlushSignals` queues, `Heartbeat` drains, and firing at the barrier instead would put a handler's own writes on the far side of the next `ClearChanges`
- [x] `RBXScriptSignal` and `RBXScriptConnection` as real types — `:Connect` returns a connection with `:Disconnect` and `.Connected`, and `:Once` besides. `script/src/Signals.cpp` holds the ordering rules **shared by both VMs**, because ordering is what a recording depends on: insertion order, a connection made during a fire does not fire in that pass, a disconnection during one takes effect immediately, and a handle is never reused so a double-disconnect kills nothing. No `:Wait()`, and that is the design — a signal fires from inside a handler pump, which is not a barrier
- [x] the instance methods that existed in C++ and had no script spelling: `:IsA`, `:Destroy`, `:Clone`, `:GetChildren`, `:GetDescendants`, `:FindFirstChild`, `:IsDescendantOf`, `:ClearAllChildren`, `:GetPropertyChangedSignal`, and `Instance.new(class, parent)`'s second argument. `workspace` answers `GetChildren` and `FindFirstChild` too, which needed `Store::EachRoot` — the world heads no sibling list, so `EachChild(NULL_ENTITY)` could never have answered it
- [x] **`task`, and the yield rule made real** — `task.wait`, `task.spawn`, `task.defer`, `task.delay`, `task.cancel`. §2 settled the unit and the spelling: seconds in, **ticks underneath**, rounded up so a wait is never shorter than asked; and `wait`, `spawn` and `delay` are refusals that name their replacements rather than absences. A yield is legal now **only when something is scheduled to resume it**, which is a lookup rather than a judgement — v0.5 refused every yield because nothing could
- [x] **the value ↔ bytes codec**. `script/src/Codec.cpp`, and every one of §3's three requirements is a property of the *format* rather than of either binding: keys sorted **by bytes** (not by either language's collation — `strcoll` and UTF-16 code units answer differently and neither may reach the wire), a number always eight bytes so the encoding cannot depend on the value's history, and depth, size and cycles refused rather than truncated. `MessagingService` takes a table now
- [x] `RunService:IsServer()` / `IsClient()` / `IsStudio()` (studio defaults to false), plus **`IsReplica()`, which is not Roblox's and is the more precise question** — `IsServer()` is about the host and a single-player process is a server whose client-side world is still a replica. `Store::SetProperty`'s refusal now names the replica case so a script that did not ask first is told why
- [x] **`Enum` as a property type.** `ecs::EnumTable` holds the named sets, `PropertyType::Enum` and `PropertyDescriptor::EnumName` carry which, and `Store::SetProperty` checks membership — so `part.Material = "Plsatic"` is refused where it was written rather than landing in a component. `Material` is the first, with Roblox's set. The manifest lists the enums and both declaration files emit **branded** aliases, so `Enum.EasingStyle.Linear` does not type-check against a `Material`
- [x] the datatypes a game reaches for once it is past a scene: `Vector2`, `UDim`, `UDim2`, `Rect`, `NumberRange`, `NumberSequence`, `ColorSequence`, `TweenInfo`, and `Random` over `core::Random` — **which is what closes `D00004`**, because a script with a seeded generator is the consumer that item was waiting for. `Region3` and `Ray` are shims over `core::AABB` and `core::Ray`, which the engine already had; adding a second spelling of either would have been the duplicate `AGENTS.md` calls the most expensive kind of debt. `RaycastParams`/`RaycastResult` are **not** done — they want `spatial`'s queries bound, which is a script-to-physics edge this version did not open
- [x] `DateTime` and `tick`/`time`/`elapsedTime`, all over `store.Time()`. **`tick()` returns the tick count and not a Unix timestamp**, which is a deliberate break from Roblox: the name is right for what this engine counts, and a `tick()` that returned real seconds is the single most effective way to make a recording stop replaying. `DateTime.now()` refuses and names `fromSimulated()` and `fromUnixTimestamp(n)`
- [x] `typeof`, `warn`, and the base-library gaps. `typeof` is **not** a bound global — Luau's is a fastcall builtin the VM reaches directly, so the mechanism is the `__type` metafield, which every value metatable now sets. `loadstring`, `getfenv`/`setfenv` and `require` are refusals that say why rather than absences
- [x] **`CollisionGroup`**, and the name-to-layer registry it needed. `spatial::CollisionGroups` — beside `LayerMask`, because that module owns the bits and `scene` deciding a naming scheme would be a component module choosing a physics policy. A **name** crosses and never the index; the setter writes the layer *and* the mask from the matrix, and a group nobody registered is refused rather than silently defaulted
- [x] **`Transparency`'s component half** — a float on `scene::Visual`, bound as a property, distinct from `Visible`. The struct grew by four rather than absorbing `Reserved[3]`, and `scene/tests/Components.cpp` caught that the moment it happened. **The sorted pass is not done** — see below
- [x] **camera from script.** `scene::Camera` is a class now, deriving from `PVInstance`, with `FieldOfView` in degrees over the stored radians. `workspace.CurrentCamera` reads and writes `ActiveCamera`, refuses an instance carrying no `Camera`, and keeps the consumer's aspect ratio across a change
- [x] **`MemoryStoreService` and `DataStoreService`.** A `Ticket` suspends the caller and the barrier's reply resumes it, which is §1's *first* legal source — a coroutine in Luau and a promise in JavaScript, both resolved by the host. Tested against a **real `Universe`** rather than a bare `Store`, because asserting on the script alone would have proved a promise was made and nothing about whether the barrier kept it. That test found two bugs a bare store could not have: `lua_resume` returning `LUA_YIELD` was being read as failure when a resumed script called a second store method, and `emplace` was silently keeping the *stale* thread reference. §5's refusals are values a script can test — `Ok`, `NotFound`, `Conflict`, `OverBudget` — and a replica's write is refused against **both** the bus's `Replica` resource and the store's adopt-only flag, because those are one fact recorded in two places
- [x] **the second consumer for every binding, which is the point of having two VMs**: every item above lands in Luau *and* JavaScript. `JsSurface.cpp` and `JsDatatypes.cpp` are the second half, and **everything about ordering is shared rather than reimplemented** — one `SignalTable`, one `ChangeQueue`, one `TaskQueue`, one codec, so which handler fires first and which tick a wait returns on are answered once for both languages. What differs is what the languages differ about: `a.mul(b)` and `a.Equals(b)` because JavaScript has no operator overloading, `typeOf(v)` because `typeof` is a keyword it cannot rebind, and a **`Promise` rather than a coroutine** — resolved by the host at a barrier, which is the same contract and legal for the same reason. `Random` is asserted to produce the *same sequence* from one seed in both, which is the strongest form the shared-machinery claim takes
- [x] **scripts are instances in a world, loaded from it rather than from a flag.** `Script`, `LocalScript` and `LuaSourceContainer` are classes; `Source` names the file and `Disabled` is a tag, so a disabled script is in another archetype and the run loop never visits one. `Runtime::RunWorldScripts` runs them in **creation order** — one script may build what another expects to find — filtered by the host's role, which is Roblox's own `Script`/`LocalScript` split. Each chunk gets a `script` global naming itself, set on the *thread* rather than the state because `luaL_sandboxthread` gives each chunk its own globals. **The source is a path and not the text**, which is the v0.6 shape: an unbounded string is not trivially copyable, so it could not be a column or cross a bus, and `Visual::Mesh` is a `core::Name` for the same reason. The example scene now creates its own `Script` instance, so the world contains what animates it
- [x] transparency's **sorted pass**. `scene::OrderForDrawing` partitions a draw list into an opaque head and a transparent tail sorted back-to-front from the eye, and the renderer draws the two as two ranges of one buffer through `first_instance` — a second pipeline with blending on and **depth writes off but the depth test on**, so a pane is hidden by a wall in front of it and does not erase the pane behind it. An **index order rather than an in-place sort**, because the consumer holds a span of a view it does not own. A test putting three panes at one distance caught that a stable sort does not fix a tail that was filled backwards, which is what makes "equal distances keep world order" true and a recording of coincident faces replay. `Visible` and `Transparency == 1` stay different facts
- [x] **`mono.engine/graph` at L9, and the pipeline it describes.** The module is `shared`, which is load-bearing: a headless host publishing a view culls it with the same arithmetic. **The split is what makes any of it testable** — a frustum test is six dot products and can be asserted against, a draw call needs a GPU and can only be looked at. `Frustum` extracts six planes from the view-projection matrix rather than from a camera's fields, so it cannot disagree with what was actually projected; `Cull` produces an index list that composes with the ordering; `FitDirectionalLight` frames the shadow map, fitted to the scene's bounding **sphere** so the map does not resize as the sun turns and make its own edges crawl; `Pipeline` is the stage list with the one check that matters at five passes — a stage reading a target nothing earlier wrote, which on a GPU is a frame lit by whatever was in that memory rather than a crash. `RENDER_PIPELINE.md` is not in this repository — see the note at the top of this file — so "stages 1 to 7" could not be followed by number and the line's own text was built to instead
- [x] **shadows.** A depth-only pass from the light, over the **whole** draw list rather than the culled one — a caster off screen still shadows into it, and culling to the eye is the classic version of that bug. Front faces are culled into the map so self-shadowing acne lands behind the surface that would show it, and the fragment bias is **slope-scaled**: a constant one large enough to stop an edge-on surface shadowing itself is large enough to detach every shadow from its caster. Four taps in a rotated square, which is a soft edge rather than a filter — the difference between four and one is most of what a viewer notices and the difference between four and sixteen is not. Only the opaque range casts, because a transparent pane writing full depth would cast a solid shadow
- [x] **surface camera.** `scene::SurfaceCamera` on a camera entity makes it render into a texture instead of onto the screen; `Visual::Surface` on a part names which one it shows. **`world::ViewChannel`'s shape with a texture on the far end.** The surface pass runs before the screen pass, so what a mirror shows is *this* frame. **The two ping-ponged textures buy legality and not recursion, and this line claimed both until the claim produced a visible bug**: binding a render target as its own sampler is undefined behaviour, and writing one while binding the other fixes that — but a mirror inside a mirror never happened, because surface instances are partitioned out of the surface pass and so no mirror is ever drawn into a mirror's texture. What the recursion story did produce was `Flags.z` being set for the whole surface pass rather than for the mirrors in it, so the *floor* sampled the previous frame's reflection and showed the clear colour wherever that projection landed on untouched texels — a black wedge in the pane, found by eye. Real recursion needs the pass to exclude only the surface it is rendering for, which is a per-view exclusion this pipeline has no shape for and the render-node system's job. The projection is **planar** — right for a flat pane, wrong for anything curved — and the narrowness is the design rather than a shortcut: a general reflection needs a cube map or a screen-space trace and neither belongs in a pipeline this size. The surface pass renders the whole scene minus the surfaces themselves, because a mirror sits between its own reflection camera and the world and would otherwise fill its texture with itself
- [x] **`mono.engine/examples/Mirrors-1-world.luau` and `.ts`** — written, staged and loaded. `Rings` proves the *loading* path; this proves the **rendering** one: a floor for shadows to fall on, twenty-four casters that move so both the shadows and the reflection are legible, a mirror pane with a reflected camera behind it, and a separate camera to watch through — which is the distinction `ActiveCamera` exists for. `engine.examples.scene` asserts what a test can without a GPU: that the scene builds the inputs each pass needs, and that the reflected camera really is the eye mirrored through the plane. On the GPU it runs at **60.2 fps with five draw calls and one surfaced instance**, against three for `Rings`
- [x] **`Demo.hpp`/`Demo.cpp` die here.** `BuildDemoWorld` is deleted and the client has one path: `--script`, defaulting to `Rings.luau`. Keeping the C++ scene beside the Luau one would have been two ways to do one job — and only one of them exercises the bindings. The files are `Scene.hpp`/`Scene.cpp` now, holding what is genuinely the client's half: `DrawList`, `CollectInstances`, `MoveCamera` and the loader. `Spin` and `Orbit` went with it, because a scripted scene writes `CFrame` directly
- [x] **`RaycastParams` and `RaycastResult`**, as `workspace:Raycast(origin, direction, params)` in both languages. Against **`physics::Raycast` and the exact shapes**, not `spatial::Raycast` and the proxy boxes — a ray reporting the bounding box of a rotated part would be plausible and wrong, which is the failure `physics/Query.hpp` says that function exists for. The direction carries the distance, as Roblox's does, and the unit split happens at the binding rather than being lost. A miss is **nil, not a result with a flag**, for the reason `core::RayHit` carries no validity field. `RaycastParams` filters on a **collision group** rather than on `FilterDescendantsInstances`: the engine has a bit test where Roblox has a per-ray walk of a subtree, and the omission is stated rather than silent. Opens the one edge from `script` to a simulation module, and it is one-way
- [x] Flamegraph `BUSY` and `IDLE` work separation.
- [x] Line count tooling for C++ and Header files.

Deferred:
- D00017 — **new, and older than every version that missed it.** The hosting half of L12 has carried a `TODO(v0.2+)` in `mono.engine/CMakeLists.txt` since v0.0, naming a version three releases back. It has no caller — `--worlds N` is a harness, not a hosting layer — so it is filed against a trigger rather than re-versioned, and the marker now reads `TODO(D00017)` the way `net::Handshake`'s already reads `TODO(D00006)`
- D00016 — **new, filed by this version rather than found by a later one, and then closed far enough to matter.** A frame is described twice: `Renderer::Render` submits five passes by name and `graph::StandardPipeline` is the same five as data. `render::PassOrder` now names the renderer's half and `tests/Passes.cpp` compares the two in order, so the sentence in `mono.engine/render/AGENTS.md` became a check — **proved by mutation**, a sixth stage added to the pipeline alone fails with the count and the entry number. `PassRecorder` checks the submission order at runtime, which is the half no headless test can see. What is left open is that the frame still does not *run* from the list, which is the render-node system and stays v0.8's
- D00002 — **the graph renderer, half-moved.** `mono.engine/graph/` exists at L9 with the *description* of a frame — `Frustum`, `Cull`, `Shadow`, `Pipeline`, `Validate` — and none of the execution: no nodes, no capabilities, no cache, no compiler, no executor. `render` calls into it as functions. Three of the entry's four bullets were false by the end of this version and are corrected in place; its trigger is stated for the first time and is v0.8's extended pipeline, which is the first line that cannot be met by making the function longer
- D00004 — **the old trigger fired and the narrowed one turned out to be phrased wrong.** The demo died here, so "does anything still need `core::Random`" could be asked, and the answer is yes: `Random.new(seed)` is bound into both VMs, which is the first *userland* consumer. Separately, `mono.tools/bindings` is the shape the narrowed trigger named — links `core`, neither `net` nor `assets` — and it pays **zero CryptoPP symbols**, because linking `core` was never the thing that costs; *calling* `Random` is. Trigger re-phrased against the call graph rather than the link line
- D00001 — **`Vector2` shipped**, which the entry had recorded as considered and refused on the grounds that only an overlay or an editor would want it. `UDim2` and `Rect` are made of it. The `AABB` half closed exactly as predicted, in `graph::Cull`. macOS SPIR-V→MSL is now the entry's only open bullet and still has no trigger, because nobody has a Mac
- D00015, D00014, D00008, D00007, D00006, D00005 — untouched, and none of their triggers fired. Nothing in this version listens on a socket, hosts a server in its own process, fires a hitscan weapon, or adds a second contributor

### v0.7

**Where this version stands.** The editor is built, drives all three programs off one game file, and has had several rounds of work since the items below were first written — a render gate, a keybind table, a world lifecycle, the shadow half of the draw list and the recording contract, all recorded here rather than left in the commits. **One item is open and it is not the editor being unfinished**: porting Roblox places waits on a class tree that does not exist yet, and is a trigger rather than a task. Everything else is closed, including the two that were left standing longest — the frame of input latency, which was an ordering problem rather than a setting, and the server-view/client-view split, which needed `RunMode::Play` to grow the client half it had been claiming to have.

- [x] **`mono.studio`, and `RunService:IsStudio()` is true in it and in nothing else.** `script/Runtime.hpp` has carried that sentence since v0.6 with the default set to `false`, on the grounds that editor-only behaviour must never appear in a shipped game because a default was optimistic. `Editor::BeginRun` is the one place in the repository that sets it, and the three programs disagree the way they should: the studio reports `studio = true, server = true`, `server --game` reports `studio = false`, and `client --game` reports `studio = false` while being both halves. The `game -> universe, workspace -> the world this script runs on` mapping needed no new code at all — `Services.cpp` established it at v0.6 and the explorer draws exactly that tree, so what an author clicks and what a script reaches through `game` are one object rather than two models kept in step
- [x] **the game file, and it is a different format from a snapshot on purpose.** `mono.engine/game` at L10 `shared`: a `<Game>` holds a `<Universe>` and one `<World>` per subworld, each with its instance tree and its script text. `world::Universe::Save` already writes a binary snapshot and keeps working — that is a *running* universe frozen mid-tick, same-build only, and restoring one into another build was never promised. A game file is authored content: it has to survive an engine version, read in a diff, merge between two people and be repairable by hand. **Nothing in it writes an entity id**; an instance carries a document-local `id` used only to resolve references inside the same file, which is rule 4 arriving at a save format. **Properties equal to their class default are not written**, so a scene of a thousand parts is a file somebody can read the diff of — and the consequence is stated rather than hidden: a file loaded by a later build picks up that build's defaults for anything it did not name, which is the behaviour you want and the choice Roblox made
- [x] **the format is written against `ecs::Classes` and has no schema of its own.** The properties a document holds are the ones a class declares, so adding a property to a class adds it to the save file with nothing in `game` changing. Two names are special-cased and both are structural — `Name` is an attribute and `Parent` is the nesting — and both live in one function. That is the same property the properties panel has, for the same reason, and it is why `mono.studio` never mentions `Part`
- [x] **safe XML, and the subset is the feature.** No DTD, so no billion laughs and no quadratic blowup; no external entities, so no XXE; no processing instructions, no namespaces, no schema. What is left is elements, attributes, text, CDATA and the five predefined entities, which is everything a save file needs and nothing an exploit does. A refusal is **counted apart from a parse error** for the reason `assets::Grant` counts forged tokens apart from expired ones — a document that tried to declare an entity is somebody trying something, and burying it in "malformed" means nobody sees it. `mono.engine/game/tests/Xml.cpp` is mostly negative cases and every one of them is a named attack. The `]]>` split in the writer is the other half: a program containing that sequence is ordinary code, and a writer that emitted it raw would drop the rest of the script into the document as markup
- [x] **script text lives in the world.** `script/Instances.hpp` predicted this field and named this version: `Source::Path` is a `core::Name` because a component must be trivially copyable to be a column, so a game file carrying paths would be a game file that does not contain the game. `script::SourceCache` is a world resource keyed by that same path and consulted **before** the filesystem, which is the arrangement `scene::SurfaceTable` already uses. `ReadSource` is the one function that knows both, so a studio's unsaved edit is what runs — a second resolver would be a second place to forget the cache, and the symptom would be edited code that runs from one entry point and not another
- [x] **explorer and properties, both driven by reflection and neither naming a class.** The explorer draws universe → worlds → instances with selection, drag-and-drop reparenting, insert, duplicate, delete and a fuzzy filter. The properties panel walks `ClassInfo::Properties` and groups by **which class in the ancestry first declares each one** — Instance / PVInstance / BasePart / Part — which is both the right grouping and impossible to forget to update, because Roblox's hand-maintained category field does not exist here and adding one would be a second declaration of what the class tree already knows. An `Enum` property is a combo over `EnumTable::MembersOf`, so `Material = "Plsatic"` is impossible rather than caught
- [x] **the user interface, and Dear ImGui was vendored for this since v0.0.** `mono.engine/ui` at L12 `client` is the only target in the repository that links it. It is **not** in `render`, and the edge runs one way: `ui` implements `render::FrameOverlayHook` — two virtual calls and an opaque handle — so a shipped client links a renderer that knows an interface layer *could* exist and pays nothing for the one it does not have. The question "do we remove imgui now we are on Vulkan" had a short answer: this renderer is not on Vulkan, it is on **SDL's GPU API**, and `MonoVendor.cmake` has been building `imgui_impl_sdl3.cpp` and `imgui_impl_sdlgpu3.cpp` — the exact backend pair — since v0.0 with nothing linking them. **The hand-rolled `OverlayImage` panels stay**: `Overlay.hpp` says they have to work when the renderer is the thing being debugged, and an imgui panel is drawn by the pipeline it is reporting on
- [x] **`render::Viewport`, so the world is projected for the rectangle it lands in.** A studio puts panels down the sides, so the world occupies a rectangle that is neither the window's size nor its shape. Drawing full-window and letting the panels cover the edges would project for an aspect ratio nothing is displayed at, changing every time somebody drags a splitter. The rectangle sets the viewport, the scissor, **the projection and the cull frustum**, and those last two travelling together is the point — one aspect resolved once, because two copies is two chances to disagree and the symptom is geometry popping at the screen edge on one machine and not another. A sixth pass, `interface`, joins `render::PassOrder` and `graph::StandardPipeline` together, because `tests/Passes.cpp` compares the two and D00016 exists to keep them comparable
- [x] **the script editor**, as tabs over `script::SourceCache` with a save that files a path for a script that has none. Two imgui lessons are written into it in comments rather than left to be relearned: a widget's id comes from its label, so appending a modified marker made the tab a *different tab* and dropped keyboard focus after exactly one character; and `io.WantTextInput` is set during a frame, so the menu bar's Ctrl+S — drawn first — always saw `false` and fired alongside the editor's. Deferred and named: no syntax highlighting and no gutter, because a monospace font is a file this repository does not have and a licence somebody has to choose
- [x] **run (server-only) and play (server+client), with the restore people forget.** Pressing Play takes a snapshot, runs the game, and pressing Stop *restores it* — so an author's scene is not whatever their scripts left behind. `Universe::Save` and `Load` are exactly that operation and had existed since v0.2 with no caller that needed them; this is the first. **Script buffers are flushed into their worlds before the snapshot is taken**, and getting that order wrong deletes whatever the author had just typed — which is what it did, once. Trying to snapshot also found `client::DrawList` had never been registered at all: `SetResource` was minting it under the compiler's spelling of the type, rule 4's exact failure, invisible because nothing had ever saved a world that had one
- [x] **one game file, three programs.** `studio` writes it, `server --game FILE.agame` hosts every world in it and serves the first, and `client --game FILE.agame` plays it single-player with `HostRole::OfBoth`. What "running a game" means is one function — `game::StartWorldScripts` — because three copies would be three places for the heartbeat's delta to become wall time. **`mono.client` gained `Engine::game` and not `Mono::server`**: playing a game file needs the format and a VM, not a hosted server, so the escape `mono.client/CMakeLists.txt` reserves is *still* not declared. `mono.studio` is the second declared user of D00008's escape and the first that is a product rather than a diagnostic, and `expected_graph.json` is where both facts are visible
- [x] **importing and exporting one world**, as a `.aworld` beside the `.agame`. Different root elements and different extensions, and each reader refuses the other document rather than half-reading it — opening a game as a world gets you an empty world, which is a lost afternoon. `ImportWorld` takes a rename because importing the same world twice is a real thing an author does and two worlds cannot share a name
- [_] porting roblox games (DEFER THIS UNTIL LATER ONCE TYPES ARE BUILT UP) — untouched, and the trigger is unchanged: there are four instance classes in this engine and a Roblox place names hundreds

- [x] **a viewport panel, and the reason the obvious version does not work.** The world is rendered into a `render::SceneTarget` and the panel shows it with `ImGui::Image`. The cheap alternative — draw to the swapchain and put a background-less imgui window over it — works until that window is *docked*, which is the only way anybody uses it: `imgui.cpp`'s `central_node_hole` is only punched while the central node is **empty**, so docking a panel into it makes the dockspace fill its whole rectangle with `ImGuiCol_WindowBg` and paint over the frame. It was built the cheap way first and the bug was reported as "when viewport is docked, doesn't render", which is exactly what it was. imgui records its draw lists before the renderer runs, so the panel binds whatever texture exists at that moment — but the world pass and the interface pass share one command buffer with the world first, so a texture that keeps its **identity** across the frame is written before it is sampled. That identity is the whole game, and getting it wrong is what "the viewport lags while resizing" was: a target allocated to the panel's exact size is a *new* texture on every frame of a drag, and the bind imgui already recorded then points at the outgoing one — so the panel spent the drag showing a stale frame stretched to a shape it was never drawn for, at a flat 16.7 ms throughout. No profiler can see that; it costs nothing. Scene targets are allocated in **64-pixel blocks** with a 2x hysteresis before shrinking, so the texture survives a drag, the pass confines the world to its corner with `SDL_SetGPUViewport` — the renderer had never set one, relying on the default covering the whole target — and `SceneTextureExtent` hands the panel the UVs to sample only what was drawn. `render::Viewport` was added and deleted inside this version; it was the wrong primitive
- [x] **the studio's latency, which is not its frame rate.** Measured with `--profile-snapshot`: ~0.8 ms of CPU work in a 16.67 ms frame, and `--uncapped` runs the same scene at **0.212 ms p50**, so the editor was never CPU-bound and resizing never cost a millisecond. What it cost was frames of *delay*, from two places. SDL's GPU device defaults to **2 frames in flight** — its own header says higher values "increase throughput at the expense of visual latency" — and nothing had ever changed it, so the picture was up to two frames behind the mouse; the device now asks for 1. `--uncapped` joins the studio from the client, off by default, because a viewport spinning as fast as the GPU allows is a laptop with its fans up for a still picture
- [x] **the frame of input latency, and it was an ordering problem exactly as this line predicted.** `Editor::Run` pumped events and *then* blocked the better part of a frame inside `Renderer::Render` waiting on the swapchain, so every frame was built from input that was already a frame old — and no amount of speed between the two would have closed it, which is why the 0.8 ms measurement was consistent with a visibly laggy editor. `Renderer::WaitForFrame` is the wait, called first; `Render` records into the frame it already claimed. **The split is what makes it possible at all**: a swapchain image cannot be acquired without a command buffer and the wait *is* the acquisition, so there is no ordering inside one call that puts it after the caller's input. **The signature did not have to change** — the prediction here was that it would. `Render` claims the frame itself when nobody claimed it first, and the claim is idempotent within a frame, so an existing caller is correct without knowing this exists and two acquisitions in one frame are impossible. `mono.client` calls it too, because it had the same shape and the same frame of delay
- [x] **a frame that is waited for and never drawn is submitted rather than leaked.** Quitting during the event pump is the case, and SDL documents cancelling a command buffer *after* a swapchain texture has been acquired as an error — so `Shutdown` submits an empty frame instead. The loop's own shape makes it unreachable, which is precisely why it is handled: "unreachable" and "guaranteed" are different words, and a swapchain image held past the device's destruction is a crash inside the backend rather than an error anybody can read
- [x] **a Worlds panel — the scene manager.** Every world with its state and its instance count, a click to choose which one is being edited, and New / Duplicate / Rename / Export / Remove. Duplicate and rename go through the save format rather than through a new mechanism: `game::WriteWorldDocument` and `ReadWorldDocument` are `ExportWorld` and `ImportWorld` with the file taken out, so an editor action and a file on disk cannot disagree about what a world is. **Rename is refused while a game is running and `world::Universe` still has no rename**, because a world's name is what a bus envelope, a subscription and a teleport carry — renaming a live world strands its traffic and nothing would report it. In Edit mode there is none, and `Universe::Adopt` reuses the hole a destroyed world leaves, so the scene keeps its handle and its place in the list
- [x] **every panel is a panel.** Dockable, tear-off, resizable, closable, with a View menu to bring back what was closed and a Reset Layout for when it has gone wrong. The dockspace id carries a version, because imgui owns the layout once it has written its ini — which is right, and means a panel added later is a window the saved layout has never heard of and opens floating in a corner. Both new panels did exactly that before the version existed
- [x] **headless rendering, and it is a device with no window rather than a hidden one.** `Renderer::Initialise(nullptr)` claims nothing: no swapchain, nothing presented, no overlay and no interface pass — and the world still drawn, into the scene target. A hidden window still owns a swapchain, and whether one can be acquired for a window nobody can see is a per-platform answer nobody should have to know. The colour format then has two answers, so it gained exactly one asker — `Impl::ColourFormat` — because a second call to `SDL_GetGPUSwapchainTextureFormat` is a pipeline built for one target and bound to another
- [x] **`studio --headless --frames N --run play --capture PATH`**, which is the editor driven by something that is not a person. `Renderer::RequestSceneCapture` downloads the scene texture through a fence and writes a BMP. `just studio-smoke` loads a game, starts it, renders it and writes the result with **no display at all** — which is how the run cycle above was verified while the machine's screen was busy. Deliberately not part of `just check`: it needs a GPU, and a build container without one would fail a check about the editor for a reason that is not about the editor
- [x] **two scenes in two viewports, and the default template is what makes it the normal case.** `Editor::NewGame` opens *two* worlds — `SkyGrid` running `SkyGrid.luau` and `Mirrors` running `Mirrors-1-world.luau` — with a viewport pinned to each, the tree expanded and the Worlds panel in front. A one-world template was teaching the single-scene habit the rest of this engine is built against: the universe holds subworlds, `ExecutionMode::WorldParallel` ticks them alongside each other, and a viewport per world is what makes that visible rather than asserted. The two scenes are opposites on purpose — the skygrid is sparse geometry over empty sky, so most of the frustum is background and the cull has something to reject (`174 culled` of 196 blocks on the frame this was measured), while the mirror is a surface camera and a texture sampled back over a floor. **A viewport with no world of its own follows the active one**, so pinning is what stops two panels showing one scene twice; `ViewportState::World` is that pin. `SkyGrid.luau` is new and is the first example written for the *renderer's* sparse case rather than for a feature
- [x] **the Server-view and the Client-view, and `RunMode::Play` stopped being one half wearing both hats.** Play has claimed since it was written to be "both halves in one process, the arrangement `HostRole::OfBoth` describes" — and it ran one world with `IsServer()` and `IsClient()` both true and no replica anywhere, so nothing a client would actually *see* was ever produced. `studio::PlayLink` is the other half: an `Authority` over the world being played and a `Replica` of it in a second world, **with nothing in between them** — no datagram, no header, no cipher, no window, which is `mono.unified_server_client`'s arrangement reused rather than a second way to stand two halves up in one process. A viewport is pinned to it when Play starts, so the two are side by side by default; the Worlds panel marks it and carries the numbers a picture cannot give — entities each side, the last complete tick, bytes and the largest message. **A second world rather than a second view of one, and that is the whole design**: two views of one store show the same rows twice, and the only thing worth looking at here is where the two *differ* — a value that never crossed because nothing marked it dirty, a creation the client has not been told about
- [x] **and the ordering that makes it work is the opposite of a server's, which cost a failing test to find.** `mono.server` publishes straight after ticking and loses nothing, because on a server every write happens inside a system. **In a studio they do not** — dragging a part, typing into the properties panel, deleting an instance are all writes *between* two ticks, and a world clears its change bits at the start of one. Publishing after the tick therefore drops every edit an author makes, silently, and the symptom is the server view moving while the client view sits still: replication looking broken when the mistake is a line's position. The studio publishes **before** the tick, which catches the previous tick's system writes and everything done to the world since, and the tick then clears exactly what was sent
- [x] **a game file does not contain a replica, and the check for it is asked of the store.** `game::WriteGame` walked every world in the universe, which was right until a universe could hold a world that is a *view* of a game rather than part of one — the file would have carried a second copy of the scene under a second name, and loading it would give an author two scenes where they had made one. It skips a world whose store is `AdoptOnly`, which is already set on every replica for exactly this reason: the rows are not this process's to mint. Asked of the store rather than passed in by each caller, because a flag threaded through three programs is the same fact recorded three times — and `mono.client --game` gets it right for free
- [x] **`studio.playlink` — six cases, and the one that matters asserts the rows are on the other side.** The client half is deliberately the part of Play a headless test can reach: what a viewport shows needs a GPU, whether there is anything correct *to* show does not. It caught the publish ordering above, and it caught a claim in its own header — `Applied == 0` was documented as "the join never finished", and a run publishes before its world has ticked, so a correct join of an unchanging world leaves it at zero for ever. Positions are asserted against the wire's **documented** quantisation bound rather than for equality: `D00015` (a) puts a position on a fixed-point grid, so 1.0 arrives as 1.000030518, and a tolerance fitted to what a run happened to produce would stop being a check the moment the grid moved
- [x] **worlds open and close themselves, and "is anybody in it" had to be given a meaning before any of it could be written.** There is no player instance in this program, so occupancy is an editor-owned token — `PlayerWorld` — moved by the Worlds panel's `Teleport Player Here`. A world with nobody in it goes to `Suspended` after `IdleCloseSeconds`, defaulting to **300 s, which is Roblox's own place-shutdown grace** and long enough that an author who walked off to build in another subarea does not come back to a stopped clock; `AutoManageWorlds` turns the policy off, because a world that keeps closing under somebody debugging it is the one case whose right answer is a switch. Four rules are load-bearing and each was arrived at rather than assumed. **The wake condition is the destination's inbox, not the teleport call** — `TeleportPlayer` resumes a suspended world before addressing it, but a script's own teleport goes through the bus, and a suspended world is the one world whose inbox nothing drains, so anything sitting in it is by construction traffic waiting on a closed world. A running world replaces its inbox every barrier and a poll would race the tick; a suspended one holds what arrived, which is what makes this reliable rather than lucky. **Being looked at is activity, in either viewport** — and forgetting the second one was a real bug: with two panels open, the world that was not the active scene was closed under the panel showing it, which reads exactly like the second viewport being broken. **A scoped run's suspended worlds are not the lifecycle's to wake**, because a world outside the run scope is empty *because* somebody stopped it, and an idle timer restarting it is the editor arguing with the play button. And **never the last world**, since a universe with everything suspended is a game that has stopped without saying so. None of it runs in Edit mode, where no world ticks and suspending one only leaves a state the author has to put back by hand
- [x] **the simulation runs parallel and the drawing is sequential, and that is decided rather than defaulted.** `Universe::Tick` has run worlds under `ExecutionMode::WorldParallel` since v0.2; what was missing was anything that *showed* it, which the two-world template now is — `NewGame` sets the mode explicitly rather than inheriting it, because a universe that had loaded a game setting something else would keep that setting and the template would quietly stop demonstrating the thing it exists to demonstrate. **Drawing stays sequential: one `Renderer::Render` per viewport, one after another.** Parallel recording buys nothing here — the passes share one command buffer and one device, so the work would serialise at submit — and it would cost the thing that makes the viewport correct, which is that the world pass writes a scene target before the interface pass samples it in the same buffer. Revisit when a viewport is expensive enough that its *record* is measurable; at the sizes measured it is not
- [x] **the tree decides what renders, and before this nothing did.** `mono.engine/scene/Visibility.hpp`, and the hole it closes is embarrassing in the way only load-bearing defaults are: every query that built a draw list matched `<Transform, PreviousTransform, Bounds, Visual>` — a *component* test — so a part was drawn because of what it was made of and never because of where it sat. A template under `StarterGui`, a part in `ReplicatedStorage`, an orphan a script had created and not yet parented: all complete parts by that test, all on screen. **`Visual::Visible` was declared, bound, saved and reloaded, and no draw path read it either.** Roblox's rule is restored — a `BasePart` renders exactly when it descends from `Workspace`, and everything else in the tree is storage. It is a **tag component and not a branch**, for two reasons that agree: `client::CollectInstances` runs `EachBatchParallel` and writes `out[first + row]`, so a `continue` would leave holes and make the returned count a lie, and compacting instead wants a parallel prefix sum to avoid work the storage gives away free — and it is the ECS's own argument, the one `RigidBody` already makes, that you componentise what you iterate. **Derived by a scan and not maintained at the write**, because ancestry is not local: parenting one model moves everything beneath it, and the alternative is hooking `SetParent`, the explorer's drag, the game loader, `InstallServices` and `DestroyInstance` — where missing one gives a part that draws after being deleted from the scene, with the symptom appearing in the renderer a very long way from the reparent that caused it. Mark-and-sweep over a byte the sweep is already touching, rather than a `std::set` allocated and thrown away every tick, and **a mark rather than a monotonic stamp because the component reaches a snapshot**. It runs in `PreRender` rather than `PostSimulation`, which is the choice with a trap on the other side of it: **a world can present without ticking** — the studio suspends a world and edits it — so a gate maintained by the simulation would leave a part dragged into `Workspace` invisible until somebody pressed play. That makes it the one structural change in `PreRender`, which is safe only because nothing in the simulation reads `Rendered` and every host derives it identically from the same tree, so `just determinism` still compares equal. **A replica does not use it at all**: ancestry is the one thing the wire does not carry — `Hierarchy` holds `Entity` handles that mean nothing across two directories — and it does not need to, because the authority has already applied the filter, so `client::CollectReplicated` honours `Visual::Visible` directly, which is the half that does arrive
- [x] **`Visual::CastShadow`, and the draw-list arithmetic moved to where a test can reach it.** A part can be excluded from the shadow pass without being excluded from the frame — the property is bound in both languages, in the manifest and both declaration files, and it crosses a save. The interesting half is `scene::ScenePlan` and `OrderScene`: the renderer used to compute its own mirror partition and caster partition inline, and **those counts are `first_instance` and a draw count — the part of a pass easiest to get wrong by one and impossible to see in a screenshot.** A shadow range short by one loses a caster somewhere off screen and the frame looks fine. It is arithmetic over a `shared` type, so it belongs beside `OrderForDrawing` where a headless suite can assert on it, and the renderer is left with the part that genuinely needs a GPU. **The shadow pass draws two ranges rather than one, and that is forced rather than chosen**: the surface pass needs the non-mirrors contiguous from zero and the shadow pass needs the casters contiguous, one partition cannot give both, so the casters are partitioned *within* each run — the mirror half being zero in every scene with no mirror in it. Every partition is stable, for this file's standing reason: an opaque scene must come out exactly as it went in or a recording stops replaying
- [x] **two viewports were thrashing the driver's allocator, and it did not stay inside this process.** One shared depth texture is correct for one viewport and catastrophic for two: the panels are different sizes, so each `Render` found the other's dimensions and reallocated — a colour-sized D32 texture destroyed and created **twice per frame, every frame**. That is what "resizing makes my whole desktop lag" was, and it is not a frame-rate symptom, which is why nothing in `--profile-snapshot` had shown it. Each scene slot owns its depth buffer now, sized to the block-rounded allocation rather than the drawn rectangle because SDL requires colour and depth dimensions to match, and released outright rather than retired — nothing samples a depth buffer, so no recorded interface bind can be pointing at it and the grace period the colour target needs does not apply. **The depth format is chosen at start-up instead of hard-coded**, which was the second bug in the same file: `SDL_gpu.h` says outright that only D16_UNORM is guaranteed and to check which of D24/D32 exists, and D32_FLOAT was written in four places. On the first GPU without it the texture creation fails, the frame is dropped and nothing says why — a black window. Asked once, for both usages at once, because the shadow map is sampled as well as targeted and a second format would be a second thing to keep in step with a pipeline
- [x] **one table naming every key in the editor, and rebinding is why it exists rather than a `switch`.** `input::Actions` opens with "the one table in the engine that names a key; everything else asks for an action" and the editor had drifted from it — F5 was spelled out in `DrawShortcuts`, again in the Run menu's label and a third time in a comment. The editor cannot use that table: it is SDL keycodes through a polled `ActionState`, and the editor's keys are read through imgui, which owns the keyboard while a text field has focus and must go on owning it. So it is the same idea over `ImGuiKey`, deliberately a second table, with a Keybinds page that searches, rebinds, reports the conflict it is about to take and resets. **The part that could not have been left out is `Scope`** — Global, Viewport, Tree, Script — because without it the only available guard is `io.WantCaptureKeyboard`, which answers "is a text field focused" and not "does this key mean anything here": Delete belongs to the tree and the viewport and is a character in the script editor, so a binding on a plain letter could never have been added safely at all. `ShowStatistics` and `ShowFrameGraph` ship **unbound** rather than moved to a different pair of function keys, because F5 is Play in an editor and always will be, and inventing a second set is a decision for whoever wants them — which is what the page is for
- [x] **and now it is enforced rather than recorded.** The decision above was a paragraph in this file and nothing else: a second viewport recording from a worker would have produced a driver validation error or a frame of somebody else's geometry, neither of which points back at the thread that caused it. `Renderer::Render` and `Shutdown` abort unless they are on the thread that called `Initialise`, which is `ecs::Store::RequireOwningThread`'s shape and its argument — by the time a second thread is inside, it has already recorded into a buffer another thread is filling, so there is nothing left to decline and the stack at the violation is the whole value. **The owner is claimed by the constructor and re-claimed by `Initialise`, and that is what makes it testable**: `engine.render.passes` exercises the contract with no GPU, on a build machine that has none. Deliberately **no `BindToCallingThread`** beside the store's — a store is picked up by a different worker every tick and a device is not, so a public rebind would be a seam for exactly what this forbids. `render/AGENTS.md` says it too now, and says it as a contract rather than as a note
- [x] **the sixth pass had a half-updated check behind it, found while writing the one above.** `tests/Passes.cpp` compares `render::PassOrder` against `graph::StandardPipeline` by count and by order, and that comparison did hold when `interface` arrived — it is generic. The *names* test beside it is not: it spells out five passes by hand and `interface` was never added, so it was the one pass whose name nothing checked. The file's own header had gone stale the same way, still describing "five passes". Both fixed, and the count is now deliberately not written down in that file — it said "five" until v0.7 made it false, which is the failure the file exists to prevent happening one level up inside it

Found while building this, and fixed here rather than filed:

- **`scene::Visual` had a hand-written serialiser that two later fields never reached.** `Transparency` and `Surface` were added after `WriteVisuals` was written and neither crossed a snapshot, so a glass pane turned solid the first time a world was saved and reopened. Invisible for an opaque part, which is why it survived three versions. A field added to a type with a *generated* serialiser crosses for free; one added to a hand-written pair crosses only if somebody remembers, and nothing in the build checks — `mono.client/tests/Presentation.cpp` is the cheapest test for that whole class of bug and now covers it
- **WASD stopped working the moment you clicked on the picture, and the guard that killed it was the keyboard twin of a bug this same function had already fixed for the mouse.** The camera's fly keys were gated on `!io.WantCaptureKeyboard`, with a comment claiming imgui raises that "while any text field has focus". It does not — `imgui.cpp` raises it while *navigation* is active: `io.NavActive && (ConfigFlags & NavEnableKeyboard) && io.ConfigNavCaptureKeyboard`. `ui::Interface` sets `NavEnableKeyboard` and the second defaults to true, so **focusing a viewport set the very flag that switched its camera off**. Twelve lines above it sits the note about `hovered && !io.WantCaptureMouse` never being satisfiable, because hovering an imgui window is what raises `WantCaptureMouse` — the identical mistake, in the same function, on the other device, and the fix for one did not go looking for the other. The guard is `io.WantTextInput` now, which is the question that was meant and the one `DrawShortcuts` already asks. `F` to frame carried the same guard and was dead in the same circumstances
- **And the camera now follows focus as well as the pointer, which needed two facts where there had been one.** A viewport clicked into and then left by the mouse got nothing, because every gate was `hovered || active`. Routing WASD by `FocusedViewport` alone would have flown the camera while somebody typed into the properties panel — that field deliberately *keeps* naming the last viewport when focus moves elsewhere, so the transport readout does not blank on every click. `FocusedIsViewport` is the second fact, resolved from the same window in the same place, so the two cannot disagree: the mouse follows the pointer, the keyboard follows focus, and neither answers for a panel the keyboard is not in
- **Auto-pinning a client view per Play run was over-reach, and `--run play` is what showed it.** That flag starts every world in the game, so "one panel per run" opened a viewport per *world* — three or four pictures nobody asked for, each taking a slice of the centre pane and a turn in `PresentWorld`'s round robin, so every view also refreshed a third as often. One client view is the feature; the rest are reachable from the scene selector like any other world
- **The replica's draw path dropped the tail of `Visual` — the same silent loss as the serialiser above, arriving through a different door.** `CollectReplicated` copied the first few fields and left `Transparency`, `Surface` and `CastShadow` at their defaults, so a glass pane replicated as solid and a mirror replicated as a plain part. Two hand-written copies of one struct's fields is two places to forget, and neither forgetting is visible on an opaque part — which is why the pair of them survived together
- **`client::DrawList` was unregisterable, and the abort landed in a world that had never heard of it.** `ecs::Components::Of<T>` caches its answer per type per process, so **the first mention of a type anywhere decides its name** — and in the replicated world that first mention was a `SetResource` rather than `RegisterClientComponents`. The world built next then tried to register `client.DrawList` properly and aborted, naming a type its own function never mentions. Rule 4's failure mode exactly: the compiler's spelling wins by arriving first, and the diagnosis surfaces one caller away from the cause. `BuildScriptedWorld` and `InstallPresentation` had both opened with the registration call for this reason; this path was the gap
- **`game:GetService("Workspace")` found nothing, and an orphan claimed to be in the world.** The global is spelled `workspace` and the service lookup went through `lua_getglobal`, so asking for the service by its class name — which is what a Roblox script does — was refused for an object the engine plainly provides. It resolves from the registry now, so the global, `game.Workspace` and `GetService` are **one instance rather than three handles that compare equal**. Separately, a null `Parent` used to hand back `workspace`, which made every orphan look like it was in the scene — the answer is `nil`, and that is where "an orphan is not in the world" starts being true
- **`scene::CapturePreviousTransforms` moved into `scene`.** It was five lines inside `examples::LoadScene` and the studio needed the same five. Two copies of a *system* is worse than two copies of a helper: both can be installed into one world and the second wins silently every tick

Deferred, and named rather than discovered:

- **One VM per world, and it is Luau.** A world whose scripts are JavaScript runs nothing in the studio, the server's game-file path or single-player. `script::LanguageOf` picks per file and the runtime is per world; reconciling those is a design decision rather than an oversight
- **No undo.** Every editor action is applied straight to the store. Undo needs a command log, and a half-undo covering property edits but not deletions would be worse than none
- **No viewport picking, no gizmos, no reference picker.** Selection is the explorer's; a click in the world does nothing, and a `PropertyType::Reference` shows its target's name and cannot be changed
- **Headless runs no interface backends.** The imgui context starts and its platform and renderer backends do not, so panel *drawing* is skipped — a headless run exercises the universe, the scripts, the presentation phase and the render. Driving the panels themselves without a window needs the backends replaced rather than skipped, which is the shape a scripted or agent-driven editor will take
- **A capture is the world and not the editor.** The chrome is drawn onto the swapchain and SDL does not promise that is readable. Capturing the whole window means rendering the frame into an offscreen target and blitting it, which is a change to every program's frame for a benefit only the editor has
- **Replication is still one world per connection.** A game of several scenes simulates all of them and streams the first, which the server says in its log rather than leaving a player to wonder which world they joined. The studio's client view is per *run*, so a game with two scenes playing has two of them — which is more than a real client gets, and is the editor being a diagnostic rather than a preview of what shipping looks like
- **The client view has no loss, no reordering and no latency** beyond the one step the pipeline costs. `mono.unified_server_client --drop` is the harness for those and a socket between two processes is the real thing; what this catches is the class of bug that survives a *perfect* link, which is state that is never sent at all. Adding a fault model here would make it a second network test, and there is already one
- **`scene::SurfaceTable` is not written to a game file.** Materials are registered by scripts, so a loaded world rebuilds its table when its scripts run. The moment the studio lets somebody edit a material by hand that stops being true, and `game` gains a `<Surfaces>` section

### v0.8

**The scripted interface**, which is v0.7's last question answered rather than
deferred. The ask was: should the studio's own UI be authored in TypeScript,
with the Roblox `GuiObject` tree as the widget set and React as an option? The
answer is yes, and **not by writing fifty class registrations first**.

Those classes are shims until three things exist underneath them, and a shim
that nothing draws is an API with no caller — which is the thing this repository
refuses everywhere else. In order:

- [x] **`core` gained the value types the tree is made of.** `UDim`, `UDim2`,
  `Vector2` and `Rect` were already in `core/types` and **none of them was a
  `PropertyType`** — so a component holding a `UDim2` could not be a property,
  could not be saved, could not appear in the properties panel and could not be
  set from a script. `ecs::PropertyType` is a closed list that grows when
  userland gains a value, and this was that. Four cases in `game::Values`, four
  widgets in the properties panel, four in each binding, and the manifest
  regenerated. **The `UDim` widget is two controls rather than a `DragFloat2`**,
  which is the one part that was not mechanical: scale is a fraction and offset
  is pixels, so one drag step cannot serve both — a hundredth of a pixel is a
  drag that never arrives and a whole unit of scale crosses the parent in one
  mouse movement
- [x] **`mono.engine/gui` at L7 `shared` — what a 2D thing *is*.** Thirty-three
  classes as `ecs::Classes` registrations over twenty-three real components,
  exactly as `scene` does for `Part`: `GuiBase` → `GuiBase2d` → `GuiObject` →
  `Frame`, `CanvasGroup`, `GuiButton`(`TextButton`, `ImageButton`),
  `GuiLabel`(`TextLabel`, `ImageLabel`), `ScrollingFrame`, `TextBox`,
  `ViewportFrame`; `LayerCollector` → `ScreenGui`, `BillboardGui`, `SurfaceGui`,
  `PluginGui` → `DockWidgetPluginGui`; and the nine `UIComponent` modifiers.
  `shared`, so a server authors a `ScreenGui` and replicates it. **`core` and
  `ecs` are the whole dependency list**, and the one thing that looked like a
  reason to link `scene` — `NormalId` for a surface gui — is a six-member enum
  registered by name, pinned by a test at each end, which is the arrangement
  `DefaultMaterial`'s duplicated "Plastic" already uses.
  **`Instance`, `Name` and `Parent` moved down into `ecs`** as
  `Classes::RegisterInstanceRoot`: they project components `ecs` owns and were
  only in `scene` because `scene` had the first class tree, and a second tree
  that may not link the first made one declaration necessary rather than
  tidier. **`VideoFrame` is deliberately absent** — there is no decoder, and a
  class that draws nothing forever is what the paragraph below refuses
- [x] **Layout, which is arithmetic and belongs beside the tree.** `UDim2`
  against a parent's absolute rect, `AnchorPoint`, `SizeConstraint`, and the
  `UIListLayout`/`UIGridLayout`/`UIPadding`/`UIAspectRatioConstraint` modifiers,
  plus `UISizeConstraint`, `UITextSizeConstraint`, `UIScale`, `UICorner` and
  `UIStroke`. One pass, parent before child, producing a `Resolved` component
  the renderer reads with a query. **The order the constraints apply in is
  load-bearing and was arrived at rather than assumed**: scale multiplies what
  the `UDim2` said, the aspect ratio reshapes that, and the size limits clamp
  the result — clamping first produces a rectangle that obeys neither, which
  reads as "the constraint does not work". **`Measure` is split from `Place`**
  for one reason: a list or a grid has to know how big a child is before
  deciding where it goes, and that is the only thing a single top-down sweep
  cannot do. `AutomaticSize` is declared, saved and bound and is **not**
  implemented — it needs that second phase over the subtree, and `D00021`
  carries the argument rather than leaving it to be discovered
- [x] **The compiled draw list, which was not on this list and is the thing
  that makes the rest affordable.** `gui::Compiled` computes a rolling
  signature over every field the compile reads and keeps the flattened,
  sorted, clipped `DrawList` while it matches — `scene::QuickHash`'s pattern
  and `studio::HierarchyView`'s, for the same reason both give:
  `ecs::Hierarchy` and `gui::Element` are not observed components, so
  `Store::ChangeVersion` does not move when an element is reparented or
  resized, and it *does* move when a physics tick writes a transform, which
  would rebuild the UI sixty times a second for nothing. **The rule that a
  field added to a component must be added to the fold is checked rather than
  written down**: `gui/tests/Compile.cpp` walks every property the class tree
  declares, writes each one and asserts the signature moved
- [x] **Input routing.** Hit testing back to front, `ZIndex`, `Active`,
  `Selectable`, and `InputBegan`/`InputEnded`/`Activated` plus
  `MouseEnter`/`MouseLeave`/`MouseMoved`. **The hit test is a backwards walk of
  the compiled list rather than a tree walk**, because the list is already in
  paint order — a second traversal that re-derived that order would be a second
  answer to "what is on top", and the two would disagree the first time
  somebody changed a sort in one of them. Two rules inside it were arrived at:
  `InputEnded` goes to where the press *began* rather than where the release
  happened, which is what makes dragging off a button and back one interaction;
  and `MouseLeave` fires before `MouseEnter`, so a handler that moves something
  on leave runs before the one reacting to the arrival
- [x] **`gui` drawn on screen, through the atlas that already exists.**
  `ui::PaintGui` takes a `gui::DrawList` and an `ImDrawList` and nothing else —
  rectangles with rounded corners, outlines, nine-slice, tiling, fit, crop,
  text with both alignment axes, and clipping pushed with *intersection* so a
  panel's own clip survives. The atlas decision the plan flagged came out the
  way it stated: `ui` already owns four faces and a second rasteriser over the
  same four files would be two answers to what a glyph looks like, so the edge
  runs `ui` → `gui` and the painter is the only thing that knows both. The
  studio's viewport panels compile and paint the world's own `ScreenGui` over
  the world image and under the editor's chrome, one `gui::Compiled` and one
  `gui::Router` per panel — per panel because a panel *is* a canvas, and two
  viewports at different sizes resolve every `UDim2` differently
- [x] **`mono.engine/examples/Interface.luau`, and it is the proof rather than
  a demo.** A `ScreenGui` built entirely from a script, so it exercises
  `Instance.new` over the new class tree, `UDim2` and `Vector2` as real
  property types, the layout pass, the compile and the painter in one run. Every
  element in it exists because one specific thing looks correct without it:
  the centred card catches an anchor point applied backwards, the strip's
  content is four times its own width and catches a clip that replaces rather
  than intersects, and the grid's cells catch a wrap that counts cells without
  counting the padding. **Its clock writes at ten hertz rather than sixty**,
  which is both `D00020`'s workaround and what keeps the compiled-list cache
  measurable — a scene that changed every frame would rebuild every frame and
  prove nothing about it. It is installed into the skygrid world by
  `Editor::NewGame`, so a new game shows it
- [x] **`gui` rendering as a render pass at L12, which is the half a shipped
  client needs.** `mono.client` does not link `Engine::ui` and must not —
  `ui/Interface.hpp` gives the reason, and it is the same one that keeps imgui
  out of a game binary — so a shipped client draws no `ScreenGui` until there
  is a batched quad pipeline plus a glyph atlas behind `Pass::Interface`. **The
  seam is already the right one and that is what makes this a renderer rather
  than a feature**: the second backend is a new consumer of `gui::DrawList`,
  not a second compile, so neither half can drift from the other about where an
  element is.

  **The atlas decision is made: `render` owns the four faces.** `ui` owned them
  through imgui, which was right while the editor was the only thing drawing
  text and stops being right the moment a shipped client draws a `ScreenGui`.
  `ui` already depends on `render`, so the edge runs the way it needs to and
  there is one rasteriser rather than two answers to what a glyph looks like.
  **The dependency that seemed to block it was already in the tree.**
  `imstb_truetype.h` and `imstb_rectpack.h` are standalone public-domain single
  headers `mono.vendor/imgui` bundles rather than parts of imgui — a rasteriser
  and a bin packer — so `render::GlyphAtlas` includes them through
  `VENDOR_INCLUDES` and links no imgui symbol. Nothing new was vendored and
  nothing of the editor's toolkit reaches a game binary.
  `GlyphAtlas` is built and tested headlessly, which is why it came first: a
  wrong glyph would otherwise be found by looking at a screen. Coverage rather
  than colour, so one sheet serves every colour of text; grown by powers of two
  until the pack fits rather than sized by a formula that is either wasteful or
  occasionally too small; a pixel of padding checked *in the sheet* rather than
  trusted from the constant, because atlas bleed only shows at non-integer
  scales, which is to say on somebody else's machine. Latin-1 only, and the rest
  is filed rather than pretended at.
  Found while testing it: the font staging gate named `engine_ui` alone, so a
  test of the new rasteriser linked the module that reads .ttf files and got
  none staged — every case took its "no fonts" branch and reported green
  without asserting anything. The gate names both modules now and
  `mono_add_tests` stages them too; the suite went from 8 assertions to 574.

  **`render::InterfaceMesh` is the rest of it that a device is not needed
  for**, and it is a type rather than a loop inside a recording function for
  exactly that reason: quad generation, outline construction, batching and text
  layout are arithmetic, and arithmetic found wrong headlessly is arithmetic not
  found by looking at a screen. It produces vertices, 16-bit indices and the
  runs between scissor changes.
  The decisions inside it. **Batches split on the scissor and on the texture
  and merge otherwise**, because a clip is pipeline state rather than a vertex
  attribute — two elements clipped differently cannot be one draw however alike
  their pixels are. **An outline is four quads and never a line primitive**,
  because a line's width is a device setting with no portable guarantee and one
  that is a pixel on one driver and three on another is a difference nobody
  reproduces. **A solid quad samples a white texel packed into the atlas**, so a
  filled rectangle and a glyph share one pipeline — two would be two places for
  the blend state to be set differently, which shows as panels at subtly the
  wrong opacity and nowhere else. That texel takes a rect from the packer rather
  than a corner somebody picked: `stbrp` fills the whole sheet, so a hand-picked
  texel is one a large glyph can land on, putting a letter's coverage under
  every filled rectangle — a bug that appears only once the atlas is full enough,
  which is to say on the machine with the wider font.
  **The shaders are written and compiling.** `interface.vert` and
  `interface.frag` build to SPIR-V and stage into both the client and studio
  trees. Two decisions in them. Positions arrive in **canvas pixels** and become
  clip space in the vertex shader, so the only thing that knows the target's
  size is the shader — a panel at a different resolution needs no rebuild of the
  mesh. And Y is flipped where X is not, because SDL's GPU clip space is Y-up on
  every backend while a canvas has its origin at the top left: the one asymmetry
  in the file, and the one everybody writes symmetrically the first time.
  The fragment shader is `inColour * sampled` for all three kinds, not a branch.
  The atlas's white texel is (1,1,1,1), so a rectangle multiplies its tint by
  white and gets its tint while a glyph multiplies by (1,1,1,coverage) and gets
  its tint at that coverage — branching on which would be a divergent branch per
  fragment to avoid a multiply by one.
  **`render::InterfacePass` is the recording loop, and it is a
  `FrameOverlayHook` rather than a branch inside `Renderer`.** That is the same
  slot the editor's chrome occupies, so a client hands one of these where the
  studio hands its imgui recorder and the renderer knows the difference not at
  all — adding an interface path to the renderer's own recording would have put
  a `gui` concern inside three thousand lines of pipeline state, where the tier
  check would not have caught the edge and a reviewer would have had to.
  The decisions in it. **Straight alpha rather than premultiplied**, which is
  the opposite of the overlay pipeline beside it and is deliberate:
  `gui::DrawCommand` carries a tint and a transparency as separate numbers, so
  the vertex colour reaching the blend has never had alpha folded into it, and
  premultiplying would mean a per-vertex multiply on the CPU for no gain.
  **No depth and no culling**, because the interface is drawn back to front in
  compile order — a depth test would hide a panel behind a wall, and a quad
  wound the other way is invisible under culling, which looks exactly like an
  element that failed to lay out. **Buffers grow and never shrink**, so a panel
  opened once and closed does not reallocate the frame it opens again. **The
  transfer buffer is cycled**, because it was written last frame and may still
  be in flight — without that the copy overwrites bytes a queued frame has not
  read, drawing last frame's interface at this frame's positions, a tearing that
  only appears under load. The atlas uploads once rather than per frame; it
  changes only when the size it was baked at does, which is a settings change.
  **An unresolved image name falls back to the atlas's white texel**, drawing
  the bounds as a flat tinted rectangle — visible on purpose, for the reason the
  entry below gives.
  **And `mono.client` constructs one**, which is the half that makes the rest
  reachable: a pass nothing hands to the renderer is unreachable code that
  compiles. The client builds it from `Renderer::Backend()` after the renderer
  is up, compiles the rendered world's `ScreenGui` each frame, and passes it as
  the `interfaceHook` — the same slot the studio fills with imgui. A failure to
  initialise is warned and not fatal: a client that refused to start over a
  missing typeface would be worse than one that draws a world with no interface
  over it.
  **`mono.client` still links no imgui**, which was the point of the whole
  entry, and the architecture check pins it.
  **What is not verified is that it draws.** Every piece compiles, every piece
  above it is tested headlessly, and the pass itself needs a device and an eye —
  which is the one check in this version that a test cannot stand in for. Corner
  rounding and the image fit modes travel to that backend rather than being
  tessellated here — a radius belongs in a shader, and nine-slice needs the
  image's pixel size, which is precisely what a content name says this layer
  must not know
- [x] **Texture resolution for `ImageLabel` and `ImageButton`.** The runtime
  format was added in v0.8 and the upload hook was connected in v0.10. The
  client and studio resolve `ui::ImageSource` through the content tables, while
  unresolved content keeps the visible fallback marker. Runtime code reads the
  baked texture format; PNG decoding remains in `bake` and is not carried by a
  shipped game.
- [x] **Connecting `gui::GuiEvent` to `script::Signals`.** The routing produced
  events and nothing turned them into a `.Activated`. **The join is in `script`
  itself, which is "above both" read correctly**: `gui` is L7 and `script` is
  L9, so the edge is legal by tier and it is the *same* edge `script` already
  has to `scene` — a script's vocabulary is the class tree and there are two of
  them, so depending on one and not the other would have put `.Activated` behind
  something higher up, and that something would have been the second path into a
  VM this line refuses. Six `SignalKind`s, both VMs, and one queue on `Runtime`
  holding nothing either VM owns.
  **Queued rather than fired on arrival**, for `Changes.hpp`'s reason one door
  along: the events are produced while a host walks a compiled draw list, and a
  handler that destroys the element it was called about would pull that list out
  from under the walk. They are delivered at the barrier, last within "what the
  previous barrier recorded" and before the tasks, so a click that defers
  belongs to the tick it happened on. **A dead element is skipped rather than
  dispatched to** — a close button destroying its own panel is the ordinary case,
  not an edge one, and the same delivery carries events about what went with it.
  **`Activated`, `InputBegan` and `InputEnded` are called with no arguments**,
  and that is the version of this decision the file's own rule produces: Roblox
  passes an `InputObject`, there is no such datatype here, and a different shape
  invented now would have to change the day one arrives — `VideoFrame`'s trade
  rather than `AutomaticSize`'s. `MouseEnter`/`MouseLeave`/`MouseMoved` get
  `(x, y)` in canvas pixels, which is Roblox's signature exactly.
  **The studio forwards and does not dispatch**, which is what keeps the editor
  out of it: `Overlay.cpp` hands the router's events to the running world's
  runtime and that is all it knows. Found while wiring it: `examples::LoadScene`
  registered the 3D class tree and not the 2D one, so `Interface.luau` — a
  shipped example built entirely from `ScreenGui` and `Frame` — could not load
  through the entry point every program shares. It worked in the editor, because
  `Editor::NewGame` registers them on its own path, which is the shape of gap
  that survives: it works everywhere somebody looked
- [x] **`GuiBase3d` and the adornments.** Eleven classes
  hung off `GuiBase`, which is the split that class was kept for: the 2D
  branch's own comment said `GuiBase3d` "and the adornments hang off `GuiBase`
  when they arrive, and a tree that had flattened the two would have to grow the
  split back at exactly the point somebody is adding a feature". This is that
  point, and `gui/tests/Adornments.cpp` asserts the promise was kept — a
  `SelectionBox` `:IsA` a `GuiBase` and deliberately **not** a `GuiBase2d`.
  **An adornment is a description and `gui` owns one half of it.** Turning
  "outline this, in this colour" into geometry needs the adornee's `CFrame` and
  stud extent, which are `scene::Transform` and `scene::Bounds`; this module
  links neither and the edge is refused. That is not a gap but `D00022`'s split
  arrived at again, word for word: **whoever draws an adornment has both
  operands and this module has one.** So what is here is the tree logic a drawer
  would otherwise each answer differently — `AdorneeOf`, `AdornmentDrawn`,
  `EachAdornment` — and the drawing is the render pass's.
  Two rules inside it were arrived at rather than assumed. **An unset `Adornee`
  means the parent**, which is what makes a `SelectionBox` usable by parenting
  it to the thing it outlines and setting nothing else; and **a dangling one
  does not fall back to the parent**, because quietly re-aiming an adornment at
  whatever it happens to be parented to would draw a box around the wrong
  object — a wrong answer to "what am I about to delete" is worse than no
  answer. Ordering is `ZIndex`, stable, decided here rather than by each drawer,
  because two drawers sorting independently is two answers to what covers what.
  **And the drawing half is `render::AdornmentGeometry`**, which is where
  `D00022` said it would be: this module links both `gui` and `scene`, so it is
  the first place with both operands. It resolves each adornment against its
  adornee's `CFrame` and `Bounds` into world-space lines — twelve edges of a
  box, not six faces, because filling one would hide what it is drawn around,
  which is the one thing a selection box exists to leave visible.
  Three decisions in it. **Corners go through the adornee's own frame** rather
  than being built in world space from a position and an extent, so a rotated
  part gets a rotated box — the axis-aligned version reads as the selection
  being wrong rather than the renderer. **The box is swollen a hair**, because
  edges coplanar with the surface z-fight along every side and a flickering
  selection box reads as a driver fault. **An adornee with no `Transform`
  produces nothing** rather than a box at the origin: a `Folder` can legally be
  an `Adornee`, and outlining it at (0,0,0) looks like a bug in the selection.
  Containment and the adornee fallback are not re-derived here — they are
  `gui::EachAdornment`'s, and a second answer would disagree with the first the
  day one was fixed
- [x] **Containment: where a collector has to live to draw at all.** Roblox's
  rule, and the engine was more permissive than it: a `ScreenGui` drew wherever
  it was found, including parented to a `Part` or to nothing. That let an author
  ship a game whose interface appears in the studio and is missing in the
  client — a difference running the worst way round, because the place it works
  is the place it is checked. A `ScreenGui` now draws from `StarterGui` or a
  player's `PlayerGui`; a `SurfaceGui` or `BillboardGui` from those and from
  `Workspace`, because each hangs off something in the world.
  **Tested by ancestor *name*, which is this module's existing arrangement
  rather than a shortcut**: those three are `scene`'s services and
  `gui/AGENTS.md` refuses the edge, the same refusal that made `gui::Face`
  re-declare `NormalId`'s six members. The names are exposed on both headers and
  pinned against each other from `examples/tests/Scene.cpp` — the lowest place
  both modules are linked — so a rename fails a test instead of making every
  interface silently stop drawing. `scene::AddPlayer` creates the `PlayerGui`,
  because a container each caller has to remember is one somebody will not.
  Found by it immediately: `Interface.luau` never parented its own `ScreenGui`
  and had been drawing on the permissive path since it was written

- [x] **`GuiService`** — and **not** `Path2D` or `GuidRegistryService`, which
  is this version's own rule applied to its own plan rather than to somebody
  else's. "Fifty registered classes with no layout, no rendering and no input
  ... draw nothing, forever, with no error" is what kept `VideoFrame` out, and
  it disqualifies two of these three: `gui::DrawKind` has four members and none
  of them is a stroked polyline, so a `Path2D` would be control points nothing
  ever looked at; and there is nothing here for a GUID registry to keep, so
  every method would return nothing. `Path2D` arrives with a fifth `DrawKind`
  and `ui::PaintGui`'s support for it, together.
  **`GuiService` is here because it has something to do.**
  `GuiObject::Selectable` had been a declared, saved, bound property with no
  reader since the tree was registered — the exact state the rule refuses — and
  the selection is that reader. `Select` refuses an element that cannot hold the
  selection rather than stranding a gamepad somewhere no direction can leave;
  `SelectNext` moves directionally over the **compiled draw list** rather than
  the tree, for `Pick`'s reason: the list is already what says an element is on
  screen, and a second traversal would be a second answer to that.
  **Scored rather than sorted**, because "is above" is not a total order — B can
  be above A while A is above C — and the across-axis term is weighted so it
  only ever breaks ties, never overrules distance. `GetGuiInset` reads the
  `Screen` rather than storing a copy that would drift on the first resize.
  Found while wiring it: the generated `Services` type was a hand-written list
  of five, so `game:GetService("StarterGui")` typechecked as an error against an
  engine that answers it perfectly well. It is derived from the class table now
- [x] **The studio's own panels, authored in TypeScript.** The point of all of
  it and deliberately the *last* step. `mono.studio/panels/Properties.ts` is a
  property grid — rows of key and value in a `ScrollingFrame`, stacked by a
  `UIListLayout`, inset by `UIPadding`, selecting on `.Activated`.
  **It is not a replacement for the imgui panels and must not be read as one.**
  `mono.studio` keeps Dear ImGui until the engine's tree can draw a property
  grid, because an editor half on each is two widget sets. This is the proof
  that it *can*, which is what has to be true before any of them move.
  **React is still an open decision and this is what it would be made
  against.** A reconciler's job is to produce this tree from a description, so
  the tree has to be known to work first — building it imperatively is how you
  find out, and `react-lua` or a hand-written one remain both reasonable.
  **Three things had to be fixed before a `.ts` panel could exist at all**, and
  each was a gap nothing failed over. `script/Runtime.hpp` had always said a
  `.ts` file "is expected to have been type-stripped already"; nothing emitted
  it, so every `.ts` in the tree was copied verbatim and QuickJS refused it at
  the first annotation — `Mirrors-1-world.ts` since the day it was written.
  Staging transpiles them now, through the `tsc` the repository already carries,
  with a missing one degrading to "no JavaScript twins" rather than a failed
  build. The generated `game.GetService` overloads were a hand-written four
  while the Luau half had been made to derive its list, so a panel asking for
  `StarterGui` typechecked as an error against an engine that answers it; both
  derive from the class table now. And **JavaScript's `GetService` never
  resolved from the tree** — the Luau half always had — so the two languages
  disagreed about what a service *is*, found by the first panel that tried to
  parent a `ScreenGui`

**Why not do the shims now, stated so nobody re-asks — and what it turned out
to buy.** Fifty registered classes with no layout, no rendering and no input
would put `TextLabel` in the insert palette, let somebody parent one under a
`Part`, save it into a game file — and then draw nothing, forever, with no
error. That is worse than not having it: it is a feature that looks present and
is not. The order above is the order in which each step has a caller.

**The rule was applied twice while doing it, both times against something that
would have been easy to ship.** `VideoFrame` is not registered, because there
is no decoder and never was going to be one this version. `AutomaticSize` *is*
declared, and that is the case where the trade came out the other way: it is a
property rather than a class, it crosses a save file and both bindings, so
adding it later is a format change and adding it now costs a byte — but a
property that is declared and does nothing is still a thing somebody will set
and wonder about, so it is filed as `D00021` rather than left to be found.

The rendering and camera work this version already carried, unchanged:

- [x] `mono.engine/examples/Mirrors-4-worlds.luau` and `.ts` — split-screen across worlds running in parallel. Four `ViewChannel` producers and one compositor, which v0.2 already built and `--worlds N` already drives with a synthetic scene. This demo is what proves the view path end to end. **Four rather than two**, because two of anything is the count at which a bug in the placement loop still looks like correct behaviour: two rooms side by side are symmetric, so swapping them or getting the sign of the offset wrong still reads as two rooms side by side. The four differ by an *ordered* palette running warm to cool and by caster count, so a wrong order reads as a wrong order and a view drawn twice reads as two rooms with the same population.

  **One file builds four different worlds, and `game.JobId` is what lets it.**
  `--worlds N` runs the same script in every world it creates; without an
  identity every view would be identical and the bug above would be invisible.
  Roblox's `JobId` names the server instance a script is standing on and a world
  here *is* that instance, so it answers `ecs::Store::Name()` — a name rather
  than a fresh GUID, because a name is already what a bus envelope, a snapshot
  and a view header carry. Both VMs, and pinned by a case in each.

  **The `.ts` twin is typechecked and not executed, and that is a gap rather
  than a choice.** `Runtime.hpp` says a `.ts` file "is expected to have been
  type-stripped already — nothing in the C++ build compiles TypeScript". Nothing
  in this repository emits it either: `tsconfig.json` sets `noEmit`, and
  `just typecheck` runs `tsc --noEmit`. So every `.ts` example, including
  `Mirrors-1-world.ts` since it was written, is a file QuickJS cannot parse — it
  fails on the first type annotation. Closing it is a staging step that emits
  stripped JavaScript beside the source, which is the toolchain's half rather
  than the engine's

### v0.9

- [x] local filesystem content delivery network (cdn) — `mono.cdn` and `cdn::ContentRoot` landed early, at v0.2
- [x] cdn content addressing — `Engine::assets` at L8 `shared`: `ContentHash` (BLAKE3-256, checked against the published vectors rather than against itself), `Chunker` (gear rolling hash, FastCDC normalised two-mask cut, table generated from a stated seed so 256 frozen constants are reviewable as an algorithm), `HashTree` (Merkle with a tagged interior and the leaf count sealed into the root, so neither a subtree nor a duplicated node can pass as a whole tree), `Signature` (Ed25519 over a domain-separated message, one signature at the root and none below it) and `Manifest` (the four-level tree, canonical order, byte-stable output, and a reader that recomputes every root rather than believing it). BLAKE3 vendored. `CDN.md` §2
- [x] cdn grants — `assets::Grant`: an HMAC-SHA256 token naming bundle roots, a session, an absolute expiry and a byte budget, plus `cdn::Gate` as the origin's whole authorisation surface. The MAC is checked before any field is acted on and compared in constant time; forged, expired and out-of-scope are counted apart so an alarm is not buried in ordinary events; a grant names content hashes and has no field a path could occupy; `nowSeconds` is passed in so neither end holds a clock of its own to drift. `CDN.md` §4
- [x] cdn group assembly — `cdn::Grouper`: self-sufficiency first (an affinity is never split, even past the ceiling, because half a scene makes nothing appear — and the oversized group is counted rather than hidden), then a size mix that falls out of largest-first packing rather than needing a rule of its own, then priority. Deterministic, because two origins that group the same content differently cache different bundles and nothing reports that they stopped sharing. `CDN.md` §5
- [x] cdn group compression — `cdn::GroupCodec` and `cdn::Dictionary` over vendored Zstd: per group and never per file or per manifest, chunks left uncompressed at rest so dedup works on them, dictionaries trained per build and content-addressed so they version like everything else. The content checksum is turned on because Zstd leaves it off and a flipped byte otherwise decompresses cleanly to the wrong content; a decompression buffer is sized from the signed manifest and never from the frame, which is what stops a kilobyte declaring a gigabyte. Zstd vendored under BSD-3-Clause, legacy frame support off as attack surface we cannot need. `CDN.md` §5
- [x] cdn prepared-group cache and cancellation — `cdn::PreparedCache`, keyed by bundle root **and** dictionary hash because a group compressed against one dictionary is a different artefact from the same group compressed against another. LRU, bounded, thread-safe, and frames handed out as shared ownership so an eviction cannot free bytes underneath a client still streaming them. Cancellation is load-bearing: a cancelled request costs no compression, and one cancelled mid-preparation has its result discarded rather than delivered to nobody
- [x] cdn as a cache server — `CDNSettings` is the whole of an origin's setup, and CDN.md §6's three sources are field combinations rather than three programs: local store (`AllowUpstream` off), cache server (local first, forward on a miss, keep what came back) and pure proxy (`LocalFirst` off, `CacheUpstream` off). Local content is checked before anything external, which is what makes a hit cost no network at all. What an upstream returns is verified against the **signed** manifest before it is cached or served — a length check, honestly not chunk-level, because the chunk layout inside a group is not designed yet; the client verifies end to end regardless, so this is defence in depth. Upstream attempts are bounded, and an upstream that is down is counted apart from one serving wrong content
- [x] **cdn wire streaming** — `Engine::net`'s `http/` sub-area landed and the origin serves over it. A strict HTTP/1.1 subset with the protocol split from the socket, so the whole wire format is exercised by a suite that opens no port: `GET` and `HEAD` only, `Content-Length` framing only, `Transfer-Encoding` refused outright and two `Content-Length` fields refused **even when they agree** — a body that can be framed two ways is request smuggling, and "even when they agree" is the check somebody eventually loosens. Polled rather than threaded, so a handler runs on the thread that pumped and every completion lands there; allowed to be asynchronous at all only because CDN.md §3's origin has no tick. Four routes and no more — `/health`, `/manifest`, `/dictionary`, `/bundle/<root>` — and **no route takes a name**, so there is nothing to walk out of a directory. Range serving answers `206` for the resumable case. `cdn` links `net` and `delivery` now, which this line predicted; every entry is still `shared`, so `just check-cdn-is-bare` still passes
- [x] cdn async pipeline — `cdn::Origin`: submit, pump, take, cancel. A publication is immutable and publishing is an **atomic swap**, so a request already admitted is served against what it was admitted against whatever is published underneath — pinned by a test that swaps mid-request. Concurrency is per request; `Jobs::For` appears in exactly one place, compressing a known set of groups, because that is CPU work with a known end and the pipeline around it waits on a filesystem. Cache lookups happen before the fan-out so a hit costs no compression. A bundle with no payload is refused rather than served empty. `CDN.md` §3
- [x] **the client half of the async pipeline** — `mono.engine/delivery` at L11 `shared`, and it is a new module rather than the `assets` addition this line predicted: fetching needs a transport, the transport is `net` at L11, and rule 1 forbids L8 seeing it. `assets/AGENTS.md` had already reached the same conclusion from the other side, so `assets` stays the format and the fetch path sits above both. **A completion becomes visible when the caller pumps and at no other moment** — no callback, no future, no background thread — which is what makes obeying rule 5 at a world's barrier the easy path. Sources are walked in priority order and a failure falls through to the next, so "local cache first, then the origin next door, then the one across the internet" is what a *list* means rather than a policy anything implements. Three checks in order: the manifest's signature, then a group's decompressed length against the **signed** manifest, then every asset against its root
- [x] **the chunk store, and the group layout** — `assets::ChunkStore` decides how chunks are laid out on a disk, which `CDN.md` §7 listed as not started and left `cdn::PayloadSource` as a seam for. It is in `assets` rather than in the origin for the reason the manifest is: **a publisher writes that tree and a client reads it**, so one implementation or the two acquire a dialect. A bundle's payload is its members concatenated in member order with no framing and no index — **derived from the signed manifest rather than transmitted**, so an origin cannot move an asset's boundary without breaking the signature
- [x] **asset kinds, and the root that binds them** — `AssetKind` in the manifest, decided once by the publisher from the extension and never re-derived downstream, which is what "connect and get assets of types" resolves to. Finding a home for it exposed a gap worth more than the feature: the manifest root covered bundle roots alone, so it bound every byte of content and nothing about what a byte was *called*. An origin serving a manifest with two names swapped handed a client content that verified perfectly and was the wrong asset. The root is now a two-leaf tree over a **descriptor root** — name, kind and content root per asset — and the bundle root. One signature, still at the top; the asset root is untouched and still addresses content alone, because folding a name into it would lose dedup across names. `Manifest::VERSION` 2, which cost nobody a re-publish because nothing had written a version-1 manifest to disk
- [x] **the origin is a program somebody can run** — `cdn --publish DIR --store DIR --signing-key HEX` builds a store, `cdn --store DIR --grant-key HEX --port N` serves one, and they are separate invocations because the signing key is separate: a key belongs to whoever publishes and **the origin holds none**, which is what makes it deployable on hardware nobody here owns. Upstreams, local-first, cache-upstream, compression level and cache size are all flags, so CDN.md §6's three deployments stay field combinations rather than three programs
- [x] **an origin attached to the server, and a client that can be pointed at one** — `server --content-store DIR --content-grant-key HEX` runs CDN.md §6's local store in-process; the grant is issued by one half and opened by the other **across a function call with both halves real**, which is §16.6's argument about the loopback transport applied to content. `client --cdn HOST:PORT --publisher-key HEX` points a game at content, repeatable and in priority order, `dir:PATH` for a local store. The studio gets the same list as a Preferences page with reorder arrows, because the order *is* the policy
- [x] **mesh importing and baking** — `assets::Mesh` is the baked format (`AMS1`: interleaved position, normal and texture coordinate, 32-bit indices because a PMX character is well past 65535 vertices, and one submesh per material because a mesh is not one material). **The bounds are derived on read and never stored**, because a stored bound is the one field of a mesh an attacker gets to choose — a zero box disappears from every frustum test and a huge one draws from everywhere, and both look like renderer bugs. `mono.engine/bake` at L9 `shared` is the only code in the engine that reads a `.glb`, a `.pmx` or an `.obj`: glTF with the node hierarchy folded into the vertices and a skinned node contributing the identity rather than its own transform (the specification's rule, and what stops a skinned model importing on its side), PMX with Z mirrored **and the winding reversed to match** — one without the other is a model you can only see from inside — and OBJ with negative indices resolved from where they are and `v` flipped once at import rather than in a shader that would flip the formats already right
- [x] **texture importing and baking** — PNG (every colour type, 8 and 16 bits, CRC-verified, interlaced refused by name), BMP (8/24/32-bit, and the signed height that means top-down), and a baseline JPEG decoder written for this — Huffman, restart intervals, greyscale and YCbCr, **libjpeg's triangle chroma upsampling weights verbatim**. The first version sampled nearest with a comment claiming that matched other decoders; measured against Pillow it was 28 levels out on a saturated chroma edge and the filtered version is 3, which is the inverse DCT's own rounding. Every decoder is checked byte-for-byte against an independent implementation on the real 2048-pixel sheets, not only on fixtures. Progressive JPEG and interlaced PNG are **refused by name** rather than half-read, because a recognisable wrong picture reads as a quality setting rather than as a bug
- [x] **the node pipeline** — `bake::Graph`, and the shape is `audio::Graph`'s deliberately: input nodes (`Source`, `Builtin`), processing nodes (`Import`, `Fit`, `Scale`, `Smooth`, `Resize`, `Opaque`) and export nodes (`Write`), run in topological order with **cycles refused at the wire**. **The graph touches no filesystem**, which is what `audio::NullDevice` is to the mixer: every importer, every processing step and every export is exercised by a suite that opens no file, and a studio baking into memory runs the identical code a CLI baking onto a disk does. A node has at most one input, so "what produced this asset" is a walk rather than a fold; fan-out is the useful direction and is allowed
- [x] **auto export to assets folder** — `assetc --input ART --output content`, which is `cdn --publish`'s input and knows nothing about it. A baked asset's name is its source's path with the extension replaced, and **that one rule is what makes a model's texture references rewritable without a lookup table**: the importer reports the path the model spells, and the same substitution names the baked texture. A file the baker does not understand is copied rather than dropped, because the output tree is what gets published and a baker that dropped every sound would produce a directory missing half the game. One unreadable file costs that file and not the run
- [x] **built-in engine meshes** — `assets::MakeBuiltin` generates a cube, plane, wedge, corner wedge, sphere and cylinder, and `render::MeshTable` registers all six at start-up so a scene may name any of them with nothing published. Generated rather than tabulated, because the property that matters is the **winding** and a hand-written sphere makes it unreadable: the suite checks that every triangle of every built-in points outwards, that every closed one is a manifold — every edge shared by exactly two triangles traversing it in opposite directions, which subsumes "no hole", "no duplicated face" and "no missing pole triangle" — and that each is the same geometry every call. `render::Primitives` held the cube and is deleted; there is one definition now
- [x] **MeshPart, SurfaceAppearance as components, and tagging** — `MeshPart` derives `BasePart` and adds no component, because `Visual::Mesh` and `SurfaceAppearance::ColourMap` already sit there; what the class is *for* is the vocabulary, so a script says `Instance.new("MeshPart")` and reads `.MeshId` and `.TextureID`. **Both new components are on `BasePart` rather than on `MeshPart`**, which costs sixteen bytes on every part in the world and buys a draw-list pass with no optional join — `client::CollectInstances` is a batched parallel walk over a fixed signature, and an optional column is exactly what that shape cannot express. `scene::Tags` is a 32-bit mask against a `TagTable` resource that maps a name to a bit: a name outside the process and a bit inside it, so a pass filtering by tag is an `and` per instance rather than a set lookup. **The thirty-third tag is refused rather than aliased**, because an alias puts one group's objects in another group's pass and is invisible until somebody notices the wrong thing reflected
- [x] **rendering a mesh, and its texture** — `render::MeshTable` packs every mesh into **one** vertex and index buffer pair, so a mesh is `(firstIndex, indexCount, vertexOffset)` and changing mesh costs three integers on a draw call rather than a rebind. `render::TextureTable` is its twin. `Renderer::DrawSlots` is the loop v0.9 exists to add: it splits each contiguous instance run wherever the mesh or the instance's texture override changes and issues a draw per piece — **consecutive-run splitting rather than grouping**, because the blended pass's order is back-to-front from the eye and reordering it is the transparency bug the sort exists to prevent. Three colour sources multiply rather than one winning: the texture, the submesh's base colour and the instance tint. Fixing the vertex layout exposed a bug that had been invisible while everything was a cube — the model matrix has a half-extent folded into it, so `mat3(model)` is not the normal matrix, and an axis-aligned normal survives the wrong matrix while a sphere's does not
- [x] **it works, end to end, on real assets** — `assetc` bakes a `fox_pet.glb` (ten material runs, no texture, recognisably a fox only because each run carries its own base colour) and a PMX character (twenty-seven submeshes, UTF-16 texture paths converted to UTF-8, six 2048-pixel sheets); `cdn --publish` signs the tree; `client --cdn` fetches, verifies against the signed manifest and registers into the renderer's tables at the pump barrier. **88,302 triangles in 136 draw calls with the origin attached and 1,796 in 28 without it** — the second number is every `MeshId` resolving to the fallback cube, which is the deliberate degraded state. `examples/Meshes.luau` is the scene, `client --capture` is the new flag that writes a BMP of it, and there is a mirror in front of the meshes: `ROADMAP.md` asked for exactly that check, and a frame where the reflection is right and the meshes are not says which of the two paths broke
- [x] scripts that create MeshPart and set mesh properties — through the same class table `Instance.new` has always used. `MeshId` and `TextureID` are declared properties projecting `Visual::Mesh` and `SurfaceAppearance::ColourMap`, so a script setting `.MeshId` and one reading `.Mesh` cannot disagree, and both languages get them from one declaration rather than from two bindings
- [_] mip chains — `assets::Texture` is one image and has no place to put the levels, so a 2048 sheet minified onto forty pixels shimmers. `bake::ResizeImage` is already the box filter that would build the chain; it is a format change that should arrive with sampler work rather than ahead of it
- [x] **a material format** — closed at v0.10 rather than here, and the entry stays in v0.9 because that is where it was promised. `AssetKind::Material` writes something now: `assets::Material` is an `.amat`, `assetc` bakes a `.mat` into one, and `Submesh::Texture` is what it references. The v0.10 line below carries the whole change, including the enum it deleted
- [_] skinning and animation — `bake` skips joints and weights and keeps the rest pose, because there are no skeletons in the engine yet
- [x] **tag-filtered surface passes** — `SurfaceCamera::TagFilter` is a mask an instance must intersect to be drawn into that surface, so a second camera pointed at the same scene draws a *group* rather than a second copy of it. **Applied per instance in the draw loop rather than by re-ordering the draw list**, because the order is shared by every view and a filter is per view: partitioning for one surface would partition for all of them and the screen pass would then draw the group instead of the world. The property is authored by name and stored as a bit — a script says `camera.TagFilter = "Imported, Reflective"` and the registration happens once, where the name is written, so the hot path never sees a string. **A list in a string**, because a mask holds thirty-two and a property holds one value; the alternative was a list type in `ecs::PropertyType` that exactly one property would use. A filter that would overflow the table is refused whole rather than applied part way, since a camera left with half its filter draws some of its group and that is harder to notice than none of it. `instance:AddTag`, `:RemoveTag` and `:HasTag` are the script side; they are methods rather than a `CollectionService` because the thing being tagged is already in hand and a service would add a lookup and nothing else. Measured on `examples/Meshes.luau`: **892 fewer triangles and 9 fewer draw calls** with the filter on, which is exactly the six built-ins, the baseplate and the two panes it excludes
- [x] **the v0.9 components replicate** — `scene.SurfaceAppearance` and `scene.Tags` are in all three authority lists, because a replica that received a mesh name and no texture name has half a model and it reads as a broken texture path rather than as a component nobody sent. `Signature` detection, since neither changes on a steady world. **The tag *names* do not cross and that is stated rather than hidden**: a `TagTable` is a resource and resources have no wire form, so a replica's table is empty and `HasTag(name)` there answers false — mask-against-mask filtering is unaffected, because a surface camera's filter and an instance's mask both come from one authority
- [x] **a priority score that reads a distance** — `replication::DistancePriority` fills in the `SetPriority` hook that had sat empty since v0.4, so a client is sent what it is standing next to before what is across the map rather than a plain round robin. **The arithmetic is `replication`'s and the lookup is the host's**, which is the division `SetPriority`'s own documentation asked for: two caller-supplied accessors, and the module still names no component. The ramp is linear rather than inverse-square — an inverse square collapses within a few metres, so everything past that sorts identically and the rotation decides anyway, which is the behaviour the score exists to improve on. Nothing a caller supplies can produce a non-finite score, because `std::sort` on a comparator that is not a weak ordering is undefined rather than merely wrong. `Server::SetClientViewpoint` is the host's half, keyed by slot *and generation* so a reused slot cannot hand a new client the previous one's viewpoint
- [x] **the handshake binds to a server identity** — `net::Handshake` has carried a `TODO(D00006)` since v0.3 saying an unauthenticated agreement is safe against a listener and not against a relay. It is closed, and **not in `net`**, which was always the plan: the fix binds the exchange to a *server identity* and that module has no idea what a server is. `Welcome` carries an Ed25519 signature over the same transcript its confirmation tag is computed on, signed with the key a publisher signs manifests with — one identity to distribute rather than two to pin wrongly — under its own domain tag so a manifest signature can never be replayed as a session one. `server --identity-key` and `client --server-key` are the two halves. **The tag and the signature are different checks**: the tag proves the sender reached these keys, which a relay does too by holding one exchange with each side; the signature proves which server reached them. The suite models a competent relay — two exchanges, so the tag passes and only the signature refuses — with a control beside it that removes the pin and watches the same bytes get in. Both ends default to the weaker mode and both log it
- [x] **lag compensation** — `replication::Rewind` keeps the last half second of placements and answers "where was this, then", so a server can judge a client's action against what that client actually saw. **A record and not a rollback**: nothing is re-simulated and no state is restored, because a hit test does not need one and rollback is a far larger blast radius. The lookup is the caller's, as the priority score's is, so the module still names no component. Two details decide whether it is right or merely present — the sample is **fractional**, because a client renders *between* two ticks and a whole-tick answer is a pose it never saw; and the compensation is **half** the round trip, because the input's journey back is already carried by the tick it names. The suite runs the whole loop at a speed where one tick of error is ten metres, and pins the fifty-metre gap between what the client aimed at and where the server thinks the target is
- [x] **the server applies inputs, authoritatively** — the line that said "nothing consumes inputs yet: an input has nowhere to be applied" is gone. `examples::Shooting` is the demo game's rule — a shot is a ray and a range, and **the ray crosses the wire rather than the hit**, because a client that decided what it hit is one nothing downstream can second-guess. The server decodes it, rewinds to the tick that client saw, tests it against the *history's* candidates rather than the world's — an entity destroyed since is still a legitimate thing to have shot at — and recolours what it struck, which replicates like any other `Visual`. Nearest wins and ties break by entity id, because candidates arrive in hash-map order and a server whose hit resolution depends on that is one whose recordings do not replay. The rule lives in a game module and not in `replication`, which is the whole reason `Input::Bytes` is documented as "the game's own encoding"
- [x] **a line of sight in the priority score** — `DistancePriority::Blocked`, wired on the server to `physics::Raycast`. Same split as the distance term: the arithmetic here, the query there, because occlusion needs a broad phase and a game's own idea of what occludes. **A hidden entity is scaled and never zeroed** — one scored at nothing is one only the rotation ever sends, so a player rounding a corner meets a wall of objects snapping into place; it should update *less*. And the cheap test gates the expensive one, so anything already scoring near nothing is never raycast about
- [x] **the client authenticates too** — `Identify`, signed over the same transcript, sent as the client's first act on the encrypted stream. It **sends** its key where a client **pins** the server's, and the asymmetry is the point: a server has one identity every client expects, and expects many clients it cannot list in a setting, so `Listener::SetClientPolicy` decides. A claim that does not verify is dropped without consulting the policy — "is this key real" is not a game's question. Adding the kind found a real trap: `ReadMessage`'s range check still named the previous last kind, so every `Identify` parsed as out of range and was dropped as malformed while every `-Wswitch` stayed quiet
- [_] finish replication for server/client, server authoritative — the mechanisms are in place and what remains is a *game*: a client that sends inputs from real aim, a player entity per connection to give `SetClientViewpoint` something true to say, — `net::ConnectionStats::RoundTripMilliseconds` is measured now, which it had not been since it was declared at v0.3: `ReliableSender` samples the gap between a reliable packet and its acknowledgement, smoothed at RFC 6298's one eighth, and **skips any packet that was resent** because an acknowledgement of a resend does not say which transmission it answers. That is Karn's rule, and the first version of it was inverted — `Attempts` counts *re*sends and starts at zero, so a check for one measured precisely the packets the rule exists to exclude
- [x] **audio in cdn and running audio in-studio and in-game** — `mono.engine/audio` at L12 `client`: the DAW-like node pipeline `core-features.md` asks for, as `Player` inputs, `Fader`/`Emitter`/`Bus` processors and one `Output`, wired by directed edges and run in topological order. **Cycles are refused at the wire rather than found at the mix**, because a feedback loop is either infinite recursion inside a callback with a hard deadline or unbounded gain. The requirement `DATATYPES_LIBRARIES.md` §11.2 marks `!` is what shapes the whole module: a game ticks at frame rate and audio runs at sample rate, so **the tick queues a command with a sample deadline and the mixer splits its block at every deadline inside it** — a `Play` due at sample 200 of a 512-frame block produces two sub-blocks, not one applied at the top. A missed buffer is audible, so nothing on the render path allocates, blocks or locks: the mixer owns the graph, commands cross on a lock-free single-producer ring, scratch is sized when the topology changes, and a sound is a shared pointer copied on the tick side. RIFF/WAV decoding treats every length field as hostile — a chunk running past the end is a refusal and never a clamp, because a clamp turns a truncated file into a shorter sound that plays. Spatialisation is equal-power panning and an inverse-square falloff pinned to zero at its end. `NullDevice` renders on demand with no sound card, which is what makes **121 cases** of this testable at all; `client --sound FILE.wav` is the caller. The cdn half was already done: `AssetKind::Audio` classifies at publish and `RequestKind` fetches every sound in a game

### v0.10

- [x] **CurrentCamera** — `workspace.CurrentCamera`, a `Reference` property over the `ActiveCamera` resource rather than a component on `Workspace`. Reading resolves the resource and writing moves it, so there is exactly one place the live camera is recorded and no way for a second to disagree — which matters because `client::InstallDefaultCamera` writes the resource directly. **A write is refused for anything that is not a camera**, because assigning a part would leave `ResolveActiveCamera` looking for a component it will not find and the matrices would silently stay as they were: a frame drawn from where the camera used to be, which reads as a renderer fault. Removing the *hand-written* declaration was the other half — the generator had special-cased `CurrentCamera` because it was not a declared property, and an extern type carrying the member twice is not a warning, it is an assertion failure inside Luau's constraint generator that surfaces as `SIGILL` on every script in the tree. The same correction `Instance.Name` went through at v0.5, failing the same way
- [x] **basic character controls** — `scene::Humanoid` under a character model, which is Roblox's arrangement: the humanoid is a sibling of the parts it drives rather than a component on one. Deliberately small, because the instruction beside this item said to spend the time on the particles — a capsule, a walk speed, a jump and a ground flag, and no state machine, animator, ladder or swim. **`MoveDirection` is a direction and not a velocity**, which is the split that lets `WalkSpeed` mean something; the horizontal velocity is *replaced* each step and the vertical is kept, because adding a force leaves momentum that makes stopping take a second and replacing Y as well would cancel gravity. Input is normalised, so running diagonally is not forty per cent quicker — the oldest movement bug there is. **Ground detection is a downward ray and the query is the client's**: `scene` may not link `physics`, so `StepCharacters` reads `Grounded` and `client::GroundCharacters` writes it, which is `DistancePriority::Blocked`'s split exactly
- [x] **basic controls (enable/disable/shift-lock)** — `CameraController::Enabled` is separate from `CameraMode::Scriptable` on purpose: a disabled camera keeps its mode and stops responding, so re-enabling puts the player back where they were, where a script that switched to `Scriptable` and back would have lost the angles. Shift-lock is a *mode* rather than a flag, because it is the one where the character faces where the camera faces rather than where it is going — and the camera sits over a shoulder, which is a distinct offset and not only a facing rule
- [x] **basic camera and controls (zoom, pan, control camera via script)** — `scene::CameraController`, a resource, with the arithmetic in `shared` and the events outside it. **Turning needs the right button unless the pointer is locked**, which is Roblox's rule and what keeps a mouse usable for anything else. **Zooming all the way in *is* first person** rather than a separate key, because the transition a player expects is continuous. The pitch clamps a hundredth of a radian short of vertical, which is not fussiness — at exactly ninety degrees the `LookAt` has no way to choose a roll and the camera spins about its own axis. `CameraMode::Scriptable` is what makes `workspace.CurrentCamera.CFrame = ...` survive: without it the controller and the script would both write the transform every frame and the last one would win silently, which is the bug `MoveCamera` already records
- [x] **UserInputService and ContextActionService** — and the interesting half is the seam. **`input` is L12 `client` and `script` is L9 `shared`, so the service cannot read the module that produces its data.** `scene::InputState` is a resource both can see: `input::Translator` turns SDL events into named keys and the client copies the result onto every simulated world, `UserInputService` reads it, and `MouseBehavior` travels back the other way for the client to apply to the window. A headless world has no such resource and every query answers "nothing pressed" rather than raising, which is what lets one script poll input on both halves of a game. **What `ContextActionService` adds over polling is a priority stack** — two systems both wanting E is the question `IsKeyDown` cannot answer — sorted at bind time, stably, so ties break in bind order and a recording replays. Two real bugs came out of building it: the action list was a `thread_local`, so two VMs on one thread shared registry references and a *MemoryStore* test crashed; and `PumpInput` used `UpvalueContext` where it has no C closure on the stack, which turned every heartbeat into a failure. A third is a Luau constraint rather than a bug and is filed as D00030 — `luaL_sandbox` enables `safeenv`, so a property read as a bare global is resolved once and cached, and the service has to be reached the way every Roblox script already reaches it: `local UIS = game:GetService("UserInputService")`
- [x] **beams, trails** — `effects::Beam` and `effects::Trail` are **one ribbon builder with two sources of points**: a beam is a strip between two attachments with a cubic Bezier through it, a trail is a strip between two attachments sampled over time. Neither is a `DrawInstance` and neither has a mesh, because a ribbon's geometry is a function of where its endpoints are *this frame* — a mesh would be a mesh table entry churning at frame rate for four vertices a segment. **A trail's history is a fixed-capacity ring on its own row**, sixteen points, because a `std::vector` per trail is an allocation per trail and — the rule that actually forbids it — a component holding one cannot survive being memcpy'd across a process boundary. **Recording happens in `Simulation` and building in `PreRender`**, which is the one place this module departs from its own pattern: a trail is a record of where something has been, so sampling it at frame rate would make its length depend on the machine drawing it, and two players swinging one sword would leave different arcs. **The pass draws them**: a triangle strip over a real vertex buffer with a blended and an additive pipeline, one draw call per run — which is the opposite of the particle path's grouping and is right for the opposite reason, since a run is already a whole beam and two runs cannot merge without joining the end of one to the start of the next. **The geometry is built on the CPU in `shared`**, so a beam's curve, its width taper and its camera-facing turn are all checkable in a suite; the vertex shader is a matrix multiply and an unpack. Verified on screen: the arc, the colour sequence along it and the width taper all render
- [x] **pointlight, spotlight, surfacelight** — three classes over **one** `scene::Light` component, which is the trade `Collider::Extent` already makes across three shapes: the three differ by two fields, and three components would be three columns, three queries and three upload paths for something a renderer packs into one array either way. Each class sets its own `LightKind` as a *prototype default*, so `Instance.new("SpotLight")` is a spot light without a script saying so — and a `Kind` property is deliberately not declared, because the class is the answer and a second way to say it is the duplicate `AGENTS.md` warns about. `Angle` and `Face` sit on the base and therefore exist on a `PointLight` that ignores them, which is the honest cost of the single component and is stated rather than hidden. **They light now**: `client::CollectLights` resolves each one through its parent — a light carries no position, for `Sound`'s reason — orders them by distance from the eye and hands the nearest sixteen to the renderer, which adds them to the directional term in the opaque pass. Added rather than replacing, so a scene with no lamps looks exactly as it did before v0.10, which is what makes it safe to switch on for every existing world. The falloff is inverse-square *windowed to zero at the range*, because a bare inverse square never reaches zero and a light would pop off at its cutoff. The one gap is honest and filed as D00029: the light count is spelled in C++ and in GLSL and nothing checks that the two agree
- [x] **attachment** — the fence post the other three hang off, which is why it was built first: beams, trails and particle emitters are all parented to one, and any of them built earlier gets built twice. **The one exception to "every transform is world space and nothing propagates it", and it is narrower than it looks** — an `Attachment` is not a `PVInstance` and carries no `Transform`; it holds its own pair of frames and one flat pass over one component type derives the second from the first. There is still no transform hierarchy, no dirty cascade and no per-entity parent walk in the simulation. **Attachments do not nest, and that is enforced rather than assumed**: resolving against another attachment's *resolved* frame is what makes a pass order-dependent, and an order-dependent pass over an archetype walk answers differently depending on which rows moved last. `WorldCFrame` is a derived field rather than a getter because a beam reads four of them a frame — but the *property* resolves on the spot, so a script reading it on the frame it created the attachment gets the right answer rather than a stale identity
- [x] **NumberSequenceKeypoint + ColorSequenceKeypoint** — real userdata types, reversing a decision this engine had made deliberately. The note in `LuauDatatypes.cpp` said two more types for a value written inline once was surface nobody asked for, and while a sequence was only ever *constructed* it was right. What made it false is that a sequence became a **property**: an emitter's `Transparency` is read back, and `Keypoints` handed out `{time, value, envelope}` tables — three anonymous numbers with no `typeof`, indistinguishable from a colour ramp's stop, and nothing to compare against. Reading a value back in a shape its own constructor accepts is the round trip a property surface owes. **The table form still works everywhere it did** and both constructors take either. Writing the test for it found a real pre-existing bug: the table branch read its fields through a *relative* stack index that shifted as the stack grew, so the second field came out of the first one's value — it had survived because every sequence in the suite was built from the two-argument form
- [x] **Vector3.zero / Vector3.one** — lowercase, because Roblox's are, and a second spelling of one constant is the duplicate `AGENTS.md` calls the most expensive kind of debt. Members of the *library* rather than of a value, so they are fields on the global table and not cases in `Vector3Index` — putting them in the index would make `part.Position.zero` answer, which is a member nobody wrote. Pushed as ordinary userdata carrying the same metatable and tag as anything `Vector3.new` returns, because a constant that failed `CheckVector3` would be one no property could be assigned from
- [x] **hook assets manager to local cdn folder** — `cdn::LocalStore` over `~/Documents/atomic-game-engine/cdn`, flat, with `raw/`, `processed/` and `content.log`. **The two folders are split because the two halves have different identities**: a raw file is what somebody called it and changes when they edit it; a processed chunk is what its bytes are and never changes at all — putting both in one folder means either hashing the names or naming the hashes, and each loses the half that made the other useful. A raw import is named `<hash><extension>`, which is the roadmap's "hash-based naming between the folders": the hash makes the two line up and makes a re-import free, and the extension keeps the folder readable. **The original name is not on disk and is in the log**, because a flat folder of original names collides the first time two projects both have a `diffuse.png` — and a log is the only thing that can answer "where did this come from" six months later. The log is appended and never rewritten, so two processes interleave rather than losing each other's lines, and a line truncated by a crash is skipped rather than refusing the file. **The client defaults to it when nobody passed `--cdn`**, which is what "hook up all cdn behavior to this by default" means in practice: content resolves with no flag, where before it took `assetc`, `cdn --publish` and a path on every run. Six tests, every one of them in a temporary directory — a suite that used the default path would write into the developer's own store. **Not done from this line: the live renderer**, which is a preview of a selected asset and wants the studio's viewport to render something that is not a world
- [x] **upload current assets to that folder** — `mono.tools/contentimport`, and a tool rather than a shell loop because a loop run once leaves nothing anybody can run again on another machine or after the store is cleared. Directories are walked and files taken as they are, because a person dragging content in thinks in folders. Run over the roadmap's own list: **289 files, 702 MB** — 231 PNGs, 12 BMPs, 6 PMX models, four GIFs and the audio, with five already present and none failed
- [x] **assets manager with an upload button and a view of what is there** — a studio panel over `cdn::LocalStore`, finished at the end of v0.10 rather than at the item. **The `[~]` was the typed path, and the reason given for it was wrong**: the note said there is no portable picker in this stack, and there is one — `studio::FilePrompt`, an imgui browser that six other dialogs in this editor already use. The probe answered "SDL has no synchronous dialog" and the question was "can this editor browse for a file". So the field is gone and there are three ways in, all ending at `ImportFile`: **drag and drop, single or many** — SDL sends one `SDL_EVENT_DROP_FILE` per path, so a forty-file drop and a dropped folder need no special case — **Add files…**, and **Add folder…**, a new `FolderPrompt` that is beside `FilePrompt` rather than a flag on it because the two differ in what a click on a row *means*. **And the listing is now the store rather than the log.** It listed log subjects, which are the paths files came from — `~/Music/...`, `~/art/...` — so most of what it showed was somewhere else on the disk and some of it no longer existed. Now every row is a file in `~/Documents/atomic-game-engine/cdn` and nowhere else: `raw/` in one tab, the signed manifest's assets in the other. The log is still read, and only to *label* a row, because `raw/` is hash-named and nothing else can say what a file was called. A file dropped into `raw/` by hand is listed too, which a log-driven listing could not have done
- [x] **convert our luau/ts Enum_Material to Enum.Material** — both languages, and getting here took a correction worth recording. TypeScript was straightforward: an `interface Material` and a `const Material` inside `declare namespace Enum` are one name meaning both, because TypeScript keeps types and values in separate declaration spaces. **Luau was declared impossible and was not.** The first pass probed a definitions file, got the error "Unknown type Enum.Material", and concluded the language could not express it — the probe was right and the conclusion did not follow. A definitions file genuinely cannot declare a dotted type: `loadDefinitionFile` only ever writes `exportedTypeBindings[name]`. What resolves `Enum.Material` is `Scope::lookupImportedType("Enum", "Material")` — the `importedTypeBindings` map, which `require` populates and which a **host** may populate directly. That is exactly what Roblox does, and it is now what `mono.tools/scriptcheck` does: it walks the extern types the generator emitted under the `Enum_` prefix and aliases each under the `Enum` prefix before freezing. 35 enums, and `local m: Enum.Material` typechecks. **The declaration file keeps the flat names**, because the aliases are built *from* the types it creates and so cannot exist while it is loading — emitting the dotted form there made the file fail to load before a single script was checked. Both spellings name the same `TypeFun`. `DEFERRED.md` D00028 is closed with the correction written out; the editor half is open as D00031, because luau-lsp reads the same file and nothing registers the prefix for it
- [x] **Attributes and GetAttribute/SetAttribute** — and the storage question is the whole item. An attribute is a name an author invents at run time mapped to a value whose type they also pick, which is exactly the shape a component column cannot hold: a component holding a map would be a heap allocation per row on `Instance`, which is every row in the world, and rule 3 forbids it outright for anything that must survive a memcpy across a process boundary. **So it is a resource keyed by `(Entity, Name)`, and an entity with no attributes costs nothing at all** — not even a byte on its row, which is the case that matters because it is almost every entity. The value types are `ecs::PropertyType`'s rather than a new list, because an attribute and a property are one question asked at run time and at declaration time; the only refusal is `Reference`, because a handle means nothing outside the world holding it and an attribute crosses a save file. Signals go through the existing change queue, so an attribute written three times in one tick fires once with the value it ended at. Writing the test found a real bug: entity ids are reused, and the first version hooked `Store::Destroy` while a script's `:Destroy()` goes through `DestroyInstance` — so an attribute outlived its instance and surfaced on a freshly created part. The hook is now on `DestroyEntity`, which is the one point every row leaves by
- [x] **particle emitters (and hook to particle system)** — `Instance.new("ParticleEmitter")`, parented to a part or to an `Attachment`, simulated by `effects::StepParticles` and drawn by the renderer's own pass. The client installs the five systems that run it and turns the pool into `render::ParticleBatch`es — a span into the pool rather than a copy, so half a million particles reach the renderer without being moved
- [x] **flipbook particle animation** — `FlipbookLayout` of `Grid2x2`, `Grid4x4` or `Grid8x8`, with four playback modes. **The grid is square and a power of two on each side**, which is Roblox's set and is also what makes the cell arithmetic two shifts rather than two divides — this is evaluated per particle per frame, so the difference is half a million divides. `OneShot` is the default and stretches the sheet over the particle's whole lifetime, which is what almost every authored sheet is drawn for; `Random` holds one cell per particle, which is what a sheet of *variations* is for rather than a sheet of frames
- [x] **particle system**, at the stated scale and with the bottlenecks measured rather than guessed. New module `mono.engine/effects` at L8 `shared` — shared because a headless suite is the only way this gets checked at all, which is `scene::OrderScene`'s argument for living outside `render`. **The whole design is one number.** 100,000 emitters times five particles is half a million a frame, and that rules out a particle being an entity (create and destroy are what an archetype store is worst at), a `DrawInstance` (80 bytes of which none varies per particle), or a reader of its emitter's curves (four sequences, a kilobyte, scanned per particle). So: emitters are entities, particles are a pooled SoA in contiguous per-emitter blocks, retirement is a swap inside a block so `Jobs::For` needs no atomic, and each block holds a 16-sample table the step indexes instead of evaluating. The Roblox property surface is there in full — shape, style, in/out, partial, spread, drag, squash, rotation, velocity inheritance, locked-to-part, Z offset, time scale — and what is *not* implemented is absent rather than declared, which `effects/AGENTS.md` lists. **Measured, `release`, 24 threads: the two simulation passes cost 389 us at the full count**, and the stress scene runs at **170 FPS with 5 draw calls**. Three bottlenecks were found by measuring and every one of them contradicted a comment that had argued the other way: the refresh pass was re-sampling curves every frame and was 70 per cent of the frame (gated on `Store::Changed`, 522 us to 192); the render pass issued **one draw call per emitter** and would have been 100,000 of them (grouped by state, 1,608 to 6 on the same scene); and the upload memcpy'd into the transfer buffer and never issued the copy to the vertex buffer, which drew the *previous* frame and so took a screenshot rather than a crash to find. A fourth was in the scene rather than the engine — its host parts were authored `Transparency = 1`, which submits them into the sorted blended pass, so a hundred thousand invisible cubes were being sorted and drawn every frame at 4 FPS; `Visible = false` submits nothing, and the three-way distinction `Visual::CastShadow` documents is exactly that
- [x] **seed the store with the listed content** — the roadmap's own file list, brought in with `contentimport`: the three `~/Music` albums, `anime_girl_out`, `fox_pet`, `blender-dragon`, the six audio tracks, `fox_dance.gif` and the image. **289 files and 702 MB**, five of them already present because two of the folders shared bytes — which is content addressing doing its job on the very first import rather than in a benchmark
- [x] **add gif support** — a decoder in `bake`: LZW, interlacing, local and global palettes, transparency and the three disposal methods, into **one flipbook sheet rather than a list of frames**. That is the whole design decision and it is why the roadmap put GIFs and flipbook particles in one version: a GIF is a short looping animation, and the thing this engine can already *draw* animated is a flipbook — so the frames become a square power-of-two grid and an ordinary texture that every existing path handles. The alternative, an animated texture type, is a real feature and would have left a GIF unable to be a particle, which is the case that asked for it. What it costs is stated rather than hidden: a 12-frame GIF wastes four cells of a 4×4 and anything past 64 frames is truncated. **Verified against the roadmap's own `fox_dance.gif`** — 24 frames of a dancing fox at 420×420, composited correctly into a 3360×3360 8×8 sheet. The test builds its GIF byte by byte rather than reading one from disk, and hand-packing the LZW stream is what caught an off-by-one in the test's own encoding: the codes pack LSB-first at three bits and come out as two bytes, not three
- [x] **materials are content, and `Enum.Material` is deleted** — the enum held seventeen names, checked membership, and **no renderer sampled anything different for any of them**: a property that looked like it worked, on the most obvious control in the properties panel, for four versions. So the whole of it is gone — `Visual::Material`, `DefaultMaterial()`, the `EnumTable` registration, the generated `Enum_Material` — and what replaces it is an asset. **`assets::Material` is the format v0.9 listed as missing**: a magic, a version and one texture name, deliberately the dullest thing that could work, with the other four maps *absent* rather than declared and ignored — `SurfaceAppearance`'s own rule about `MetalnessMap`, applied to the type that would have carried it. `assetc` bakes a `.mat` source into it and rewrites the colour map through the **same `BakedName`** a model's texture reference goes through, because two spellings of that rule is a material resolving to nothing on a machine nobody tested. **A `Material` is an instance under a part**, which is Roblox's `SurfaceAppearance` arrangement and is what v0.11's node-based asset pipeline needs — a thing that will grow several texture references cannot be a `core::Name` field on `BasePart`. It resolves through `scene::MaterialCatalogue` and one flat `PreSimulation` pass onto the parent's `SurfaceAppearance::ColourMap`, which is `ResolveAttachments`' shape exactly and for its reason: the draw-list pass is a batched parallel walk over a fixed signature and cannot follow a child. **The default is `None`, and `None` now draws something** — `render::DefaultTexture`, ambientCG's Plastic 013 A as a compiled-in 64-pixel tile, lifted so its brightest texel is white because a default that multiplies every untextured part by 0.92 is a scene-wide darkening no author asked for. The one white texel it replaces was a stand-in for a *binding* and is still that; conflating the two is why every untextured part in the engine was flat white. One gap, filed as **D00032**: deleting a `Material` child leaves the texture it last resolved, because nothing visits a part that no longer has one
- [x] **seed the store with public-domain materials** — `scripts/fetch-materials.py`, stdlib-only, over **ambientCG, Poly Haven and cgbookcase**, all three CC0. **291 materials, 2.7 GB of source**, at 1K with the full map set — colour, normal, roughness, ambient occlusion and height — of which only the colour map is *named* by the `.mat`, because only a colour map is sampled today and a key nothing read would be half a feature. The other four are published beside it as ordinary textures and are waiting for v0.11's G-buffer, so what is missing then is a field and a pass rather than the pixels. **The map suffixes are normalised and all three sources disagree about every one of them** — `Color`/`BaseColor`/`Diffuse`, `NormalGL`/`nor_gl`/`Normal` — and a `.mat` naming whichever spelling its source used would make the material format's reference depend on where the pixels came from, which is a dialect in the one place this engine has been careful not to grow one. The script stops at a staging tree and `assetc` bakes it into `raw/`: three steps, each already having one implementation, and a downloader that grew a second would be the mistake `AGENTS.md` names. **`cdn::RawContents` is recursive now**, which it had to be — `cdn::Publish` has always walked `raw/` recursively and named assets by relative path, so a tree there was always publishable, and v0.10's material import is the first thing that writes one, because a material has to *name* its texture and `ImportFile`'s hash rename gives it no name to write. A store of six thousand files listed as empty, which reads as a broken panel
- [_] the other four maps — normal, roughness, ambient occlusion and height are published and nothing reads them. `assets::MaterialData` gains four names and `.mat` gains four keys when there is a pass that samples them, which is v0.11's G-buffer. The baker already ignores unknown keys for exactly this, so a material written for the newer engine bakes on the older one
- [x] **material previews and a material picker that shows one** — the assets panel thumbnailed a texture and a `.amat` had no picture of its own, so every material row drew a dash for ever. It is the engine's own **sphere** wearing the material's colour map, drawn through the preview slot the mesh hover already owns — which is why this arrived as a small change rather than the deferred one this line predicted: "point the studio's viewport at something that is not a world" was built for meshes and a material is the same slot with a texture bound. A sphere because it shows a map at every angle at once — the top faces the light, the limb goes to grazing, and the silhouette is identical for every material, so two are told apart by their surface rather than by their outline. `MakeSphere` duplicates its seam column, which is what stops the map running backwards across one column of triangles. **Resampled to `PREVIEW_MATERIAL_SIDE`**, because this is the one preview that would otherwise grow without a bound: a mesh is capped by its triangle count and nothing evicts a preview, so hovering thirty 2K sheets at full size is half a gigabyte of device memory for pictures of spheres. **White under a texture and grey without one** — the grey stops an untextured mesh being a silhouette, and multiplying a material's own colour map by 0.92 would darken every preview against the part the same material draws on, which is the one thing a thing being looked at to judge a colour must not do. A material naming no map previews as a bare sphere and one naming a map this machine lacks is `Unavailable`, because a grey ball would be a picture of something the material is not. The routing — which kinds are a render rather than a bitmap — is `PreviewIsRendered`, one function rather than the same condition spelled in the hover panel and in the list row
- [x] **the world lifetime policy is somewhere both hosts can reach — `D00017`'s near half** — when a world stops (idle past a limit), when it starts again (something in its inbox) and three refusals lived entirely inside `mono.studio`, and `mono.server` has no `SetState` anywhere. `engine::world::DecideLifecycle` is the decision, at L4 where both programs already link it; **the gathering stays in the studio**, because "somebody is looking at it" is a question only an editor can ask and a scoped run means nothing to a server. **The dividend arrived before the de-duplication did**: `mono.server` is deliberately still not a caller — giving it suspension changes the behaviour of a program whose output `just determinism` compares byte for byte — so nothing is shared yet, and what changed is that a policy previously reachable only by opening the editor and waiting five minutes now has eight cases, two of which were pure comment before. Moving it also surfaced an ordering bug: the idle-clock lookup sat ahead of the suspended-world case, so a teleport waiting on a closed world would have been delayed a frame. Placement — which host a world runs on, and what happens when it dies — is untouched and still has no caller
- [x] **the local store bakes, and content is fetched because something names it** — two bugs with one shape: nothing checked what a store actually contained. `cdn::PublishLocal` published `raw/` **directly**, so a PNG imported through the assets panel reached a client as a PNG and `assets::Texture::Read` refused it — `Texture.hpp`'s whole argument is that a runtime does not decode. This repository's own store held **231 PNGs, 12 BMPs, 6 PMX models and a GLB, and the engine could read none of them**: an `ImageLabel` drew its missing-image marker, a part's `ColorMap` did nothing at all, a `MeshPart` drew the fallback cube, and the content picker offered every one of them as a choice. So the store has a **`baked/`** folder now and publishes that; **nothing in `mono.cdn` bakes**, because a baker is `Engine::bake`'s decoders and that is exactly the interpretation an origin must not do — the two programs that can link both do it instead. Publishing an unbaked store is **refused**, because an empty manifest written over a working one reads as a store somebody deleted. The second bug only appears once the first is fixed: `RequestKind` asked for **every** texture in the catalogue, `render::TextureTable`'s 512 MB went in manifest order, and 1,463 uploads were refused — so the sheets a scene named were usually among the refused. Textures are demand-driven now (`client::CollectWantedTextures`), meshes and materials still by kind because both are small and a material has to arrive before anything knows which texture it names. **A material's sheet is deliberately not requested when the material arrives**, which was the version that did not work: 295 materials arriving is 295 textures requested, which is requesting by kind again one indirection later. It reaches a part through `ResolveMaterials`, which writes it into a field the collector already reads. And `assets::IsRuntimeReadable` is the one place the two halves of the extension table are told apart, so a picker no longer offers a `.pmx` that can never draw
- [x] **a constant identity for the local store** — `cdn::DevelopmentSigningKey`, in the source and not a secret. A signature answers "did the publisher I trust produce this", and for a folder on somebody's own disk serving their own editor the answer is always yes; the key was pure friction, with the same sixty-four characters typed for `--publish` and again for `--publisher-key` and a client that refused every asset when one was wrong. **The client trusts it only when it is using the default local store and nobody named an origin** — naming any source leaves the key required, because a key everybody knows is not a trust boundary and must never become one for content somebody else served. `cdn --publish` still requires one outright
- [x] **the interface's image hook, connected** — `ui::ImageSource` and `render::InterfacePass::SetImageSource` are both hooks taking a resolver "from whoever owns the textures", and **nothing had ever supplied one**, so every `ImageLabel` in the engine drew the atlas's white texel whatever had loaded. The seam was right and one end of it was never connected — on both paths, the client's own GPU pass and the studio's imgui painter. The size travels with the handle, because a nine-sliced or tiled image is laid out in source pixels and a resolver returning a handle alone draws every slice at the wrong scale
- [x] **`.gif` as an animated texture** — the pixels, the grid, the frame count and the rate all arrived at v0.10 and **every pass sampled cell zero**, so a GIF was a still of its first frame — which reads as the decoder having dropped the animation rather than as nothing advancing it. `render::FlipbookCell` is the half that advances it, and it is arithmetic rather than a system because *three* passes need the same answer: the opaque pass for a part, the interface pass for an `ImageLabel`, and the studio's imgui painter for the same label in a viewport panel. **A scale and an offset rather than a cell index**, so a still image is the identity and no shader needs a branch per fragment. **`fract` before the transform**, because an imported mesh's UVs run past one and the sampler repeats them — right for a whole sheet and catastrophic for a cell, where 1.2 would land two frames along and show several at once. **Wrapped on the frame count and not on the grid**: a 24-frame GIF lands in an 8x8 with forty cells empty, and walking all sixty-four would spend five eighths of every loop blank. The clock is the caller's, for the standing rule every module here keeps — so a paused editor holds its frame with no second mechanism, and the modulo is taken in `double` because a `float` count stops resolving whole numbers after nineteen days and the animation would stutter and then stop. Verified against this store's own `fox_dance.gif`: two captures from a pinned camera, 50,880 pixels different and all of them inside the part
- [x] **a raw tab in the picker that bakes on demand** — `raw/` was invisible to the picker, so a file somebody had just dragged in could not be chosen until the whole store was republished, which on this repository's store is four minutes. Two tabs now, and picking a raw row bakes **that one source** through `assetc::Settings::Only` — a filter on the real walk rather than a second entry point, so a material picked on its own still has its colour map rewritten through `BakedName`; a shortcut path would be a second spelling of that rule failing only on the editor's side, which is the half nobody tests. The baked bytes go straight into this editor's renderer, so the asset appears in the viewport before any publish, and the tab says plainly that a publish is what a *client* needs. **The name written into the property is the baked one**, because a picker handing back a `.png` would put a source form on a `ColorMap` — the exact thing this store spent four versions doing
- [x] **an edited world is drawn where it is, not where it started** — `World::Present` runs `PreRender` alone and `capture-previous` is a `PreSimulation` system, so a *suspended* world never updates `PreviousTransform`: the draw list interpolated from wherever each part was created toward its real transform, at an alpha that also never advanced. A part dragged in the properties panel stayed at its birthplace while its selection outline moved away from it — two halves of one frame disagreeing about where something is, which reads as a renderer fault. A world that is not ticking is presented at **alpha one**, because there is no next tick to draw towards. **The obvious fix was the wrong one and is recorded rather than quietly dropped**: making an authored write clear `PreviousTransform` — "a teleport, not a simulation step" — reads well and breaks every scripted animation in the engine, because `Rings.luau` sets `CFrame` once a tick and relies on interpolation for smooth motion at 300 frames a second. `client.scene.tick` caught it
- [x] **the editor fetches content** — its delivery client existed and **nothing ever asked it for anything**, and its default source list was an HTTP origin nobody runs with no publisher key, which `DeliverySettings::IsValid` refuses — so `MakeAssetClient` was never called. A `MeshPart` drew the fallback cube however good its `MeshId` was, a `ColorMap` drew nothing, and the assets panel listed a store full of content none of which the viewport could show. Nothing said why: an unconfigured delivery client and one whose fetches fail look identical from a viewport. The default is the store on this machine plus `cdn::DevelopmentPublisher`, and the pump is `Client::PumpContent`'s policy one program over — meshes and materials by kind, textures on demand
- [x] **`BasePart` is what its subclasses share** — `Mesh` and `ColorMap` are `MeshPart`'s `MeshId` and `TextureID` now and nothing else's. A mesh reference on a `Part` was a picker that did nothing. The *storage* is unchanged and stays dense on `BasePart` for `CollectInstances`' reason; what moved is the vocabulary. One spelling each also ends the trap `studio/Assets.hpp` warned about, where an alias missing from the picker's table gave a plain text field on the name people use
- [x] **`PVInstance` pivots** — `Pivot::Offset`, `PivotOffset`, and `GetPivot`/`PivotTo` on the script surface. A placement says where a thing's centre is and almost nothing is placed by its centre: a door turns on its hinge, a lid sits on its rim. `PivotTo` is `target * Offset⁻¹` and the inverse is the whole of it — setting the transform to the target and hoping is what "PivotTo ignores the offset" bugs are. Identity is the default, so a part nobody has given a pivot behaves exactly as it did before the field existed
- [x] **the asset picker is clipped, and three bugs went with it** — it listed every texture in the store, 1,637 of them, submitting a `Selectable` and asking for a thumbnail for each on every frame the modal was open. That is a visible stall; worse, `ThumbnailFor` queues what it is asked for against a 256-entry cache, so asking for 1,637 a frame evicted each picture long before its turn to be built came round — the previews were not failing, they were being thrown away, and the list looked permanently broken after the first interaction. The picker was small when it was written and stopped being small when the store filled up
- [x] **the gallery scans once rather than every frame** — `DrawGallery` rebuilt when `Gallery.empty()`, and an empty result *is* a result: a place naming no assets rescanned every entity of every world on every frame the panel was open, which is what "the studio greys out for three seconds when I insert something" was. The gate is "have I scanned", and a structural change invalidates it
- [x] **previews follow the store's layout** — every preview read `raw/<name>`, defended by a comment that was true for exactly as long as the publisher walked `raw/`. The day it walked `baked/`, every published name resolved to a file that was not there and silently became "no local pixels". `cdn::FindInStore` is the one place that knows where an asset lives
- [x] **nothing is fetched by kind** — the editor froze for half a minute on start-up and the reason is one sentence from `delivery/Client.hpp`: *the unit that travels is a bundle*. Asking for every mesh and material by kind asks for essentially every bundle in the store, and `AssetClient::Pump` resolves, verifies and decompresses all of it **on the calling thread** — deliberately, because a completion arriving at a moment scheduling chose would be a desync. On this repository's own store that is 6.9 GB through one function on the frame the studio opens. A world names an asset or it is not fetched: **studio start-up went from about 29 seconds to 1.4**, and a scene naming one mesh fetches one mesh, in 26 ms. **Asynchrony here cannot mean a thread and does not pretend to** — what is spread is the *asking*, a bounded number of new requests per pump, so a large place arrives over a second or two instead of in a frame that never ends
- [x] **the picker wrote nothing, and one unset field is the whole of four bug reports** — "setting a mesh does nothing", "the cube never changes", "the texture never appears", "the mesh id stays blank". `game::PropertyValue` defaults to `PropertyType::Opaque` and the picker's confirm built one by hand, filling the name and never the type; `game::WriteProperty` refuses a type mismatch on its **first line**, and refuses silently, because a refusal there is ordinarily a script error somebody else reports. So the editor's most obvious control had been inert for five properties for a version, and every symptom pointed at content delivery rather than at the panel. **The fix is a constructor rather than an assignment** — `ChosenContentValue(type, chosen)` is the one place a chosen row becomes a value, so the type cannot be filled in by whoever remembers; the `...` button now carries `descriptor->Type` to it. Worth stating because the shape recurs: a struct whose default is a *refusal* is safe against the code that forgets to set it and useless against the code that half-sets it
- [x] **the mesh preview blanked the whole editor**, which is the same bug class one layer up and was reported as "hovering a row makes the engine go blank". `RenderPreviewSlot` passed `nullptr` where the interface goes, so the frame it drew wrote no imgui at all — and `Renderer::Render` owns the swapchain, so it cleared and presented an empty one. The panel was not failing to draw a mesh; it was drawing a mesh *instead of the editor*. **And the preview took the frame rather than a slot in it**: it returned early from `PresentWorld`, so a hovered row pre-empted every viewport for as long as the pointer sat there. It is a rotation candidate now, which is what the round-robin was already for. The mesh turns — `TURNTABLE_RADIANS_PER_SECOND` about the camera, not the mesh, so nothing writes a component to animate an icon — and built-ins resolve through `assets::MakeBuiltin` rather than through the store, because the six shapes the engine ships have no bundle to fetch
- [x] **a scene can ask what the store published**, which it could not, and the reason is the previous version's own rule biting. `GetMeshes()` reports what the catalogue *holds*, and since "nothing is fetched by kind" a catalogue holds only what something already named — so a scene reading it to decide what to name can never be the thing that asks, and the example grid filled only for a place that already named imports. `scene::PublishedCatalogue` is a derived resource holding `core::Name`s, and `ContentService:GetPublishedMeshes()` reads it. **Names rather than `cdn::PublishedEntry` because `scene` is L7 and cannot see `assets::AssetKind`** — the filtering by kind and by `IsRuntimeReadable` happens in the studio, which links both, and what crosses is the answer rather than the question. Offered per pump against a count, not once, because `Play` mints a server world and a client replica *after* the catalogue opens and a one-shot offer leaves both empty
- [x] **`MeshPart.Size` is a box and not a multiplier, which is what makes the cull correct** — reported as "the culling is deleting them". `graph::CullAndBound` tests `scene::Bounds` against the frustum, and nothing had ever tied the drawn geometry to that box: a PMX baked at authored scale extended ten times past the bounds describing it, so it vanished while still plainly on screen. `MeshEntry` now carries the mesh's own `Centre` and `Extent`, derived by `Mesh::Read` from the vertices so a file cannot lie about its own, and the renderer scales by `HalfExtent / Extent` — per axis, so a part's size *stretches* the mesh onto it rather than fitting it inside. **Geometry fills its bounds by construction**, which retires the mesh-aware cull this was going to need instead. Built-ins are a unit shape about their own origin, so `HalfExtent / 0.5` is `HalfExtent * 2` and every built-in draws exactly as it did before the field existed. The centre is subtracted before scaling, so a model authored off-origin sits where its part is
- [x] **swapping a mesh keeps the part's size, and that is a name rather than a flag** — selecting a `MeshId` fits the part to the mesh's natural bounds once, so a part resized afterwards stays resized and a *replacement* mesh does not resize it back. `Visual::Fitted` records which mesh the box was last shaped to, so `Fitted != Mesh` **is** the condition. A bool would need clearing in every path that writes `Mesh` — the properties panel, a script, a game file, a replication delta — and whichever one forgot would produce a part that silently stopped fitting. It is serialised, which is what makes reopening a place leave sizes alone, and it costs four bytes on every part including every plain `Part` that will never name a mesh — the trade `SurfaceAppearance` already makes to keep `CollectInstances` a flat walk. `sizeof(Visual)` is pinned by a padding test, which failed on the first build and now carries the reasoning
- [x] **a texture reference that resolves to nothing is now refused rather than baked** — reported as "textures aren't loading for a bunch of them". `assetc::Resolve` is purely lexical and never asked whether the file it named exists, and `ImportFile` flattens the store to `<hash><extension>` — so importing a model **destroys the directory relationship its texture references are written against**, and every one of them resolved to a plausible path that was not there. `cdn::StoreTextureResolver` walks the content log back the other way: model hash → original path → its directory → the reference → normalise → hash. What the bake gains beside it is the check that was always missing — the resolved file must be a regular file in the input tree — with `Report::DanglingTextures` counting what it dropped and a warning naming the model and the reference. **The pre-existing test would have passed on every day the bug was live**: it asserted the bitmap baked, not that the model still pointed at it
- [x] **custom models bake at unit scale** — `Settings::ModelSize = 1.0f` in both `contentimport` and the studio, so an imported mesh's natural bounds are a unit box whatever the authoring tool thought a metre was. This was load-bearing when it landed and is now merely conventional: the stretch above makes a mesh unable to escape its bounds at any bake scale, so what the unit bake buys is a library where two models are the same size until somebody says otherwise
- [x] **the headless smoke shot photographs a named world** — `--capture-world NAME` moves the active viewport before the first frame, because a capture photographs whichever world the drawing panel holds and only the panel that drew has anything in it. A name nothing answers to warns and falls back rather than aborting, since a capture is a diagnostic and one that killed a run over a renamed scene would be worse than one that shoots the default. `studio-smoke` takes a second shot of the mesh grid at **700 frames rather than twelve** — naming a mesh is what fetches it, the fetch spans frames, and the grid grows on `Heartbeat`, so an early capture photographs an empty plate and asserts nothing. **This is the gap that mattered**: every other check here is a count, and a count reads identically whether the models are right, squashed, inside-out, untextured or culled, all five of which happened at least once
- [x] **the light cap has one home — `D00029` closed** — `render::MAX_SCENE_LIGHTS` was 16 and `MAX_LIGHTS` in `opaque.frag` was 16, and the only thing keeping them equal was that somebody wrote both. `mono_add_library` takes `SHADER_DEFINES` now and the configure **reads** the value out of the header to pass `-DMAX_LIGHTS` to glslc, so the shader keeps no literal to drift. **The number stayed in C++ rather than moving into the build**, which is where the deferred entry's plan changed: `Renderer.hpp` is included by targets that do not link `render`, so a compile definition would have needed a fallback default — the second spelling again. **Demonstrated by mutation**: the header set to 8 changes the command line and the staged SPIR-V's hash. There is deliberately no test, because the disagreement is now unrepresentable rather than detectable — and a test could never have read the number anyway, which is what the first attempt at this discovered
- [x] **a part is cleared when its material goes away — `D00032` closed** — deleting a `Material` left the colour map it last resolved sitting on the parent for ever, because nothing walks a part that has no material. **Not closed with the destruction hook the entry proposed, which would have been harder and weaker**: `DropAttributes` is a hard-coded call inside `DestroyEntity` rather than a registration, and `scene` sits above `ecs` — but more to the point, a hook catches only destruction, and a `Material` **reparented** to another part destroys nothing while leaving exactly the same stale texture. What shipped is a difference between two passes: `ResolveMaterials` records the parents it wrote and the next pass clears any it did not write again, which is O(materials) rather than the O(parts) walk the entry rejected. **The entry named the symptom it had seen and wrote the fix for that symptom** — the trigger was "deleted" and the defect was "stopped being written". Handles rather than indices, because a freed index is reused immediately; the set is cleared on load, because a handle does not survive a directory replacement
- [x] **the mirror stopped flashing — `D00027` closed** — once per orbit the reflected camera whipped 180°, because `facing` flips sign as the viewer crosses the pane's plane and there is no continuous path between the two. **Fixed by deciding what a surface view means rather than by arithmetic**: a pane the viewer is level with covers no pixels, so within `EDGE_ON_MARGIN` it renders nothing and its pane comes off its slot. The sign still flips and no frame is shown while it does. Worst single-step turn over a 360-sample lap went from **exactly 2.0 to exactly 0**, and the zero is not a tuned bound — within one side of the plane the orientation does not change at all. Closing it forced the test to be rewritten, which surfaced the better question: continuity is only owed to frames that *draw*, and a second assertion now requires the blank band to actually be entered so the test cannot pass by no longer crossing the plane. **Two existing cases turned out to be pinning the degenerate arrangement without saying so** — one required a clamped frustum for a view nobody can see, and "a rotated pane reflects along the way it actually faces" had its eye exactly level with the rotated normal, so it passed for a reflection that was never computed

- [x] **`just docs-check`'s first pass is green, and the pass behind it has run for the first time — `D00035` closed** — the recipe had been failing on 19 lines, and **fixing the first one made the count go up**: the site pass had been stopping at a dangling `README` link, so the seven it then reported had never been seen, two of them live source defects where a new member's doc block had been inserted *inside* an existing one and left `bake::Graph::AddWrite` and `render::Renderer::TextureHandle` documented as the wrong function. The eighteen remaining had one cause, and it is a collision between a Doxygen setting and this repository's house style: `JAVADOC_AUTOBRIEF` ends the brief at the first sentence stop and does not care that the stop is inside emphasis, so `**A bold sentence.**` is split between the `**` and its partner. **Emphasis spanning a line break was the wrong suspect and was believed for an afternoon** — the first minimal reproduction held the stop inside the bold while dashes, quotes and wrapping were varied around it, so everything failed and wrapping took the blame. `docgen::Promote` now moves the stop outside the emphasis, which preserves the line count every source link depends on and leaves 355 bolds across 145 headers alone. The last one was in Markdown, which is not filtered: a code span containing apostrophes, found by neutralising each of its line's sixteen spans in turn. **What that uncovered is `D00036`** — the coverage pass behind it reports 307 undocumented public entities, and it had never completed once

- [x] **the asset that baked and never published has a name — `D00034` closed** — the store re-baked to 1974 raw and 1973 baked, and the gap was only ever visible by subtracting two numbers printed by two different tools. It was not the baker and not the publisher: `cdn::ImportFile` accepted a **zero-byte file**. Found by diffing the two folders by hash, which left exactly one — `af1349b9…`, BLAKE3's empty-input digest — and reading the content log, which named `blender-dragon/.venv/.lock`, a Python virtualenv lock file swept along by the folder import of a model. **The entry's diagnosis was right and its location was wrong**, which is the part worth keeping: "the reporting is the defect, not the missing asset" held exactly, but the missing report was three stages upstream of where it was looked for — `cdn::Publish` already names an empty file it skips, and by then the file has been in `raw/` for good. It is refused at import now, by name, with nothing written and nothing logged, because a refusal that left the file behind would move the silence rather than remove it

- [x] **a rendered preview outlives the cursor — `D00033` closed** — a mesh or a material had a picture only while it was hovered, because the preview slot is scratch: there are a handful, they are drawn into on rotation, and whatever is in one is gone within a few frames. `Renderer::CaptureSceneTexture` blits a slot into a new texture and publishes it through a new `TextureTable::Adopt`, **device to device** — reading a picture back to the host only to upload it again would be a round trip across the bus for bytes that never needed to leave the GPU. **`Adopt` transfers ownership, which is the whole contract**: the table releases on `Drop`, on replacement and on `Shutdown` exactly as for one it uploaded, so every refusal has to leave the caller still owning the texture, and a full table returns before releasing what was under the name rather than after. **The drawn rectangle is copied and not the allocation** — a target is rounded up to 64-pixel blocks, so copying it whole would keep an unwritten margin down two edges and force every consumer to carry `SceneTextureExtent` beside the handle. It lands in the studio's existing thumbnail cache rather than beside it, so `PumpThumbnails` evicts a capture by the same rule and the same call, and is taken once per asset rather than once per frame. **Every refusal is tested and none of the success is**: a capture needs a device, and standing a fake in front of that would test the fake

- [x] **every public entity is documented, and the check that says so has run to completion for the first time — `D00036` closed** — `docs-check`'s coverage pass had never finished, because the site pass in front of it had been red for two versions and the recipe stops at the first. Fixing that (`D00035`) let it report **307** gaps, all of them genuinely public. **About a third were unattached rather than unwritten**: `ecs::AttributeValue`'s fifteen payload fields are one idea, and `gui::Describe`'s nineteen overloads sit under one paragraph about all nineteen — Doxygen attaches a comment to the declaration beneath it, so the rest counted as missing. `DISTRIBUTE_GROUP_DOC` plus `//@{` documents each family once, which is not a lowered bar: the comment is still written and the group still marked by hand. The remainder was writing, and the entries worth having are the ones where a field's name hides the trap — `Delta::Baseline`, why `Forgotten` is never merged with `Destroyed`, and `Refused` against `Deferred`. It also turned up **three more orphaned doc blocks** of the kind that left `bake::Graph::AddWrite` documented as the wrong function, all three added by this version's own mesh work. `just docs-check` exits 0

### v0.11

- [_] extended rendering pipeline (handle multiple worlds in parallel, handling gpu traffic) — ~~`RENDER_PIPELINE.md` stages 8 to 12, including the HDR and G-buffer prerequisite its §17 opens with~~ **corrected: those stage numbers do not index this repository.** That document's §16 stages a graph renderer built *beside* an existing one — stage 8 is "switch the default, keep `--renderer=v1`" and 8b is "strip v1" — and it records stages 1, 3 and 5 as done *there*. Here, `NodeDesc`, `ExecContext`, `ChainCache`, `SceneParts`, `GeometryNode`, `SwapchainPresent`, `CapabilitiesOf` and `ClearPolicy` appear in **zero** files, `graph` is four headers of description with no executor, and there is no second renderer to switch a default away from. The work this line actually names is that document's stages **1, 3 and 5** adapted — skeleton and interpreted executor, capabilities, port the passes — then 10, then 11 and 12. **Two of its stated blockers are already retired here**: §17 calls runtime shader compilation unverifiable until shaderc is vendored, and this engine has linked it since v0.1; §14's physics-tick split, which §16 says to do early because it changes what every frame measurement means, shipped at v0.1. Planned in `v11.md` at the repository root
- [_] convert the entire rendering pipeline to the node system with a studio editor? then during play, it'll compile and work. Add a node editor like blender's system as well for both the mesh pipeline and the rendering pipeline, as separate widgets called Render Pipeline and Assets Pipeline. I plan to have all assets be managed in Assets Pipeline using many node trees in one editor (and same for render but as different subpipelines).
- [_] improved physics pipeline with spatial optimisations

### v0.12

- [_] deferred items
- [_] studio editing tools; select, move, rotate, 3d scene interactable gimbals, grid step amount input, rotation amount input, anchor toggle, "lock" toggle, pivot editor mode, reset pivot button. Reference "~/Pictures/Screenshot from 2026-08-08 15-59-40.png" and "~/Pictures/Screenshot from 2026-08-08 16-00-19.png".
- [_] studio tooling tabs
- [_] plugin system
- [_] expose ECS underlying to luau and typescript (locked down but can query engine for entities using direct and returns instances)
- [_] rojo folder syncing + tests, support multi-world by subfoldering (main.universe.json for universe mapping, or assume per-viewport state for rojo-like sync system with main.default.json being in each subfolder)
- [_] world => rojo sync, universe => multi-rojo sync (i reckon keep them separately syncing so even if one has bad formatting, the rest still sync)
- [_] update built-in MCP integrations, add a studio ui button to enable/disable and information panel
- [_] create a unified networking system for LAN, Peer2Peer and remote connections. Put in mono.network, then, build by import for the engine, studio and the cdn. I plan the engine to support direct connections between users (the host runs a server + client, then other clients connect to the local server) like LAN and Peer2Peer, then the studio supports it as well for team create with many users, then the cdn supports it to support different distribution streams like LAN, Peer2Peer, private (key accessed) networks and public distribution.

---
