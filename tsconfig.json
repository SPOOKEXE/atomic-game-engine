{
	"$comment": [
		"The TypeScript authoring surface, pointed at the generated type root.",
		"",
		"`mono.engine/script/bindings/engine.d.ts` is generated by mono.tools/bindings",
		"from the class table -- it is listed in `files` rather than installed as a",
		"package, which is the shape a toolchain consumes in place of `@rbxts/types`.",
		"Run `just bindings` after changing what a script can name, and `just",
		"bindings-check` fails the build if the checked-in file drifted from it.",
		"",
		"`types: []` and no `lib.dom` on purpose: the VM is QuickJS-ng, not a browser",
		"and not Node. Nothing here should complete `document` or `require`.",
		"",
		"`moduleDetection: force` gives every example its own scope. They are separate",
		"scripts loaded into separate worlds rather than one program, so two of them",
		"declaring `PLATE` is not a redeclaration -- which is what a shared global",
		"scope would call it.",
		"",
		"Typecheck with `bunx tsc --noEmit` (or `npx tsc --noEmit`). Nothing emits:",
		"the engine compiles TypeScript through its own path, and this file exists",
		"for the editor and for the check."
	],
	"compilerOptions": {
		"target": "ES2020",
		"lib": ["ES2020"],
		"module": "ESNext",
		"moduleDetection": "force",
		"moduleResolution": "bundler",
		"types": [],
		"strict": true,
		"noEmit": true,
		"skipLibCheck": false,
		"forceConsistentCasingInFileNames": true
	},
	"files": ["mono.engine/script/bindings/engine.d.ts"],
	"include": ["mono.engine/examples/**/*.ts"]
}
