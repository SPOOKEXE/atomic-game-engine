# Planted

`just em-dash-check` scans this file and fails when it is not reported.

The line after this one carries the character — and nothing else here does.

`expect` beside this file names the line, so a rewrite that shifts it fails
loudly rather than quietly matching nothing.
